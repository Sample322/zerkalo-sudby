---
phase: 01-foundation-telegram-auth
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - backend/app/seed/__init__.py
  - backend/app/seed/__main__.py
  - backend/app/seed/loader.py
  - backend/app/seed/data/topics.json
  - backend/app/seed/data/decks.json
  - backend/app/seed/data/spreads.json
  - backend/app/seed/data/cards.json
  - backend/app/seed/data/prompts.json
  - backend/tests/integration/test_seed.py
autonomous: true
requirements: [INFRA-03]

must_haves:
  truths:
    - "`python -m app.seed` loads exactly 7 topics, 6 decks, 7 spread_types (with their positions), 78 cards, and the base prompt_templates"
    - "Running the seed twice produces identical row counts with no duplicate-key error (idempotent upsert by slug)"
    - "Seed slugs match TZ §27 exactly (topics love/work/money/choice/day/self_reflection/general; decks classic_arcana/moon_mirror/shadow_arcana/heart_oracle/path_deck/forest_oracle; spreads three_keys/thread_of_time/between_us/two_roads/day_three_signs/resource_and_risk/what_is_in_shadow)"
    - "Seed data contains only original deck names and style-free universal card meanings — no RWS/commercial deck references"
  artifacts:
    - path: "backend/app/seed/loader.py"
      provides: "idempotent upsert-by-slug loader run in FK-safe order"
      contains: "on_conflict_do_update"
    - path: "backend/app/seed/data/cards.json"
      provides: "78 universal tarot card rows (22 major + 56 minor) with placeholder meanings"
      contains: "slug"
    - path: "backend/app/seed/data/prompts.json"
      provides: "base prompt_templates incl. system, final_summary, single_card, 6 deck_modifier, safety, refusal"
      contains: "deck_modifier"
  key_links:
    - from: "backend/app/seed/__main__.py"
      to: "backend/app/seed/loader.py"
      via: "invokes loader against an AsyncSession"
      pattern: "loader"
    - from: "backend/app/seed/loader.py"
      to: "backend/app/models"
      via: "upserts model rows"
      pattern: "from app.models"
---

<objective>
Make the schema useful: ship the MVP seed content as JSON files and load it via an idempotent `python -m app.seed` CLI that upserts by slug in FK-safe order — 7 topics, 6 decks, 7 spreads (+ positions), 78 universal cards (placeholder meanings, all rows present), and the base prompt_templates (system / single_card / final_summary / 6 deck_modifier / safety / refusal). Re-running is safe.

Purpose: The core value ("same question, different deck") lives in seed data (6 deck prompt_modifiers + templates); locking these slugs and rows now lets Phase 2 render the catalog and Phase 4 assemble prompts. Satisfies INFRA-03. Keeps card-meaning text minimal but complete (78 rows) per the downstream-consumer directive.
Output: `backend/app/seed/data/*.json`, `backend/app/seed/loader.py` + `__main__.py`, and a green `tests/integration/test_seed.py` (exact counts + idempotency).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-telegram-auth/01-RESEARCH.md
@.planning/phases/01-foundation-telegram-auth/01-VALIDATION.md
@.planning/phases/01-foundation-telegram-auth/01-02-SUMMARY.md
@backend/app/models/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed JSON data files (topics, decks, spreads+positions, 78 cards, prompts) + idempotent loader CLI</name>
  <files>backend/app/seed/__init__.py, backend/app/seed/__main__.py, backend/app/seed/loader.py, backend/app/seed/data/topics.json, backend/app/seed/data/decks.json, backend/app/seed/data/spreads.json, backend/app/seed/data/cards.json, backend/app/seed/data/prompts.json</files>
  <read_first>
    - .planning/REFERENCE-TZ.md §27 (exact seed slugs/titles for topics, decks, spreads + card_counts) and §13.10 (prompt_templates type set: system/single_card/final_summary/deck_modifier/safety/refusal) and §13.3 (cards columns)
    - .planning/phases/01-foundation-telegram-auth/01-RESEARCH.md (Pattern 6 Seed Strategy — idempotent upsert_by_slug, FK load order topics→decks→cards→deck_cards→spread_types→spread_positions→deck_spread_compatibility→prompt_templates; recommend CLI not Alembic data-migration)
    - .planning/research/ARCHITECTURE.md (Data Model Boundaries — cards hold universal meaning only; IP-clean, no commercial deck names)
    - backend/app/models/__init__.py (model classes + enum value sets to populate)
  </read_first>
  <action>
    Create JSON seed files under `backend/app/seed/data/`:
    - `topics.json`: the 7 topics from §27.1 (slug, title, sort_order, is_active=true).
    - `decks.json`: the 6 decks from §27.2 (classic_arcana, moon_mirror, shadow_arcana, heart_oracle, path_deck, forest_oracle), each with slug, title, a short original subtitle/description/atmosphere/tone, a distinct non-empty `prompt_modifier` (the core-value differentiator — give each deck a genuinely different tonal instruction), `visual_style` JSONB (at least an accent color/mood key per the 6-palette intent), `recommended_topics` (subset of the 7 topic slugs), access_type "free", is_active=true, is_mvp=true, sort_order. Original names only — NO RWS/Rider-Waite/commercial references.
    - `spreads.json`: the 7 spread_types from §27.3 with correct `card_count` (three_keys/thread_of_time/between_us/two_roads/day_three_signs = 3; resource_and_risk/what_is_in_shadow = 4), recommended_topics, plus each spread's `positions` array (length = card_count) with position_index, title, description, and a meaningful `prompt_instruction` per position (TZ §13.6 example shows the style).
    - `cards.json`: all 78 universal tarot cards — 22 Major Arcana (arcana_type "major", number 0–21) + 56 Minor (arcana_type "minor", suit ∈ wands/cups/swords/pentacles, number 1–14 each). Each row: slug (e.g. `major_00_fool`, `wands_01`), title (original/style-free), title_en, keywords_upright/keywords_reversed (TEXT[]), and PLACEHOLDER but non-empty meaning_upright/meaning_reversed/advice_upright/advice_reversed (a short neutral sentence each is fine — full copy is a content task, not code). Style-free meanings only (universal layer).
    - `prompts.json`: base prompt_templates — one `system`, one `single_card`, one `final_summary`, six `deck_modifier` (one per deck slug, slug like `deck_modifier_classic_arcana`), one `safety`, one `refusal`; each with slug UNIQUE, title, type, a non-empty `template_text`, version "v1", is_active=true. The `safety`/`refusal` templates carry the crisis-safe copy seed (used by Phase 4 SafetyService).
    Create `backend/app/seed/loader.py` with `async def upsert_by_slug(session, Model, rows)` using `pg_insert(Model).values(**r).on_conflict_do_update(index_elements=["slug"], set_={k:v for k,v in r.items() if k!="slug"})`, and an orchestration `async def run_seed(session)` that loads files and upserts in FK-safe order; for spreads it also upserts `spread_positions` keyed by (spread_type_id, position_index). Create `backend/app/seed/__main__.py` that opens an `AsyncSession` and runs `run_seed`, committing once, so `python -m app.seed` works. Cards/decks/spreads relationships that need deck_cards/deck_spread_compatibility are NOT required for Phase 1 counts (those are populated when art/compat are authored in Phase 2); seed only the rows the INFRA-03 counts require (topics, decks, spread_types+positions, cards, prompt_templates).
  </action>
  <verify>
    <automated>cd backend && alembic upgrade head && python -m app.seed && python -m app.seed && python -c "import json; assert len(json.load(open('app/seed/data/cards.json')))==78; assert len(json.load(open('app/seed/data/decks.json')))==6; assert len(json.load(open('app/seed/data/spreads.json')))==7; assert len(json.load(open('app/seed/data/topics.json')))==7; print('counts-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `cards.json` has 78 entries (22 major + 56 minor with the 4 suits); `decks.json` 6; `spreads.json` 7; `topics.json` 7.
    - `prompts.json` contains exactly the type set with 6 deck_modifier entries (one per deck) + system + single_card + final_summary + safety + refusal.
    - `python -m app.seed` runs twice with no duplicate-key error (idempotent).
    - Deck names and card meanings are original/style-free (no commercial deck references in any seed file).
    - Each deck has a distinct non-empty `prompt_modifier`.
  </acceptance_criteria>
  <done>Seed files at the exact §27 slugs load idempotently via the CLI; 7/6/7/78 counts + base prompt templates present; IP-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Seed integration test — exact counts + idempotency</name>
  <files>backend/tests/integration/test_seed.py</files>
  <read_first>
    - .planning/phases/01-foundation-telegram-auth/01-VALIDATION.md (test_seed_counts: topics=7, decks=6, spreads=7, cards=78, prompt_templates=N; test_seed_idempotent: run twice → same counts, no dup-key)
    - backend/app/seed/loader.py (run_seed entrypoint)
  </read_first>
  <action>
    Replace the skipped stub `tests/integration/test_seed.py` (from Plan 01 Wave 0). `test_seed_counts`: against the migrated test DB, run `run_seed` then assert SELECT count(*) == 7 topics, 6 decks, 7 spread_types, 78 cards, and == the expected prompt_templates count (system+single_card+final_summary+6 deck_modifier+safety+refusal = 11), and assert spread_positions count equals the sum of card_counts (3+3+3+3+3+4+4 = 23). `test_seed_idempotent`: run `run_seed` twice and assert identical counts with no IntegrityError. Remove the `@pytest.mark.skip`.
  </action>
  <verify>
    <automated>cd backend && alembic upgrade head && pytest -q tests/integration/test_seed.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q tests/integration/test_seed.py` exits 0 with both tests running (skip removed).
    - Test asserts exact counts 7/6/7/78, prompt_templates == 11, spread_positions == 23.
    - Second `run_seed` causes no IntegrityError and counts are unchanged.
  </acceptance_criteria>
  <done>Seed counts + idempotency proven against the real test DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed CLI → PostgreSQL | Code-managed content rows are written; re-runnable, no user input involved |
| seed content → public product | Deck names + card meanings become user-visible; IP/brand-voice constraints apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Re-running seed double-inserts / corrupts catalog | mitigate | Upsert-by-slug `on_conflict_do_update`; idempotency unit-tested. |
| T-03-02 | Information Disclosure / Legal | Commercial deck/card references create IP exposure | mitigate | Original deck names + style-free universal meanings only; acceptance criterion forbids commercial references (PROJECT legal/IP constraint; full legal review gated to Phase 8). |
| T-03-03 | Tampering | SQL injection via seed content | accept | Seed rows are code-managed constants written through SQLAlchemy parameterized inserts (no string concatenation); not user-supplied. |
</threat_model>

<verification>
- `cd backend && alembic upgrade head && python -m app.seed && python -m app.seed` → no error (idempotent).
- `cd backend && pytest -q tests/integration/test_seed.py` → green.
- Manual grep of `backend/app/seed/data/*.json` confirms no commercial deck names.
</verification>

<success_criteria>
- INFRA-03: Seed loads 7 topics, 6 decks, 7 spreads (+ 23 positions), 78 cards, and base prompt templates idempotently via `python -m app.seed`, at the exact TZ §27 slugs, IP-clean.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-telegram-auth/01-03-SUMMARY.md` when done.
</output>
