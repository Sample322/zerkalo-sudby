---
phase: 01-foundation-telegram-auth
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/app/models/__init__.py
  - backend/app/models/base.py
  - backend/app/models/enums.py
  - backend/app/models/user.py
  - backend/app/models/deck.py
  - backend/app/models/card.py
  - backend/app/models/spread.py
  - backend/app/models/reading.py
  - backend/app/models/prompt.py
  - backend/app/models/billing.py
  - backend/app/models/analytics.py
  - backend/app/models/topic.py
  - backend/alembic/versions/0001_initial_schema.py
  - backend/tests/integration/test_migration.py
autonomous: true
requirements: [INFRA-02]

must_haves:
  truths:
    - "`alembic upgrade head` creates all 17 tables (16 from TZ §13 + the topics lookup table) with no error"
    - "`alembic downgrade base` drops them cleanly (reversible migration)"
    - "users.telegram_id is UNIQUE; decks.slug, cards.slug, spread_types.slug, prompt_templates.slug, products.slug, topics.slug are UNIQUE; payments.payload is UNIQUE"
    - "UUID primary keys, JSONB columns (visual_style, raw_update, event_properties), TEXT[] columns (recommended_topics, keywords_*), and native ENUMs for fixed status/type sets exist as specified"
  artifacts:
    - path: "backend/alembic/versions/0001_initial_schema.py"
      provides: "single initial migration covering all 17 tables + indexes + uniques + FKs"
      contains: "def upgrade"
    - path: "backend/app/models/reading.py"
      provides: "readings + reading_cards SQLAlchemy 2 typed models with status/orientation enums"
      contains: "class Reading"
    - path: "backend/app/models/user.py"
      provides: "User model with telegram_id BIGINT UNIQUE + profile/settings columns"
      contains: "telegram_id"
    - path: "backend/app/models/topic.py"
      provides: "topics lookup table (slug UNIQUE, title, sort_order, is_active)"
      contains: "class Topic"
  key_links:
    - from: "backend/alembic/env.py"
      to: "backend/app/models/__init__.py"
      via: "Base.metadata imports all models so autogenerate sees them"
      pattern: "from app.models"
    - from: "backend/app/models/reading_cards"
      to: "backend/app/models/cards + deck_cards + spread_positions"
      via: "foreign keys"
      pattern: "ForeignKey"
---

<objective>
Define the durable substrate: SQLAlchemy 2 typed models for all 16 TZ §13 tables plus a lightweight `topics` lookup table (orchestrator directive 1), and ONE initial Alembic migration that creates all 17 tables with the correct UUID PKs, JSONB/TEXT[] columns, native ENUMs, foreign keys, and the UNIQUE constraints later phases and the admin panel depend on.

Purpose: Lock the schema and slug keys now so Plans 03–08 (seed, auth, readings, payments, admin) never need DDL changes. Satisfies INFRA-02. The schema also enables backend-only card draw and limit checks built in later phases.
Output: `backend/app/models/*.py` (typed, importable via `Base.metadata`), `backend/alembic/versions/0001_initial_schema.py`, and a green `tests/integration/test_migration.py` asserting all 17 tables + key uniques via `information_schema`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-telegram-auth/01-RESEARCH.md
@.planning/phases/01-foundation-telegram-auth/01-VALIDATION.md
@.planning/phases/01-foundation-telegram-auth/01-01-SUMMARY.md
@backend/app/models/base.py
@backend/alembic/env.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SQLAlchemy 2 typed models for all 17 tables (16 TZ §13 + topics)</name>
  <files>backend/app/models/__init__.py, backend/app/models/enums.py, backend/app/models/topic.py, backend/app/models/user.py, backend/app/models/deck.py, backend/app/models/card.py, backend/app/models/spread.py, backend/app/models/reading.py, backend/app/models/prompt.py, backend/app/models/billing.py, backend/app/models/analytics.py</files>
  <read_first>
    - .planning/REFERENCE-TZ.md §13.1–§13.16 (the EXACT column list + enum value sets for every table — the source of truth; reproduced in <behavior> below for convenience but read the TZ for any ambiguity)
    - .planning/phases/01-foundation-telegram-auth/01-RESEARCH.md (Code Examples: typed model with UUID PK / JSONB / TEXT[] / PG enum; Open Question #2 the exact 16-table set; Open Question #3 indexes/uniques; recommendation to use native ENUM for fixed sets)
    - .planning/research/ARCHITECTURE.md (Data Model Boundaries — cards (universal) vs deck_cards (style) vs readings/reading_cards; boundary rules)
    - backend/app/models/base.py (Base, UUID PK default, timestamp mixins from Plan 01)
  </read_first>
  <behavior>
    Models must declare exactly these tables/columns (types per TZ §13; UUID PK `id` default uuid4 + created_at/updated_at from Base unless noted):
    - topics (NEW, orchestrator directive 1): id, slug TEXT UNIQUE, title TEXT, sort_order INT, is_active BOOL default true. Lookup only — NOT a FK target; readings.topic stays a TEXT slug, decks/spreads recommended_topics stay TEXT[].
    - users (§13.1): telegram_id BIGINT UNIQUE NOT NULL, username/first_name/last_name/language_code/photo_url TEXT NULL, is_premium_telegram BOOL default false, onboarding_completed BOOL default false, reversals_enabled BOOL default true, allow_history_personalization BOOL default false, status TEXT default 'active', created_at/updated_at/last_seen_at.
    - decks (§13.2): slug TEXT UNIQUE, title, subtitle, description, atmosphere, tone TEXT; visual_style JSONB; prompt_modifier TEXT; recommended_topics TEXT[]; is_active BOOL default true, is_mvp BOOL default true; access_type TEXT default 'free' (enum: free/premium/subscription/one_time_purchase/seasonal/limited); sort_order INT.
    - cards (§13.3, universal meaning ONLY): slug TEXT UNIQUE; arcana_type enum(major/minor); suit enum(wands/cups/swords/pentacles) NULL; number INT NULL; title, title_en TEXT; keywords_upright TEXT[]; keywords_reversed TEXT[]; meaning_upright/meaning_reversed/advice_upright/advice_reversed TEXT.
    - deck_cards (§13.4, style layer): deck_id FK decks.id, card_id FK cards.id, image_url, thumbnail_url, back_image_url NULL, visual_prompt TEXT; deck_specific_keywords TEXT[]; deck_specific_upright_modifier/deck_specific_reversed_modifier TEXT; is_active BOOL default true; sort_order INT.
    - spread_types (§13.5): slug TEXT UNIQUE, title, description TEXT; card_count INT; recommended_topics TEXT[]; is_active BOOL default true; access_type TEXT default 'free'; sort_order INT.
    - spread_positions (§13.6): spread_type_id FK spread_types.id, position_index INT, title, description, prompt_instruction TEXT.
    - deck_spread_compatibility (§13.7): deck_id FK decks.id, spread_type_id FK spread_types.id, compatibility_score INT, is_recommended BOOL default false, custom_note TEXT NULL.
    - readings (§13.8): user_id FK users.id, question TEXT, topic TEXT, deck_id FK decks.id, spread_type_id FK spread_types.id, status enum(pending/generating/completed/failed/deleted), reversals_enabled BOOL, summary_short/summary_full/main_factor/advice TEXT NULL, model_name/prompt_version/generation_error TEXT NULL, created_at, completed_at NULL, deleted_at NULL.
    - reading_cards (§13.9, immutable once written): reading_id FK readings.id, card_id FK cards.id, deck_card_id FK deck_cards.id, position_id FK spread_positions.id, position_index INT, orientation enum(upright/reversed), short_meaning/interpretation/mystical_accent TEXT, created_at.
    - prompt_templates (§13.10): slug TEXT UNIQUE, title, type enum(system/single_card/final_summary/deck_modifier/safety/refusal), template_text TEXT, version TEXT, is_active BOOL, created_at/updated_at.
    - user_limits (§13.11): user_id FK users.id, free_weekly_limit INT default 3, free_used_this_week INT default 0, week_start DATE, paid_spreads_balance INT default 0, subscription_spreads_limit INT default 0, subscription_spreads_used INT default 0, updated_at.
    - products (§13.12): slug TEXT UNIQUE, title, description TEXT, product_type enum(one_time_spreads/subscription), stars_price INT, spreads_amount INT NULL, subscription_days INT NULL, is_active BOOL, created_at/updated_at.
    - payments (§13.13): user_id FK users.id, product_id FK products.id, provider TEXT default 'telegram_stars', currency TEXT default 'XTR', amount INT, payload TEXT UNIQUE, telegram_payment_charge_id TEXT NULL (indexed), status enum(created/pre_checkout_approved/paid/failed/refunded/canceled), raw_update JSONB, created_at, paid_at NULL, refunded_at NULL.
    - subscriptions (§13.14): user_id FK users.id, product_id FK products.id, telegram_payment_charge_id TEXT, status enum(active/canceled/expired/payment_failed), started_at, current_period_start, current_period_end, canceled_at NULL, created_at/updated_at.
    - app_events (§13.15): user_id UUID NULL (indexed), event_name TEXT, event_properties JSONB, created_at.
    - generation_logs (§13.16): reading_id FK readings.id (indexed), prompt_template_version TEXT, model_name TEXT, input_tokens/output_tokens INT NULL, latency_ms INT, status TEXT, error TEXT NULL, created_at.
  </behavior>
  <action>
    Create one model file per aggregate group (topic.py, user.py, deck.py [decks+deck_cards+deck_spread_compatibility], card.py, spread.py [spread_types+spread_positions], reading.py [readings+reading_cards], prompt.py, billing.py [user_limits+products+payments+subscriptions], analytics.py [app_events+generation_logs]). Use `Mapped[...]` + `mapped_column`, `UUID(as_uuid=True)` PKs, `JSONB`, `ARRAY(String)` for TEXT[], and native `Enum(..., name=...)` from `app/models/enums.py` for the fixed sets (reading status, orientation, payment status, subscription status, product_type, prompt_templates type, cards arcana_type/suit, decks access_type). Add UNIQUE on every slug column + users.telegram_id + payments.payload; add indexes on payments.telegram_payment_charge_id, reading_cards.reading_id, readings.user_id, app_events.user_id, generation_logs.reading_id (RESEARCH Open Question #3). `app/models/__init__.py` MUST import every model class so `Base.metadata` is complete for Alembic autogenerate. Keep `cards` free of imagery and `deck_cards` free of base meaning (ARCHITECTURE boundary rule).
  </action>
  <verify>
    <automated>cd backend && ruff check app/models && python -c "from app.models import *; from app.models.base import Base; print(sorted(Base.metadata.tables.keys())); assert len(Base.metadata.tables)==17, len(Base.metadata.tables)"</automated>
  </verify>
  <acceptance_criteria>
    - `Base.metadata.tables` contains exactly 17 tables including `topics`.
    - `app/models/__init__.py` imports all model classes (autogenerate completeness).
    - Native ENUMs exist for reading.status, reading_cards.orientation, payments.status, subscriptions.status, products.product_type, prompt_templates.type, cards.arcana_type, cards.suit.
    - cards model has no image/back_image columns; deck_cards has no meaning_upright/meaning_reversed columns.
    - `ruff check app/models` exits 0.
  </acceptance_criteria>
  <done>All 17 typed models load; metadata has 17 tables; enums + uniques + FK indexes declared; cards/deck_cards boundary respected.</done>
</task>

<task type="auto">
  <name>Task 2: Initial Alembic migration (0001) + migration test asserting 17 tables and key uniques</name>
  <files>backend/alembic/versions/0001_initial_schema.py, backend/tests/integration/test_migration.py</files>
  <read_first>
    - .planning/phases/01-foundation-telegram-auth/01-RESEARCH.md (Pattern 1 async env.py; Open Question #3 required uniques/indexes)
    - .planning/phases/01-foundation-telegram-auth/01-VALIDATION.md (test_full_schema_applies: assert 16 tables + topics + key uniques via information_schema; downgrade base clean)
    - backend/alembic/env.py (Plan 01 async scaffold — target_metadata = Base.metadata)
  </read_first>
  <action>
    Generate the initial migration by running Alembic autogenerate against the test DB (`alembic revision --autogenerate -m "initial schema"` with revision id `0001_initial_schema`), then HAND-AUDIT the output: confirm all 17 `op.create_table` calls, every native ENUM is created, every UNIQUE/index from Task 1 is present, FK order is correct (topics/users/decks/cards/spread_types first; deck_cards, spread_positions, deck_spread_compatibility, readings, reading_cards, user_limits, products, payments, subscriptions, generation_logs after their parents). Ensure `downgrade()` drops tables in reverse dependency order and drops the created ENUM types. Do NOT hardcode the DB URL (env.py injects it).
    Replace the skipped stub `tests/integration/test_migration.py::test_full_schema_applies` (from Plan 01 Wave 0): run `alembic upgrade head` against the test DB, query `information_schema.tables` to assert all 17 expected table names are present, assert UNIQUE constraints on users.telegram_id, decks.slug, cards.slug, spread_types.slug, prompt_templates.slug, products.slug, topics.slug, payments.payload via `information_schema.table_constraints`/`pg_indexes`, then `alembic downgrade base` and assert the tables are gone. Remove the `@pytest.mark.skip`.
  </action>
  <verify>
    <automated>cd backend && alembic upgrade head && alembic downgrade base && alembic upgrade head && pytest -q tests/integration/test_migration.py</automated>
  </verify>
  <acceptance_criteria>
    - `alembic upgrade head` applies cleanly; `alembic downgrade base` reverses cleanly; re-`upgrade head` succeeds (idempotent round-trip).
    - `pytest -q tests/integration/test_migration.py` exits 0 and asserts 17 tables + the seven listed slug/telegram_id/payload uniques via `information_schema`.
    - The migration file is a single revision `0001_initial_schema` (no second migration).
    - `@pytest.mark.skip` removed from `test_full_schema_applies`.
  </acceptance_criteria>
  <done>One initial migration creates and reverses all 17 tables; migration integration test passes against the real test DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → PostgreSQL | DDL is applied to the database; the migration is the source of truth for schema integrity |
| (future) services → tables | UNIQUE/FK constraints declared here are the durable integrity guarantees later phases (auth upsert, payment idempotency) rely on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Duplicate `telegram_id` → identity confusion / account collision (auth upsert in Plan 04) | mitigate | `users.telegram_id BIGINT UNIQUE` enforced at the DB; upsert in Plan 04 relies on it (`on_conflict`). |
| T-02-02 | Tampering | Double-grant on payments (Phase 7) if `payload` not unique | mitigate | `payments.payload UNIQUE` + index `telegram_payment_charge_id` declared now so Phase 7 idempotency is DB-guaranteed. |
| T-02-03 | Repudiation | No audit trail for generations/payments | mitigate | `generation_logs` + `app_events` + `payments.raw_update JSONB` tables present from the initial schema. |
| T-02-04 | Information Disclosure | Schema drift forcing later ad-hoc DDL on production | accept | Single initial migration covers all 17 tables; later phases add data/columns via new migrations only when genuinely needed. |
</threat_model>

<verification>
- `cd backend && alembic upgrade head && alembic downgrade base && alembic upgrade head` → clean.
- `cd backend && pytest -q tests/integration/test_migration.py` → green.
- `python -c "from app.models import *; from app.models.base import Base; assert len(Base.metadata.tables)==17"` → ok.
- `ruff check backend/app/models` → 0.
</verification>

<success_criteria>
- INFRA-02: Alembic migrates the full schema — all 16 TZ §13 tables + the topics lookup table (17 total) — with UUID PKs, JSONB/TEXT[]/ENUM columns, FKs, and the required UNIQUE constraints, reversibly.
- Slug keys and `telegram_id`/`payload` uniqueness are locked for every downstream phase and the admin panel.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-telegram-auth/01-02-SUMMARY.md` when done.
</output>
