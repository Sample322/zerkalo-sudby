---
phase: 01-foundation-telegram-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - .gitignore
  - backend/Dockerfile
  - backend/pyproject.toml
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/script.py.mako
  - backend/app/__init__.py
  - backend/app/main.py
  - backend/app/core/config.py
  - backend/app/core/db.py
  - backend/app/core/redis.py
  - backend/app/core/logging.py
  - backend/app/models/base.py
  - backend/app/api/__init__.py
  - backend/app/api/deps.py
  - backend/app/api/health.py
  - backend/tests/conftest.py
  - backend/tests/unit/test_config.py
  - backend/tests/integration/test_health.py
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/lib/api.ts
autonomous: false
requirements: [INFRA-01, INFRA-04]
user_setup:
  - service: telegram
    why: "Bot token is a required startup secret (initData HMAC key in Plan 04). The skeleton boots fail-fast if BOT_TOKEN is absent."
    env_vars:
      - name: BOT_TOKEN
        source: "Telegram @BotFather -> /newbot or existing bot -> API token"
      - name: ANTHROPIC_API_KEY
        source: "console.anthropic.com -> API keys (required at startup per INFRA-04; unused until Phase 4)"
    dashboard_config: []

must_haves:
  truths:
    - "Running `docker compose up` brings up postgres:16 + redis:7 + the FastAPI backend together"
    - "GET /healthz returns 200 with {db:ok, redis:ok} when both dependencies are reachable, 503 otherwise"
    - "The backend process refuses to start (ValidationError at import) when any required secret (BOT_TOKEN, DATABASE_URL, REDIS_URL, JWT_SECRET, ANTHROPIC_API_KEY) is missing"
    - "The Vite frontend dev shell boots and renders the live result of calling the backend /healthz endpoint"
    - "pytest runs from backend/ with asyncio_mode=auto and the conftest fixtures import cleanly"
  artifacts:
    - path: "docker-compose.yml"
      provides: "postgres:16 + redis:7 + backend services with healthchecks and depends_on ordering"
      contains: "postgres:16"
    - path: "backend/app/core/config.py"
      provides: "pydantic-settings Settings with fail-fast required secrets + ADMIN_TELEGRAM_IDS NoDecode parser"
      contains: "NoDecode"
    - path: "backend/app/api/health.py"
      provides: "GET /healthz probing DB SELECT 1 + Redis PING"
      contains: "/healthz"
    - path: "backend/app/models/base.py"
      provides: "SQLAlchemy 2 DeclarativeBase + UUID/timestamp mixins (metadata target for Alembic in Plan 02)"
      contains: "DeclarativeBase"
    - path: "backend/tests/conftest.py"
      provides: "async app client (ASGITransport), DB session fixture, make_init_data helper signature"
      contains: "make_init_data"
    - path: "frontend/src/App.tsx"
      provides: "skeleton shell that calls backend health endpoint"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/api/health.py"
      via: "include_router"
      pattern: "include_router"
    - from: "frontend/src/App.tsx"
      to: "/healthz"
      via: "fetch in lib/api.ts"
      pattern: "healthz"
    - from: "backend/alembic/env.py"
      to: "backend/app/models/base.py"
      via: "target_metadata = Base.metadata"
      pattern: "Base.metadata"
---

<objective>
Stand up the Walking Skeleton: the thinnest end-to-end stack that proves the platform works. `docker compose up` brings up PostgreSQL 16 + Redis 7 + a FastAPI backend that fails fast on any missing secret and answers `GET /healthz` with a real DB+Redis probe; a Vite/React frontend shell boots and renders the live health result. This plan also lays the Wave-0 test infrastructure (pytest + async client + the `make_init_data` helper) that every later plan's verifications depend on, and the SQLAlchemy `Base` + async Alembic scaffold that Plan 02 migrates against.

Purpose: Establish the deployable substrate and the security/config seams (fail-fast secrets, `ADMIN_TELEGRAM_IDS` parser, async DB/Redis lifespan) before any user data exists. Satisfies INFRA-01 and INFRA-04.
Output: Running compose stack, healthy `/healthz`, booting frontend shell, green `tests/unit/test_config.py` + `tests/integration/test_health.py`, and the `Base` metadata + Alembic async `env.py` that Plan 02 consumes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-telegram-auth/01-RESEARCH.md
@.planning/phases/01-foundation-telegram-auth/01-VALIDATION.md
@.planning/phases/01-foundation-telegram-auth/SKELETON.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package Legitimacy verification (gates the first install)</name>
  <read_first>
    - .planning/phases/01-foundation-telegram-auth/01-RESEARCH.md (## Package Legitimacy Audit, ## Standard Stack — every package is tagged [ASSUMED] because slopcheck was unavailable)
  </read_first>
  <what-built>
    Nothing yet — this checkpoint MUST run before any `pip`/`npm` install in Task 2/4/5. The research tagged every dependency `[ASSUMED]` (slopcheck unavailable this session). Per the package-legitimacy gate, a human verifies each package exists on its registry with the expected source repo before installation.
  </what-built>
  <how-to-verify>
    Confirm each package below resolves to its known source repo on the registry (open the URL, check the GitHub link + recent release date):
    Backend (PyPI — pypi.org/project/NAME): fastapi, uvicorn, sqlalchemy, asyncpg, alembic, pydantic, pydantic-settings, pyjwt, redis, pytest, pytest-asyncio, httpx, ruff.
    Frontend (npm — npmjs.com/package/NAME): react, react-dom, vite, @tanstack/react-query, zustand, motion (NOT framer-motion), tailwindcss, @tailwindcss/vite.
    Pins to enforce (drift traps): redis `>=5.2,<6` (NOT 8.x), vite `7.3.*` (NOT 8.x), asyncpg `0.31.*`, alembic `1.18.*`, pydantic-settings `2.14.*`, pyjwt `2.13.*`. Confirm `motion` is npm-only (do NOT `pip install motion`).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with installs, or list any package that looks wrong/squatted.</resume-signal>
  <acceptance_criteria>
    - Human has confirmed all backend + frontend package names against their registries and approved.
    - No package flagged as suspicious/squatted is carried into Task 2/4/5.
  </acceptance_criteria>
  <done>Human approval recorded; pinned versions confirmed (redis &lt;6, vite 7.x).</done>
</task>

<task type="auto">
  <name>Task 2: Backend scaffold — config fail-fast, async DB/Redis, /healthz, compose, Dockerfile, Base + Alembic async env</name>
  <files>backend/pyproject.toml, backend/Dockerfile, backend/alembic.ini, backend/alembic/env.py, backend/alembic/script.py.mako, backend/app/__init__.py, backend/app/main.py, backend/app/core/config.py, backend/app/core/db.py, backend/app/core/redis.py, backend/app/core/logging.py, backend/app/models/base.py, backend/app/api/__init__.py, backend/app/api/health.py, docker-compose.yml, .env.example, .gitignore</files>
  <read_first>
    - .planning/phases/01-foundation-telegram-auth/01-RESEARCH.md (Pattern 1 async Alembic env.py; Pattern 2 pydantic-settings fail-fast + ADMIN_TELEGRAM_IDS NoDecode footgun; Code Examples: GET /healthz, async engine/session lifespan, typed model base; Pitfall 3 healthcheck-that-lies; Pitfall 4 version drift; Pitfall 6 host 3.14 vs container 3.12; Recommended Project Structure)
    - .planning/research/STACK.md (## Deploy: timeweb.cloud docker-compose shape; version compatibility table)
    - .planning/REFERENCE-TZ.md §29.2 (backend rules: user sees soft error not stacktrace, admin endpoints protected)
  </read_first>
  <action>
    Create `backend/pyproject.toml` with `requires-python = ">=3.12,<3.13"`, dependencies pinned per RESEARCH ## Standard Stack (fastapi 0.136.*, uvicorn[standard]>=0.49, sqlalchemy[asyncio]==2.0.*, asyncpg==0.31.*, alembic==1.18.*, pydantic==2.13.*, pydantic-settings==2.14.*, redis[hiredis]>=5.2,<6, pyjwt==2.13.*), dev deps (pytest, pytest-asyncio, httpx, ruff>=0.9), and `[tool.pytest.ini_options]` with `asyncio_mode = "auto"` and `testpaths = ["tests"]`, plus a `[tool.ruff]` section.
    Create `backend/app/core/config.py` exactly per RESEARCH Pattern 2: a `Settings(BaseSettings)` with required (no-default) secrets `BOT_TOKEN`, `DATABASE_URL`, `REDIS_URL`, `JWT_SECRET`, `ANTHROPIC_API_KEY`; tunables `JWT_EXPIRE_SECONDS` (default 604800), `INITDATA_MAX_AGE_SECONDS` (default 86400), `WEBHOOK_SECRET: str | None = None`; and `ADMIN_TELEGRAM_IDS: Annotated[list[int], NoDecode] = []` with a `field_validator(mode="before")` `_parse_ids` that splits a comma string into ints. Instantiate `settings = Settings()` at module import so a missing secret raises `ValidationError` at process start (fail-fast). Add `model_config = SettingsConfigDict(env_file=".env", extra="ignore")`.
    Create `backend/app/core/db.py`: `create_async_engine(settings.DATABASE_URL, pool_pre_ping=True)` + `async_sessionmaker(expire_on_commit=False, class_=AsyncSession)` + a `get_session` dependency yielding a session. Create `backend/app/core/redis.py`: a `redis.asyncio` client from `settings.REDIS_URL` + a `get_redis` dependency. Create `backend/app/core/logging.py`: a `configure_logging()` that sets up JSON-ish structured stdlib logging (level from env, default INFO) — called once on startup.
    Create `backend/app/models/base.py`: `class Base(DeclarativeBase)` plus reusable typed mixins/columns the Plan 02 models will use — a UUID primary-key default (`uuid.uuid4`), and `created_at`/`updated_at` timestamp helpers (`server_default=func.now()`, `onupdate`). This `Base.metadata` is the Alembic target.
    Create `backend/app/api/health.py` exactly per RESEARCH Code Examples: `GET /healthz` that runs `await session.execute(text("SELECT 1"))` and `await redis.ping()`, returns `JSONResponse(checks, status_code=200 if all ok else 503)` with per-dependency `{db, redis}` status. Do NOT return 200 from a bare handler (Pitfall 3).
    Create `backend/app/main.py`: FastAPI app with a lifespan that calls `configure_logging()`, disposes engine + closes redis on shutdown; mount the health router via `include_router`. Leave a comment seam where Plan 04 will register the auth/admin routers and the global exception handler.
    Create `backend/app/api/deps.py` with `get_session` and `get_redis` re-exported (Plan 04 adds `get_current_user`/`require_admin` here). Create `backend/app/api/__init__.py` and `backend/app/__init__.py`.
    Scaffold async Alembic: `alembic.ini` (no hardcoded URL), `alembic/env.py` exactly per RESEARCH Pattern 1 (`async_engine_from_config` + `connection.run_sync(do_run_migrations)` + `asyncio.run`, `poolclass=pool.NullPool`, `config.set_main_option("sqlalchemy.url", settings.DATABASE_URL)`, `target_metadata = Base.metadata`), and the standard `script.py.mako`. No version files yet (Plan 02 creates 0001).
    Create `backend/Dockerfile` (`python:3.12-slim` -> install deps -> `CMD uvicorn app.main:app --host 0.0.0.0 --port 8000`). Create `docker-compose.yml` with services `postgres` (`postgres:16`, healthcheck `pg_isready`, named volume), `redis` (`redis:7`, healthcheck `redis-cli ping`), `backend` (build ./backend, `depends_on` postgres+redis with `condition: service_healthy`, env from `.env`, port 8000). DSN form `postgresql+asyncpg://...@postgres:5432/...`; `REDIS_URL=redis://redis:6379/0`.
    Create `.env.example` listing every secret NAME with no values: `BOT_TOKEN=`, `DATABASE_URL=postgresql+asyncpg://zerkalo:zerkalo@postgres:5432/zerkalo`, `REDIS_URL=redis://redis:6379/0`, `JWT_SECRET=`, `ANTHROPIC_API_KEY=`, `ADMIN_TELEGRAM_IDS=` (document format `111,222` — comma, no brackets), `WEBHOOK_SECRET=`. Create `.gitignore` ignoring `.env`, `__pycache__`, `node_modules`, `dist`, `*.pyc`, `.venv`.
  </action>
  <verify>
    <automated>cd backend && ruff check app && python -c "import ast,sys; [ast.parse(open(f).read()) for f in ['app/core/config.py','app/core/db.py','app/api/health.py','app/main.py','app/models/base.py','alembic/env.py']]; print('parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/core/config.py` contains `NoDecode`, an `ADMIN_TELEGRAM_IDS` field, and `settings = Settings()` at module scope.
    - `backend/app/api/health.py` issues both `SELECT 1` and `redis ... ping` and can return status 503.
    - `docker-compose.yml` declares `postgres:16` and `redis:7` services with healthchecks and `depends_on: condition: service_healthy` on backend.
    - `backend/alembic/env.py` sets `target_metadata = Base.metadata` and injects the URL from settings (no URL in `alembic.ini`).
    - `.env.example` lists all 7 env names with no secret values; `.env` is gitignored.
    - `ruff check app` exits 0.
  </acceptance_criteria>
  <done>Backend scaffold parses, lints clean, compose + Dockerfile + Alembic async scaffold + Base metadata + .env.example exist; config fails fast on missing secrets by construction.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 test infrastructure — conftest fixtures, make_init_data helper, config + health tests</name>
  <files>backend/tests/__init__.py, backend/tests/conftest.py, backend/tests/unit/__init__.py, backend/tests/unit/test_config.py, backend/tests/integration/__init__.py, backend/tests/integration/test_health.py, backend/tests/unit/test_initdata.py, backend/tests/integration/test_auth_flow.py, backend/tests/integration/test_me.py, backend/tests/integration/test_admin_guard.py, backend/tests/integration/test_migration.py, backend/tests/integration/test_seed.py, backend/tests/integration/test_error_shape.py</files>
  <read_first>
    - .planning/phases/01-foundation-telegram-auth/01-VALIDATION.md (Per-Task Verification Map; Wave 0 Requirements — conftest must provide async DB session, app client via ASGITransport, Redis, and make_init_data(bot_token, user, auth_date))
    - .planning/phases/01-foundation-telegram-auth/01-RESEARCH.md (Validation Architecture: capturing a real initData fixture by constructing a valid one from a fake BOT_TOKEN with the two-stage HMAC; Wave 0 Gaps list; Pattern 3 initData algorithm so the helper signs identically)
  </read_first>
  <action>
    Create `backend/tests/conftest.py` providing pytest-asyncio fixtures: (1) an async app `client` using httpx `ASGITransport` against `app.main.app` (no live server); (2) a `db_session` fixture that connects to the test DATABASE_URL, runs `alembic upgrade head` once per session (or `Base.metadata.create_all` as a fallback gated to test DB), and yields a transaction-rolled-back `AsyncSession` per test; (3) a `redis_client` fixture; (4) a deterministic `make_init_data(bot_token: str, user: dict, auth_date: int | None = None) -> str` helper that builds a Telegram-style query string and computes the correct `hash` via the SAME two-stage HMAC as RESEARCH Pattern 3 (`secret = HMAC_SHA256(b"WebAppData", bot_token)`, then HMAC over the sorted `k=v\n` data_check_string excluding `hash`) so unit tests can produce valid + tampered + stale variants. Provide a `TEST_BOT_TOKEN` constant and a sample `user` dict.
    Create `backend/tests/unit/test_config.py` with `test_missing_secret_fails_fast` (instantiating Settings with a required secret absent raises `ValidationError`) and `test_admin_ids_csv_parsed` (`ADMIN_TELEGRAM_IDS="111,222"` -> `[111, 222]`, not a JSON crash). These must PASS now (config exists).
    Create `backend/tests/integration/test_health.py::test_healthz_ok` asserting `GET /healthz` returns 200 with `db == "ok"` and `redis == "ok"` when the test DB+Redis are up. This must PASS now.
    Create RED placeholder stubs (skeleton test files with the exact node IDs from VALIDATION.md, marked `pytest.mark.skip(reason="Wave 0 stub — implemented in Plan 02/03/04")` so the suite is green now but the node IDs exist): `tests/unit/test_initdata.py` (test_forged_hash_rejected, test_tampered_field_rejected, test_stale_auth_date_rejected, test_missing_hash_rejected); `tests/integration/test_auth_flow.py` (test_valid_initdata_issues_jwt_and_upserts, test_repeat_auth_updates_last_seen); `tests/integration/test_me.py` (bearer accept/reject); `tests/integration/test_admin_guard.py` (test_non_admin_403, test_admin_200); `tests/integration/test_migration.py` (test_full_schema_applies); `tests/integration/test_seed.py` (test_seed_counts, test_seed_idempotent); `tests/integration/test_error_shape.py` (test_no_stacktrace_leak). Each stub references the requirement ID in a docstring. Later plans REMOVE the skip and implement the body.
    Document in each stub that removing `@pytest.mark.skip` and implementing is the consuming plan's job (so no plan ships a self-passing skipped test as "done").
  </action>
  <verify>
    <automated>cd backend && pytest -q tests/unit/test_config.py tests/integration/test_health.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q tests/unit/test_config.py tests/integration/test_health.py` exits 0 (these two files PASS, requiring a reachable test DB+Redis for health).
    - `conftest.py` defines `make_init_data` whose computed hash matches RESEARCH Pattern 3 (verified indirectly when Plan 04 unit tests pass against the real validator).
    - `pytest --collect-only -q` lists every node ID from VALIDATION.md (stubs present), with the not-yet-implemented ones skipped.
    - No stub asserts real behavior while skipped (skip reason names the owning plan).
  </acceptance_criteria>
  <done>conftest + make_init_data exist; config and health tests pass; all VALIDATION.md node IDs are collectable as stubs for later plans to implement.</done>
</task>

<task type="auto">
  <name>Task 4: Frontend Vite/React shell that calls the backend health endpoint</name>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/tsconfig.json, frontend/tsconfig.node.json, frontend/index.html, frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/lib/api.ts, frontend/.env.example, frontend/src/vite-env.d.ts</files>
  <read_first>
    - .planning/phases/01-foundation-telegram-auth/01-RESEARCH.md (Core — Frontend stack pins: React 19.2.*, Vite 7.3.* NOT 8, Tailwind 4.3 + @tailwindcss/vite, motion NOT framer-motion; Open Question #5 WebView CSP connect-src must allow the API origin; Frontend walking-skeleton example)
    - .planning/research/STACK.md (frontend stack rationale; What NOT to Use — framer-motion package name)
    - .planning/PROJECT.md (Brand voice: no "AI / нейросеть / модель / сгенерировано ИИ" in any UI string)
  </read_first>
  <action>
    Scaffold a Vite 7 React-TS app under `frontend/` (use `npm`, pnpm is not installed). `package.json` pins react@19.2.*, react-dom@19.2.*, vite@7.3.* (NOT 8), @tailwindcss/vite@4.3.*, tailwindcss@4.3.*; include @tanstack/react-query@5 and zustand@5 and motion as deps now (used in Plan 05 / later phases) so the lockfile is established once. Scripts: `dev`, `build`, `preview`.
    `vite.config.ts`: React plugin + `@tailwindcss/vite` plugin; read the backend base URL from `import.meta.env.VITE_API_BASE` (default `http://localhost:8000`). Create `frontend/.env.example` with `VITE_API_BASE=http://localhost:8000` and a comment that for real Telegram testing this becomes the HTTPS tunnel origin (cloudflared/ngrok) and must be allowed by the WebView CSP `connect-src` (RESEARCH Open Question #5).
    `frontend/src/lib/api.ts`: an `API_BASE` const from `import.meta.env.VITE_API_BASE` and `getHealth()` that `fetch`es `${API_BASE}/healthz` and returns the JSON.
    `frontend/src/App.tsx`: on mount, call `getHealth()` and render the live status (e.g. "Хранилище: ok · Кэш: ok" while loading shows a neutral ritual-styled placeholder). NO occurrence of "AI", "нейросеть", "модель", or "сгенерировано ИИ" anywhere. `main.tsx` mounts `<App/>`; `index.html` has the root div and viewport meta.
    Keep styling minimal (one Tailwind import) — full per-deck theming is Phase 2. This task only proves the FE→BE round-trip.
  </action>
  <verify>
    <automated>cd frontend && npm install --no-audit --no-fund && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` produces `frontend/dist` with no type errors.
    - `frontend/package.json` pins `vite` to a `7.3.*`/`^7` range (NOT 8) and lists `motion` (NOT `framer-motion`).
    - `frontend/src/lib/api.ts` fetches `${API_BASE}/healthz`; `App.tsx` renders its result.
    - No banned brand-voice string ("AI"/"нейросеть"/"модель"/"сгенерировано ИИ") appears in `frontend/src`.
    - `VITE_API_BASE` is read from env (not hardcoded) and documented in `frontend/.env.example`.
  </acceptance_criteria>
  <done>Frontend shell builds and renders the live `/healthz` result via an env-configured API base, with no AI-branding strings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (Telegram WebView) → FastAPI | Untrusted HTTP enters at `/healthz`; no auth yet, but the origin/CSP seam is established here |
| docker host env → backend process | Secrets cross via env vars; fail-fast validation guards a misconfigured boot |
| compose network → postgres/redis | Backend reaches data stores by service name; healthchecks gate readiness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Secrets in repo / `.env` committed | mitigate | `.env` gitignored; `.env.example` carries NAMES only, never values (RESEARCH V8). |
| T-01-02 | Denial of Service | `/healthz` lies → unhealthy deps look healthy, first real request 500s | mitigate | `/healthz` performs real `SELECT 1` + Redis `PING`, returns 503 on failure (Pitfall 3). |
| T-01-03 | Tampering | Supply-chain / slopsquat on first install (all packages `[ASSUMED]`) | mitigate | Task 1 blocking-human Package Legitimacy checkpoint; pinned versions (redis &lt;6, vite 7.x); `motion` is npm-only. |
| T-01-04 | Configuration | App boots with a missing/empty critical secret | mitigate | pydantic-settings required fields → `ValidationError` at import (fail-fast); unit-tested. |
| T-01-SC | Tampering | npm/pip installs in Tasks 2/4 | mitigate | Gated by T-01-01 checkpoint; `[ASSUMED]` packages verified on PyPI/npm before install. |
</threat_model>

<verification>
- `cd backend && pytest -q tests/unit/test_config.py tests/integration/test_health.py` → green (needs test DB+Redis up).
- `docker compose up -d` then `curl -f localhost:8000/healthz` → 200 `{db:ok,redis:ok}` (manual/CI smoke; see Manual-Only Verifications in 01-VALIDATION.md).
- `cd frontend && npm run build` → succeeds.
- `ruff check backend/app` → 0.
</verification>

<success_criteria>
- INFRA-01: `docker compose up` brings up postgres:16 + redis:7 + backend together with healthcheck-gated ordering; monorepo layout (`/backend`, `/frontend`, `/docs` reserved) established.
- INFRA-04: `GET /healthz` returns healthy only when DB+Redis are reachable; the backend fails fast (ValidationError) when BOT_TOKEN / DATABASE_URL / REDIS_URL / JWT_SECRET / ANTHROPIC_API_KEY is missing.
- Wave-0 test harness (conftest + `make_init_data` + collectable node IDs) is in place for Plans 02–04.
- Frontend shell renders the live backend health result with no AI-branding copy.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-telegram-auth/01-01-SUMMARY.md` when done.
</output>
