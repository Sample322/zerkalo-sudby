---
phase: 08-admin-analytics-polish-deploy
plan: 04
type: execute
wave: 2
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - frontend/src/reading/copy.ts
  - frontend/src/components/CatalogScreen.tsx
  - frontend/src/components/ritual/RitualScreen.tsx
  - frontend/src/components/result/ResultScreen.tsx
  - frontend/src/components/history/HistoryScreen.tsx
  - frontend/src/components/admin/AdminScreen.tsx
  - frontend/src/components/admin/PromptVersions.tsx
  - frontend/src/components/result/ShareCardButton.tsx
  - frontend/src/reading/copy.test.ts
autonomous: true
requirements: [UI-05]
must_haves:
  truths:
    - "Every screen's empty / error / loading branch renders a product-voice copy.ts constant — never a stack trace, a raw slug, an HTTP status, or a bare spinner (ТЗ §9.8)"
    - "The gap-audit covers the NEW Phase-8 surfaces too — the admin prompt versions section and the share-card failure/cancel path"
    - "No banned brand tokens (AI/нейросеть/модель) appear in any state copy — copy.test.ts scan stays green"
    - "Fixes are additive polish only — no core logic/behaviour changes"
  artifacts:
    - path: "frontend/src/reading/copy.ts"
      provides: "the in-voice constants filling every audited empty/error/loading gap (existing + new Phase-8 surfaces)"
      contains: "EMPTY"
  key_links:
    - from: "frontend screens (Catalog/Ritual/Result/History/Admin)"
      to: "frontend/src/reading/copy.ts"
      via: "each empty/error/loading branch renders a copy.ts constant, not a raw error"
      pattern: "copy"
---

<objective>
Ship the app polished (UI-05 / ТЗ §9.8): a gap-audit, not a rewrite. Most states already use
product-voice copy (HISTORY_EMPTY/ERROR/LOADING, READING_ERROR, THROTTLE_MESSAGE, the §9.8 soft-body
band, the global soft-500 handler `core/errors.py`). This plan sweeps every screen + every query
error/empty/loading branch — INCLUDING the new Phase-8 surfaces (admin prompt versions from 08-01,
the share-card fallback from 08-03) — and ensures each renders an in-voice copy.ts constant with no
stack trace, raw slug, HTTP status, or bare spinner. Runs LAST (wave 2) so the new surfaces exist to
polish (08-RESEARCH.md §4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-admin-analytics-polish-deploy/08-CONTEXT.md
@.planning/phases/08-admin-analytics-polish-deploy/08-RESEARCH.md

# The existing polish substrate to extend + the banned-token scan to keep green
@frontend/src/reading/copy.ts
@frontend/src/reading/copy.test.ts
@frontend/src/components/history/HistoryScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit — enumerate every empty/error/loading branch and its current copy</name>
  <read_first>
    - All flow screens: AuthGate, CatalogScreen, RitualScreen, reveal/*, ResultScreen,
      history/HistoryScreen, admin/AdminScreen + admin/PromptVersions (new), result/ShareCardButton
      (new).
    - frontend/src/reading/copy.ts — the existing constants + naming convention.
    - The TanStack Query usages (isLoading/isError/empty) across those screens.
  </read_first>
  <action>
    Produce a short in-PR audit table (in the SUMMARY) of each screen × {empty, error, loading}
    branch → current rendering → gap? Flag every branch that shows a raw error (`err.message`, an
    HTTP status, a slug), a bare spinner with no copy, or a missing empty state. Include the new
    08-01 PromptVersions states (empty list, publish error, activate error) and the 08-03 share
    fallback/cancel/failed states. No code change in this task — just the enumerated gap list driving
    Task 2.
  </action>
  <verify>Audit table present in the plan SUMMARY; every screen accounted for.</verify>
</task>

<task type="auto">
  <name>Task 2: Fill the gaps with in-voice copy.ts constants</name>
  <read_first>
    - frontend/src/reading/copy.ts — add constants following the existing naming (e.g.
      CATALOG_ERROR, RITUAL_ERROR, ADMIN_PROMPTS_EMPTY/ERROR, SHARE_FAILED already from 08-03).
    - ТЗ §9.8 voice (soft, mystical, never technical).
  </read_first>
  <action>
    For each gap from Task 1, replace the raw/absent state with an in-voice copy.ts constant. Loading
    → a calm in-voice line or the existing soft band (no bare spinner-only where a message belongs);
    error → a soft "колода/оракул" phrasing with a retry affordance where one exists; empty → an
    inviting in-voice line. Keep changes additive + presentational — do NOT alter data flow, query
    logic, or behaviour. Reuse existing constants where they already fit; add new ones only for real
    gaps. NEVER surface `err.message`, HTTP codes, or slugs. No banned brand tokens.
  </action>
  <verify>Each previously-flagged branch now renders a copy.ts constant; grep shows no `err.message`/status rendered in the audited screens.</verify>
</task>

<task type="auto">
  <name>Task 3: Guard — banned-token scan + a state-copy regression test</name>
  <read_first>
    - frontend/src/reading/copy.test.ts — the SAFE-06 banned-token scan; extend its constant coverage
      to the new copy.
  </read_first>
  <action>
    Ensure copy.test.ts scans the newly added constants for banned tokens (AI/нейросеть/модель/
    сгенерировано). Add a light regression test that a representative error branch (e.g. Catalog
    load error) renders the in-voice constant, not a raw error, when its query is forced to error
    (mock). Keep it deterministic (no timeout-based assertions).
  </action>
  <verify>`cd frontend && pnpm exec vitest run` green; `pnpm exec tsc --noEmit` clean; copy.test.ts banned-token scan green over all constants.</verify>
</task>

</tasks>

<verification>
- Every empty/error/loading branch (incl. new 08-01 admin + 08-03 share surfaces) renders an in-voice copy.ts constant.
- No stack traces, HTTP statuses, raw slugs, or bare spinners in audited screens.
- copy.test.ts banned-token scan green; tsc + vitest green.
- Changes are presentational-only — no behaviour change.
</verification>
