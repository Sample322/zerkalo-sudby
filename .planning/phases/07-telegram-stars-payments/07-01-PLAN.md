---
phase: 07-telegram-stars-payments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/conftest.py
  - backend/tests/fakes_payments.py
  - backend/tests/test_payments_service.py
  - backend/tests/test_payments_api.py
  - backend/tests/test_reading_buckets.py
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, PAY-05, PAY-06, PAY-07]
must_haves:
  truths:
    - "Running uv run pytest collects every new payment test file without import errors"
    - "Each PAY-0x behavior has a named test that xfails today and is the green target for its implementation plan"
    - "No test ever calls api.yookassa.ru — FakeYooKassa is the only ЮKassa surface in the suite"
  artifacts:
    - path: "backend/tests/fakes_payments.py"
      provides: "FakeYooKassa stand-in (create_payment / find_payment / create_refund / find_refund) returning canned objects"
      contains: "class FakeYooKassa"
    - path: "backend/tests/test_payments_service.py"
      provides: "Red service tests: grant idempotency, recurring key logic, refund reconciliation"
      contains: "def test_"
    - path: "backend/tests/test_payments_api.py"
      provides: "Red API tests: products/create/webhook/refund + IP gate"
      contains: "def test_"
    - path: "backend/tests/test_reading_buckets.py"
      provides: "Red gate tests: subscription/paid consume + correct-bucket refund"
      contains: "def test_"
  key_links:
    - from: "backend/tests/conftest.py"
      to: "tests/fakes_payments.py FakeYooKassa"
      via: "fixture exposing FakeYooKassa + dependency_overrides wiring"
      pattern: "FakeYooKassa"
---

<objective>
Lay the Wave-0 RED test substrate for the entire ЮKassa payments phase: a `FakeYooKassa`
client stand-in (mirroring the established `FakeLLM`/`FakeSafety` `dependency_overrides`
seam), a conftest fixture that wires it, and the red test files covering every PAY-0x
behavior. Each downstream implementation plan turns its own xfailing tests green.

This satisfies the Nyquist rule (every later task has an automated `<verify>` that exists
before the code) AND the no-real-charge mandate (07-VALIDATION.md): the fake is the ONLY
ЮKassa surface the suite ever touches — no test calls `api.yookassa.ru`.

Purpose: De-risk the highest-risk surface in the app by fixing the test contract first —
idempotency, grant-only-on-refetched-succeeded, server-authoritative price, recurring key
discipline, and refund reconciliation become asserted invariants, not afterthoughts.
Output: 4 test files + 1 fakes module + conftest wiring; full suite still green (new tests
xfail/skip cleanly without Postgres).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-telegram-stars-payments/07-CONTEXT.md
@.planning/phases/07-telegram-stars-payments/07-RESEARCH.md
@.planning/phases/07-telegram-stars-payments/07-VALIDATION.md

# The seam to mirror + the existing gate under test
@backend/tests/conftest.py
@backend/app/services/reading.py
@backend/app/api/readings.py
@backend/app/models/billing.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: FakeYooKassa client + conftest fixture (the no-real-charge seam)</name>
  <read_first>
    - backend/tests/conftest.py — the existing fixtures (`client`, `db_session`, `_TEST_ENV_DEFAULTS`, `make_init_data`), and the import-before-app env pattern; ADD the two ЮKassa secrets to `_TEST_ENV_DEFAULTS` so `config.Settings()` imports.
    - 07-RESEARCH.md §"Validation Architecture" → "No-real-charge mandate" and §"Code Examples" (Payment.create / find_one / Refund.create canned object shapes; webhook envelope `{type,event,object}`).
    - backend/app/services/reading.py — how `FakeLLM`/`FakeSafety` are injected via constructor + `app.dependency_overrides[get_reading_service]` (the seam to mirror for a `get_payment_service` override).
  </read_first>
  <action>
    Create `backend/tests/fakes_payments.py` exposing `class FakeYooKassa` with async-compatible
    methods the service seam (Plan 03) will call: `create_payment(*, value_rub, return_url,
    idempotence_key, metadata, save_payment_method=False)`, `find_payment(payment_id)`,
    `create_refund(*, payment_id, value_rub, idempotence_key)`, `find_refund(refund_id)`. Each
    returns a simple object (use a `SimpleNamespace` or a small frozen dataclass) with the ЮKassa
    fields the service reads: `id`, `status`, `amount` (`{"value","currency"}`), `confirmation`
    (`{"confirmation_url"}` for create), `payment_method` (`{"id","saved"}` when
    `save_payment_method`), `metadata`, and a `json()`/dict accessor for `raw_update`. Give
    FakeYooKassa controllable behavior: a constructor arg (e.g. `succeeded=True`, `next_status`,
    `recorded_calls` list) so a test can make `find_payment` return `pending` vs `succeeded`, and
    record every call's args (assert the deterministic recurring key + the server-recomputed amount).
    NEVER import the real `yookassa` package here.
    In `backend/tests/conftest.py`: add `YOOKASSA_SHOP_ID="test_shop"` and
    `YOOKASSA_SECRET_KEY="test_secret"` to `_TEST_ENV_DEFAULTS` (so the Plan-02 fail-fast config
    imports under test). Add a `fake_yookassa` pytest fixture returning a `FakeYooKassa()` and a
    helper note (docstring) describing how Plan 03/04 will inject it via the service constructor +
    `app.dependency_overrides`. Export `FakeYooKassa` from the conftest `__all__`.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/ -q --co 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/fakes_payments.py` defines `class FakeYooKassa` with `create_payment`,
      `find_payment`, `create_refund`, `find_refund`; no `import yookassa` anywhere in the file
      (`grep -rn "import yookassa" backend/tests` returns nothing).
    - `backend/tests/conftest.py` `_TEST_ENV_DEFAULTS` contains `YOOKASSA_SHOP_ID` and
      `YOOKASSA_SECRET_KEY`; a `fake_yookassa` fixture exists.
    - `uv run pytest tests/ --co` collects with no import/collection error (clean-skips without PG).
  </acceptance_criteria>
  <done>FakeYooKassa + conftest fixture exist; collection passes; no real ЮKassa import.</done>
</task>

<task type="auto">
  <name>Task 2: Red test files for PAY-01..07 + bucket consume (xfail targets)</name>
  <read_first>
    - 07-VALIDATION.md §"Phase Requirements → Test Map" and §"Wave 0 Gaps" — the exact test names + automated commands each implementation plan must make green.
    - 07-RESEARCH.md §"Common Pitfalls" 1/2/4/5 (grant-on-create, double-grant redelivery, amount tampering, recurring key) and §"Idempotency test (critical)".
    - backend/app/services/reading.py — `determine_access`, `Bucket`, `_consume_free_gate`, `_refund_free`, `_consume_free_atomic` (the SUBSCRIPTION/PAID seams the bucket tests target).
  </read_first>
  <action>
    Create three red test files. Use `pytest.mark.xfail(strict=False, reason="Plan NN")` on tests
    whose implementation is not yet present, and import the not-yet-existing symbols INSIDE the test
    body (so collection never errors), mirroring the Phase-6 Wave-0 stub pattern. DB-touching tests
    must use the existing `db_session`/`_db_ready` fixtures so they clean-skip without Postgres.

    `backend/tests/test_payments_service.py` (service-level, target Plan 03):
      - `test_grant_paid_balance_on_succeeded` — webhook grant path increments
        `paid_spreads_balance` by the product's `spreads_amount` only when the (faked) re-fetch
        returns `succeeded`.
      - `test_grant_idempotent_on_redelivery` — delivering the SAME `payment.succeeded` twice grants
        exactly once (balance unchanged on the 2nd; the conditional `UPDATE ... WHERE
        status=CREATED ... RETURNING` no-ops). THIS IS THE CRITICAL idempotency test.
      - `test_no_grant_on_unconfirmed_status` — if the re-fetch returns `pending`/`canceled`, no
        balance change.
      - `test_recurring_charge_uses_deterministic_key` — a renewal calls FakeYooKassa.create_payment
        with `payment_method_id` set, NO confirmation, and Idempotence-Key == `renew:<sub_id>:<period_index>`.
      - `test_refund_recon_flips_status_and_adjusts_access` — a faked `refund.succeeded` flips
        `Payment.status=refunded`, sets `refunded_at`, and decrements balance / ends the subscription.

    `backend/tests/test_payments_api.py` (route-level via the `client` fixture, target Plan 04):
      - `test_products_list` — `GET /api/products` returns active products only (PAY-01).
      - `test_create_payment_no_grant` — `POST /api/payments/create {product_slug}` returns a
        `confirmation_url`, writes a CREATED `payments` row, and does NOT change any balance (PAY-02).
      - `test_create_recomputes_price` — the amount sent to FakeYooKassa equals the `products` row
        price (a client-sent `price`/`amount` field is ignored/absent); inactive product → 4xx (PAY-03).
      - `test_webhook_grants_on_refetched_succeeded` — posting the webhook envelope (from a ЮKassa
        IP) triggers a re-fetch and grants (PAY-04).
      - `test_webhook_idempotent_and_ip_gated` — same event twice grants once; a non-ЮKassa source
        IP is rejected/ignored; the body status is never trusted (PAY-05).
      - `test_refund_requires_admin` — `POST /api/payments/{id}/refund` is 403 for a non-admin JWT,
        allowed for an admin (PAY-07 / T-07-IDOR).

    `backend/tests/test_reading_buckets.py` (gate, target Plan 05 — EXTEND, do not duplicate the
    Phase-6 free-quota tests; if the file does not exist, create it):
      - `test_subscription_bucket_consumed_when_active` — with an active subscription window
        (`subscription_spreads_limit>used`), `determine_access` returns `Bucket.SUBSCRIPTION` and a
        reading consumes from it (free → subscription → paid order, D-11).
      - `test_paid_bucket_consumed_when_no_free_no_sub` — with `paid_spreads_balance>0` and no
        free/sub, `Bucket.PAID` is chosen and consumed.
      - `test_honest_fail_refunds_correct_bucket` — an honest LLM fail after a SUBSCRIPTION/PAID
        consume refunds THAT bucket (not free).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_payments_service.py tests/test_payments_api.py tests/test_reading_buckets.py -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - The three files exist; `uv run pytest` collects them with zero import/collection errors.
    - Every test listed above is present by name (`grep -c "def test_"` ≥ the counts above per file).
    - Tests that need unimplemented symbols are `xfail(strict=False)` (xfail/xpass or clean-skip
      without PG); the pre-existing full suite (83 backend pass baseline) is not broken
      (`uv run pytest -q` exit code 0).
    - `grep -rn "api.yookassa.ru" backend/tests` returns nothing.
  </acceptance_criteria>
  <done>All PAY-0x + bucket behaviors have named red tests; suite green; no live ЮKassa calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → ЮKassa | MUST be severed: FakeYooKassa is the only ЮKassa surface; no test reaches the live API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-TEST-LIVE | Tampering | test suite hitting `api.yookassa.ru` | mitigate | FakeYooKassa stand-in; `grep` gate asserts no real-API string or `import yookassa` in `tests/`. |
| T-07-SECRET-LEAK | Info Disclosure | test env secrets | accept | Test-only dummy `YOOKASSA_*` values in conftest; never real creds, never logged. |
| T-07-SC | Tampering | no new package install in this plan | accept | This plan adds zero dependencies (pytest already present); the `yookassa`/`APScheduler` legitimacy checkpoint lives in Plan 02/06. |
</threat_model>

<verification>
- `cd backend && uv run pytest -q` exits 0 (baseline preserved; new tests xfail/skip cleanly).
- `cd backend && uv run pytest tests/ --co` collects all new files with no errors.
- No test references `api.yookassa.ru`; no `import yookassa` under `backend/tests/`.
</verification>

<success_criteria>
- FakeYooKassa + conftest fixture + the two ЮKassa test env vars exist.
- Every PAY-01..07 behavior and the subscription/paid bucket consume + correct-bucket refund have
  named, collectable red tests that are the green targets for Plans 03/04/05.
- Full backend suite remains green.
</success_criteria>

<output>
Create `.planning/phases/07-telegram-stars-payments/07-01-SUMMARY.md` when done.
</output>
