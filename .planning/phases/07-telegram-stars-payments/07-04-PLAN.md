---
phase: 07-telegram-stars-payments
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - backend/app/services/reading.py
autonomous: true
requirements: [PAY-06]
must_haves:
  truths:
    - "When free is exhausted but an active subscription window exists, the reading consumes from the subscription bucket"
    - "When free and subscription are exhausted but paid balance exists, the reading consumes from the paid bucket"
    - "An honest LLM failure after a subscription/paid consume refunds THAT bucket (not free)"
    - "Consumption order stays free → subscription → paid (Phase-6 LIMIT-04 / D-11)"
  artifacts:
    - path: "backend/app/services/reading.py"
      provides: "Filled SUBSCRIPTION/PAID arms in _consume_free_gate + correct-bucket refund on honest-fail"
      contains: "Bucket.SUBSCRIPTION"
  key_links:
    - from: "backend/app/services/reading.py _consume_free_gate"
      to: "user_limits subscription/paid columns"
      via: "atomic conditional UPDATE ... RETURNING per bucket (mirrors _consume_free_atomic)"
      pattern: "Bucket.PAID"
    - from: "backend/app/services/reading.py _honest_fail"
      to: "the consumed bucket"
      via: "refund routed to the bucket that was actually consumed"
      pattern: "_refund"
---

<objective>
Fill the Phase-6 SUBSCRIPTION/PAID seams in the existing atomic consume-gate so granted access
(Plan 03's webhook grant) is actually spendable: `_consume_free_gate` falls through free →
subscription → paid (D-11), each via its own atomic conditional `UPDATE ... RETURNING` mirroring
`_consume_free_atomic`, and the honest-fail path refunds the bucket that was ACTUALLY consumed (not
always free). This is the ONLY change to `reading.py` — a surgical extension of the seams already
documented in the module, NOT a fork of the gate (the safety-before-gate order and the Phase-4
`test_limit_untouched_on_*` invariants are preserved).

Purpose: A paid pack or subscription that grants but cannot be spent is worthless — this closes the
loop so PAY-06's "immediately usable" holds. Disjoint file from the payments service/API/scheduler,
so it runs in parallel with Plan 03.
Output: extended `_consume_free_gate` + bucket-aware refund; turns Plan-01
`test_reading_buckets.py` xfails green; Phase-6 free-quota tests stay green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-telegram-stars-payments/07-CONTEXT.md
@.planning/phases/07-telegram-stars-payments/07-RESEARCH.md
@.planning/phases/07-telegram-stars-payments/07-VALIDATION.md

# The gate to extend (do NOT fork) + the red tests this turns green
@backend/app/services/reading.py
@backend/app/models/billing.py
@backend/tests/test_reading_buckets.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill SUBSCRIPTION/PAID arms in _consume_free_gate (atomic, ordered free→sub→paid)</name>
  <read_first>
    - backend/app/services/reading.py — `determine_access` (already returns SUBSCRIPTION/PAID in order), `Bucket`, `_consume_free_gate` (the FREE-only seam to extend), `_consume_free_atomic` (the conditional `UPDATE ... WHERE ... RETURNING` + `.first()` idiom to MIRROR — "no slot" detected via the RETURNING row being absent, never rowcount), and the module docstring step 2.
    - 07-RESEARCH.md §"Anti-Patterns to Avoid" ("Forking the consume-gate — fill the SUBSCRIPTION/PAID seams with analogous atomic UPDATE ... RETURNING; reuse _refund_* for honest-fail") and §"Architectural Responsibility Map" (entitlement enforcement = reading.py gate).
    - backend/app/models/billing.py — `UserLimits.subscription_spreads_limit/subscription_spreads_used` and `paid_spreads_balance` (the columns the new atomic statements touch); the D-09 subscription encoding established by Plan 03's grant (subscription = "unlimited for the window" via a large limit vs used).
  </read_first>
  <action>
    Extend `_consume_free_gate` (do NOT rewrite the FREE path): after `bucket = determine_access(...)`,
    route each bucket to its own atomic consume:
      - `Bucket.FREE` → `_consume_free_atomic` (unchanged).
      - `Bucket.SUBSCRIPTION` → a new `_consume_subscription_atomic(session, user_id, now)`: a single
        conditional `update(UserLimits).where(UserLimits.user_id==user_id,
        UserLimits.subscription_spreads_used < UserLimits.subscription_spreads_limit)
        .values(subscription_spreads_used = UserLimits.subscription_spreads_used + 1)
        .returning(UserLimits.subscription_spreads_used, UserLimits.subscription_spreads_limit)`;
        `.first()` → `(used, limit)` or `None`. (Per the D-09 "unlimited for the window" contract,
        Plan 03 sets `subscription_spreads_limit` high enough that the window, not the count, is the
        real bound; the gate stays count-atomic so concurrency is still serialized by the row lock.)
      - `Bucket.PAID` → a new `_consume_paid_atomic(session, user_id, now)`: a single conditional
        `update(UserLimits).where(UserLimits.user_id==user_id, UserLimits.paid_spreads_balance > 0)
        .values(paid_spreads_balance = UserLimits.paid_spreads_balance - 1)
        .returning(UserLimits.paid_spreads_balance)`; `.first()` → a sentinel "consumed" or `None`.
      - `Bucket.NONE` → `None` (paywall).
    Return a value that lets `create_reading` know WHICH bucket was consumed (extend the gate's
    return so the caller can route the refund): e.g. return `(bucket, remaining)` where `remaining`
    is the free remaining for FREE (unchanged display), and the bucket tag is recorded for the
    honest-fail refund. Keep the "no slot ⇒ None ⇒ paywall" contract intact. Update the gate
    docstring to describe the now-filled arms. Preserve the `limits is None` fail-closed behavior.
    Adjust `create_reading`'s call site so `remaining` is still computed for the response (FREE: the
    free remaining; SUBSCRIPTION: None or the window-based value; PAID: the post-decrement balance —
    pick a consistent `remaining_limits` meaning and document it) and so the consumed bucket is
    threaded to `_honest_fail` (Task 2).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_reading_buckets.py -q -k "subscription or paid or bucket" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `_consume_free_gate` routes FREE/SUBSCRIPTION/PAID/NONE; the SUBSCRIPTION and PAID arms are
      atomic conditional `UPDATE ... RETURNING` statements (no `SELECT`-then-`UPDATE`, no rowcount —
      "no slot" via absent RETURNING row), mirroring `_consume_free_atomic`.
    - `determine_access` order free → subscription → paid is honored (the bucket-order test passes).
    - With an active subscription and no free, a reading consumes the subscription bucket; with only
      paid balance, it consumes the paid bucket (both tests pass).
    - The consumed bucket is threaded out of the gate for the refund path (Task 2 consumes it).
    - Phase-6 free-quota tests still pass; `cd backend && uv run pytest tests/test_reading_buckets.py
      -q` passes (or clean-skips without PG).
  </acceptance_criteria>
  <done>SUBSCRIPTION/PAID arms filled atomically in priority order; free path unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Bucket-aware honest-fail refund (refund the bucket actually consumed)</name>
  <read_first>
    - backend/app/services/reading.py — `_honest_fail` (currently refunds free only via `_refund_free`), `_refund_free`, and the `create_reading` honest-fail branch (`refund=not unlimited`).
    - 07-RESEARCH.md §"Anti-Patterns" ("reuse _refund_* for honest-fail") and the Phase-6 invariant that a post-consume non-success exit must leave the net counter unchanged (READ-10).
    - backend/tests/test_reading_buckets.py — `test_honest_fail_refunds_correct_bucket`.
  </read_first>
  <action>
    Add `_refund_subscription(session, user_id)` (atomic `update(UserLimits)...
    subscription_spreads_used = subscription_spreads_used - 1`) and `_refund_paid(session, user_id)`
    (atomic `... paid_spreads_balance = paid_spreads_balance + 1`), mirroring `_refund_free`. Change
    `_honest_fail` to accept the consumed-bucket tag (threaded from the gate via Task 1) and refund
    the MATCHING bucket: FREE → `_refund_free`, SUBSCRIPTION → `_refund_subscription`, PAID →
    `_refund_paid`, NONE/unlimited → no refund. Keep the refund in the SAME transaction before the
    soft-body return, and keep `await session.refresh(limits)` so the returned `remaining_limits`
    reflects the restored counter. Update the `create_reading` honest-fail branch to pass the
    consumed bucket. Do NOT change the safety-before-gate order, the crisis/abusive short-circuit
    (which never consumes), or any other behavior.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_reading_buckets.py tests/ -q -k "honest_fail or limit_untouched or refund or bucket" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `_refund_subscription` and `_refund_paid` exist (atomic UPDATEs mirroring `_refund_free`).
    - `_honest_fail` refunds the bucket that was actually consumed (the correct-bucket-refund test
      passes); a subscription/paid consume followed by an honest fail leaves that bucket's net
      counter unchanged.
    - The Phase-4 `test_limit_untouched_on_crisis`/`_abusive` and Phase-6 free honest-fail invariants
      still pass (no regression).
    - `cd backend && uv run pytest -q` exits 0 (full backend suite green; clean-skip without PG).
    - `ruff check app/services/reading.py` passes.
  </acceptance_criteria>
  <done>Honest-fail refunds the consumed bucket; all gate invariants preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reading request → consume gate | The atomic conditional UPDATE per bucket is the over-spend boundary under concurrency (same as Phase 6 free). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-OVERSPEND | Tampering/Elevation | subscription/paid consume | mitigate | Each bucket consumed via an atomic conditional `UPDATE ... WHERE <has slot> ... RETURNING`; the PG row lock serializes concurrent requests (mirrors the verified Phase-6 free-quota control). |
| T-07-REFUND-WRONG-BUCKET | Tampering | honest-fail refund | mitigate | Refund routed to the bucket actually consumed (threaded from the gate), so a paid/sub failure never silently credits free (READ-10 net-unchanged invariant). |
| T-07-IDOR | Elevation | gate UPDATEs | mitigate | Every consume/refund UPDATE is scoped `WHERE UserLimits.user_id == user.id` (the JWT identity); no body user_id (inherited from the reading router). |
</threat_model>

<verification>
- `cd backend && uv run pytest -q` exits 0 (full backend suite, incl. Phase-4/6 invariants).
- `tests/test_reading_buckets.py` subscription/paid consume + correct-bucket refund tests pass.
- `ruff check app/services/reading.py` passes.
</verification>

<success_criteria>
- Granted subscription/paid access is spendable through the same atomic gate, in free→sub→paid order.
- Honest-fail refunds the consumed bucket; no Phase-4/6 invariant regresses.
- The gate is extended, never forked.
</success_criteria>

<output>
Create `.planning/phases/07-telegram-stars-payments/07-04-SUMMARY.md` when done.
</output>
