---
phase: 07-telegram-stars-payments
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - backend/app/models/billing.py
  - backend/alembic/versions/0004_yookassa_payment_fields.py
  - backend/app/core/config.py
  - backend/app/seed/data/products.json
  - backend/app/seed/loader.py
  - backend/app/schemas/payment.py
  - backend/app/schemas/auth.py
  - backend/app/services/telegram_auth.py
  - backend/pyproject.toml
  - .env.example
  - DEPLOY.md
autonomous: false
requirements: [PAY-01, PAY-02, PAY-06]
user_setup:
  - service: yookassa
    why: "ЮKassa merchant account is required for live RUB payouts and webhook delivery (owner-provisioned, external)."
    env_vars:
      - name: YOOKASSA_SHOP_ID
        source: "ЮKassa dashboard → Settings → Shop (shopId)"
      - name: YOOKASSA_SECRET_KEY
        source: "ЮKassa dashboard → Integration → API keys (secret key)"
    dashboard_config:
      - task: "Register the webhook URL https://<backend>/api/payments/yookassa/webhook for events payment.succeeded / payment.canceled / refund.succeeded"
        location: "ЮKassa dashboard → Integration → HTTP notifications (or the Webhook API)"
must_haves:
  truths:
    - "Booting with YOOKASSA_SHOP_ID/SECRET_KEY absent fails fast at startup (no defaults)"
    - "alembic upgrade head applies 0004 adding the ЮKassa columns; downgrade reverses it"
    - "Seed loads 4 products (pack_1=69, pack_3=169, pack_10=449, sub_moon=299/30д) as RUB rows"
    - "GET /api/me surfaces paid balance + subscription window/status for the shop"
  artifacts:
    - path: "backend/alembic/versions/0004_yookassa_payment_fields.py"
      provides: "Additive migration: provider_payment_id/confirmation_url/idempotence_key/payment_method_id on payments; payment_method_id + nullable charge id on subscriptions; RUB/yookassa server_defaults"
      contains: "down_revision"
    - path: "backend/app/schemas/payment.py"
      provides: "ProductOut, CreatePaymentIn (product_slug only), CreatePaymentOut, RefundIn, webhook envelope"
      contains: "class CreatePaymentIn"
    - path: "backend/app/seed/data/products.json"
      provides: "4 seed product rows with RUB prices (D-15)"
      contains: "sub_moon"
  key_links:
    - from: "backend/app/core/config.py"
      to: "Settings fail-fast"
      via: "YOOKASSA_SHOP_ID / YOOKASSA_SECRET_KEY required fields (no default)"
      pattern: "YOOKASSA_SHOP_ID"
    - from: "backend/app/schemas/auth.py LimitsOut"
      to: "GET /api/me shop surface"
      via: "subscription_active + subscription_period_end fields"
      pattern: "subscription_period_end"
    - from: "backend/app/seed/loader.py"
      to: "products table"
      via: "upsert_by_slug(Product, products)"
      pattern: "products.json"
---

<objective>
The additive data + config foundation the whole ЮKassa phase stands on: migration 0004
(provider-agnostic payment id, saved-method id, confirmation url, idempotence key, RUB/yookassa
server-defaults, nullable subscription charge id), the `YOOKASSA_*` fail-fast secrets, the seed
`products` rows at D-15 RUB prices, the request/response schemas (`product_slug`-only create — no
client amount), and the `LimitsOut`/`GET /api/me` extension that lets the shop show the balance +
subscription window. Also adds `yookassa` to deps behind the legitimacy checkpoint.

Purpose: Every later plan (service, API, gate, scheduler, FE) consumes these contracts — defining
them first prevents the executor scavenger-hunt and locks the price-unit + idempotency-column
decisions (RESEARCH A1 / Schema Delta). All changes are additive (no real payments exist).
Output: model + migration 0004 + config + seed + payment schemas + LimitsOut fields + dep lock.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-telegram-stars-payments/07-CONTEXT.md
@.planning/phases/07-telegram-stars-payments/07-RESEARCH.md

# Models / migration style / config / seed / schema analogs to mirror
@backend/app/models/billing.py
@backend/app/models/enums.py
@backend/alembic/versions/0003_reading_answer_style.py
@backend/app/core/config.py
@backend/app/seed/loader.py
@backend/app/schemas/auth.py
@backend/app/services/telegram_auth.py
@.env.example
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy checkpoint — yookassa SDK</name>
  <what-built>
    Nothing yet — this gate authorizes adding `yookassa` to `backend/pyproject.toml`
    `[project.dependencies]`. Per 07-RESEARCH.md §"Package Legitimacy Audit", `yookassa` 3.11.0
    (official YooMoney SDK, github.com/yoomoney/yookassa-sdk-python, published 2026-06-26) is
    `[VERIFIED: PyPI]` + slopcheck `[OK]`. The unofficial async forks `async_yookassa`/`aioyookassa`
    are REJECTED — do NOT add them. This mirrors the Phase-1/4 lock-once checkpoint pattern.
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/yookassa/ — confirm version 3.11.x, maintained by YooMoney,
       repo github.com/yoomoney/yookassa-sdk-python.
    2. Confirm it is the OFFICIAL SDK (not a community fork) and supports Python 3.12.
    3. Approve adding `"yookassa==3.11.*"` to `backend/pyproject.toml` dependencies.
  </how-to-verify>
  <action>
    BLOCKING human gate — no auto work in this task. Present the legitimacy evidence above and PAUSE
    until the developer types "approved" (or names an alternative). On approval, Task 2 adds
    `"yookassa==3.11.*"` to `backend/pyproject.toml`; NEVER add `async_yookassa`/`aioyookassa`
    (rejected — unofficial, money path). This is a package-install legitimacy gate and is never
    auto-approvable (it ignores `workflow.auto_advance`).
  </action>
  <verify>
    <automated>echo "Manual legitimacy gate — yookassa verified at https://pypi.org/project/yookassa/ before install"</automated>
  </verify>
  <resume-signal>Type "approved" to add yookassa, or name an alternative.</resume-signal>
  <done>Developer approved the yookassa SDK (or chose an alternative); Task 2 may add the dependency.</done>
</task>

<task type="auto">
  <name>Task 2: billing.py model fields + migration 0004 + yookassa dep + config fail-fast</name>
  <read_first>
    - backend/app/models/billing.py — `Payment` (provider/currency/payload UNIQUE/telegram_payment_charge_id indexed/raw_update/status) and `Subscription` (telegram_payment_charge_id NOT NULL — must become nullable) + `Product`.
    - backend/alembic/versions/0003_reading_answer_style.py — the EXACT hand-written reversible migration style (revision/down_revision header, `op.add_column`, `op.drop_column`); 0004's `down_revision = "0003_reading_answer_style"`.
    - 07-RESEARCH.md §"Schema Delta (migration 0004)" — the precise additive column list + the A1 price-unit decision (store integer RUBLES in `stars_price`, format `"{:.2f}"`).
    - backend/app/core/config.py — the fail-fast `Settings` pattern (required fields have no default); add `YOOKASSA_SHOP_ID`/`YOOKASSA_SECRET_KEY` as required `str` and an optional `YOOKASSA_WEBHOOK_IPS: Annotated[list[str], NoDecode] = []` override (same NoDecode split as CORS_ORIGINS).
  </read_first>
  <action>
    Add `"yookassa==3.11.*"` to `backend/pyproject.toml` `[project.dependencies]` (approved Task 1),
    with a comment mirroring the anthropic/aiogram lock-once notes; run `uv sync`.
    In `backend/app/models/billing.py`: on `Payment` add nullable columns
    `provider_payment_id: Mapped[str | None]` (`String`, `unique=True`, `index=True`),
    `confirmation_url: Mapped[str | None]` (`String`), `idempotence_key: Mapped[str | None]`
    (`String`), `payment_method_id: Mapped[str | None]` (`String`); flip the `provider`
    server_default to `"yookassa"` and `currency` server_default to `"RUB"` (keep the old columns,
    only change the default for NEW rows — D-02). On `Subscription` add
    `payment_method_id: Mapped[str | None]` (`String`), `provider_payment_id: Mapped[str | None]`
    (`String`, nullable), `last_charge_at: Mapped[datetime | None]`, `period_index: Mapped[int]`
    (`Integer`, default 0, server_default "0"); make `telegram_payment_charge_id` NULLABLE
    (`Mapped[str | None]`) so a ЮKassa subscription insert (no Telegram charge id) is legal. Update
    the module docstring to note the provider pivot (Stars → ЮKassa). On `Product`, document in the
    docstring that `stars_price` now holds the price as **integer RUBLES** (A1 decision), formatted
    `"{:.2f}"` at charge time by the service helper.
    Create `backend/alembic/versions/0004_yookassa_payment_fields.py` (hand-written, reversible,
    `down_revision = "0003_reading_answer_style"`): `upgrade()` adds every new column above
    (`op.add_column`), and for the two server_default flips use
    `op.alter_column("payments","provider", server_default="yookassa")` /
    `...,"currency", server_default="RUB")` and
    `op.alter_column("subscriptions","telegram_payment_charge_id", existing_type=sa.String(),
    nullable=True)`. Add the UNIQUE index on `payments.provider_payment_id`
    (`op.create_index(..., unique=True)`). `downgrade()` reverses all of it (drop columns/index,
    restore the old server_defaults `telegram_stars`/`XTR` and the NOT NULL). No data migration (no
    real payments).
    In `backend/app/core/config.py`: add required `YOOKASSA_SHOP_ID: str` and
    `YOOKASSA_SECRET_KEY: str` (no default → fail-fast, INFRA-04) and an optional
    `YOOKASSA_WEBHOOK_IPS: Annotated[list[str], NoDecode] = []` with a `mode="before"` splitter
    (reuse the CORS splitter style). Update the `WEBHOOK_SECRET` comment: no longer used by ЮKassa.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "import app.models.billing, app.core.config; from app.models.billing import Payment, Subscription; assert hasattr(Payment,'provider_payment_id') and hasattr(Subscription,'payment_method_id'); print('models ok')" && uv run python -c "import importlib.util, pathlib; p=pathlib.Path('alembic/versions/0004_yookassa_payment_fields.py'); s=importlib.util.spec_from_file_location('m',p); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); assert m.down_revision=='0003_reading_answer_style'; assert callable(m.upgrade) and callable(m.downgrade); print('migration ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `Payment` has `provider_payment_id` (unique, indexed, nullable), `confirmation_url`,
      `idempotence_key`, `payment_method_id`; `provider`/`currency` server_defaults are
      `yookassa`/`RUB`. `Subscription` has `payment_method_id`, `provider_payment_id`,
      `last_charge_at`, `period_index`, and a NULLABLE `telegram_payment_charge_id`.
    - `0004_yookassa_payment_fields.py` exists with `down_revision="0003_reading_answer_style"`,
      reversible `upgrade()`/`downgrade()`, and the unique index on `provider_payment_id`.
    - `YOOKASSA_SHOP_ID`/`YOOKASSA_SECRET_KEY` are required in `Settings` (instantiating without
      them raises ValidationError); `pyproject.toml` lists `yookassa==3.11.*`.
    - The verify import/exec command prints `models ok` and `migration ok`.
  </acceptance_criteria>
  <done>Model + reversible migration 0004 + fail-fast config + yookassa dep landed.</done>
</task>

<task type="auto">
  <name>Task 3: Seed products + payment schemas + LimitsOut/GET /api/me extension + docs</name>
  <read_first>
    - backend/app/seed/loader.py — `upsert_by_slug(session, Product, rows)` and `run_seed` (add a `products = _load("products.json")` + `await upsert_by_slug(session, Product, products)` step; return a `products` count). Note `Product` must be importable in loader (add to the `from app.models import (...)` block).
    - backend/app/schemas/auth.py — `LimitsOut` (extend with subscription window/status) + the `from_attributes` pattern; backend/app/services/telegram_auth.py `project_limits` (extend the synthetic-unlimited branch with the new fields defaulting to inactive/None).
    - backend/app/schemas/reading.py (read the `ReadingOut.reason`/`reset_at` optional-field style) — mirror the Field(description=...) discipline for the new schemas.
    - 07-RESEARCH.md §"Schema Delta" (D-15 seed rows + product_type/spreads_amount/subscription_days) and §"Component Responsibilities" (schemas/payment.py contents).
  </read_first>
  <action>
    Create `backend/app/seed/data/products.json` with 4 rows (D-15), each carrying `slug`, `title`,
    `description`, `product_type`, `stars_price` (integer RUBLES), `is_active`:
    `pack_1` (title «1 расклад», product_type `one_time_spreads`, stars_price 69, spreads_amount 1),
    `pack_3` («3 расклада», `one_time_spreads`, 169, spreads_amount 3),
    `pack_10` («10 раскладов», `one_time_spreads`, 449, spreads_amount 10),
    `sub_moon` («Лунный доступ», `subscription`, 299, subscription_days 30, spreads_amount null).
    Brand-safe titles/descriptions (SAFE-06 — no «AI/нейросеть/модель»). Wire `run_seed` to upsert
    Product by slug (FK-safe — products have no FK deps) and include the count in its return dict.
    Create `backend/app/schemas/payment.py` (Pydantic v2, `from_attributes` where mapping ORM):
      - `ProductOut` — `slug, title, description, product_type, price_rub (int, sourced from
        stars_price), spreads_amount, subscription_days` (the catalog projection for `GET /api/products`).
      - `CreatePaymentIn` — `product_slug: str` ONLY. NO `amount`/`price` field (T-07-AMOUNT — the
        server recomputes price). Document the threat in the docstring.
      - `CreatePaymentOut` — `confirmation_url: str`, `payment_id: str` (our `payments.id`),
        `provider_payment_id: str`.
      - `RefundIn` — optional `amount_rub: int | None = None` (full refund when omitted).
      - `WebhookEnvelope` — `type: str`, `event: str`, `object: dict` (the unsigned ЮKassa
        notification; the handler reads only `object["id"]`, NEVER `object["status"]`).
    Extend `backend/app/schemas/auth.py` `LimitsOut` with `subscription_active: bool = False` and
    `subscription_period_end: datetime | None = None` (the shop reads «активна до DD.MM»). Extend
    `project_limits` (telegram_auth.py) so the synthetic-unlimited (no-row) branch sets both to
    inactive/None, and the row branch leaves them at the schema defaults (False/None) for now. NOTE:
    Plan 05 makes `project_limits` async + session-aware and fills these two fields from the live
    `Subscription` window (updating BOTH the /api/me and /api/auth/telegram call sites); in THIS plan
    they only need to exist + default so `GET /api/me` is shape-complete and the FE can render. Do
    NOT change `project_limits`'s sync signature here — that migration is Plan 05's explicit scope.
    Update `.env.example` (add `YOOKASSA_SHOP_ID=` / `YOOKASSA_SECRET_KEY=` / commented
    `# YOOKASSA_WEBHOOK_IPS=` with a note ЮKassa webhooks are unsigned → IP allowlist + re-fetch;
    note `WEBHOOK_SECRET` is no longer used by ЮKassa) and `DEPLOY.md` (a short «ЮKassa» section:
    set the two secrets in the backend app env; register the webhook URL
    `https://<backend>/api/payments/yookassa/webhook` in the ЮKassa dashboard).
  </action>
  <verify>
    <automated>cd backend && uv run python -c "import json,pathlib; rows=json.loads(pathlib.Path('app/seed/data/products.json').read_text(encoding='utf-8')); slugs={r['slug'] for r in rows}; assert slugs=={'pack_1','pack_3','pack_10','sub_moon'}, slugs; assert next(r for r in rows if r['slug']=='sub_moon')['subscription_days']==30; print('seed ok')" && uv run python -c "from app.schemas.payment import ProductOut, CreatePaymentIn, CreatePaymentOut, RefundIn, WebhookEnvelope; from app.schemas.auth import LimitsOut; assert 'amount' not in CreatePaymentIn.model_fields and 'price' not in CreatePaymentIn.model_fields; assert 'subscription_period_end' in LimitsOut.model_fields; print('schemas ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `products.json` has exactly the 4 slugs `pack_1/pack_3/pack_10/sub_moon` with RUB prices
      69/169/449/299 and `sub_moon.subscription_days == 30`; titles are brand-safe.
    - `run_seed` upserts Product by slug and reports a `products` count.
    - `schemas/payment.py` exports `ProductOut`, `CreatePaymentIn` (no `amount`/`price` field),
      `CreatePaymentOut`, `RefundIn`, `WebhookEnvelope`.
    - `LimitsOut` gains `subscription_active` + `subscription_period_end`; `project_limits` still
      returns a valid projection (sync signature unchanged here) for both the row and
      synthetic-unlimited branches, with the two new fields defaulting to False/None.
    - `.env.example` + `DEPLOY.md` document the two ЮKassa secrets + the dashboard webhook step.
    - The verify command prints `seed ok` and `schemas ok`.
  </acceptance_criteria>
  <done>Seed products + payment schemas + LimitsOut shop fields + env/deploy docs landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → backend config | YOOKASSA secrets cross here; fail-fast if absent, never defaulted, never logged. |
| client → create schema | `CreatePaymentIn` accepts only `product_slug`; no client amount can enter the price path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-SECRET-LEAK | Info Disclosure | YOOKASSA_SHOP_ID/SECRET_KEY | mitigate | Required `Settings` fields, no default (fail-fast like JWT_SECRET); never in any response schema; not logged. |
| T-07-AMOUNT | Tampering | `CreatePaymentIn` | mitigate | Schema carries no `amount`/`price` field — the server recomputes from the `products` row (enforced in Plan 04). Verify asserts the field's absence. |
| T-07-IDOR | Elevation | `payments.provider_payment_id` UNIQUE | mitigate | UNIQUE column added in 0004 is the exactly-once backstop for the Plan-04 grant; ownership scoping is enforced at the route. |
| T-07-SC | Tampering | `yookassa` install | mitigate | Blocking human-verify legitimacy checkpoint (Task 1) before the dep is added; official SDK only, unofficial async forks rejected. |
</threat_model>

<verification>
- `cd backend && uv run pytest -q` exits 0 (existing suite unaffected; new fields are additive).
- `Settings()` raises when `YOOKASSA_SHOP_ID`/`YOOKASSA_SECRET_KEY` are unset (fail-fast).
- Migration 0004 module imports, has the right `down_revision`, and both `upgrade`/`downgrade` are
  callable. (Live `alembic upgrade head` apply is exercised in the HUMAN-UAT deploy smoke.)
- `ruff check app` passes on the changed files.
</verification>

<success_criteria>
- Additive migration 0004 + model fields land the ЮKassa columns reversibly.
- `YOOKASSA_*` secrets are fail-fast required config.
- 4 RUB products seed; payment schemas (no client amount) + LimitsOut shop fields exist.
- `yookassa` is locked in deps behind the approved legitimacy checkpoint.
</success_criteria>

<output>
Create `.planning/phases/07-telegram-stars-payments/07-02-SUMMARY.md` when done.
</output>
</content>
