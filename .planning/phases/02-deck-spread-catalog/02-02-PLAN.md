---
phase: 02-deck-spread-catalog
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/main.tsx
  - frontend/src/lib/queryClient.ts
  - frontend/src/stores/selection.ts
  - frontend/src/theme/deckThemes.css
  - frontend/src/theme/useDeckTheme.ts
  - frontend/src/components/CardArtFallback.tsx
  - frontend/src/index.css
  - frontend/src/test/renderWithClient.tsx
  - frontend/src/stores/selection.test.ts
  - frontend/src/theme/useDeckTheme.test.ts
  - frontend/src/components/CardArtFallback.test.tsx
autonomous: true
requirements: [UI-02, DECK-05]

must_haves:
  truths:
    - "The React tree is wrapped in a QueryClientProvider so TanStack Query hooks work (first consumer)"
    - "A Zustand selection store holds {topic, deckSlug, spreadSlug} with setters and never mirrors server catalog data"
    - "Setting deckSlug updates document.documentElement dataset.deck, which swaps the active CSS-variable palette (background/accent change)"
    - "Each of the 6 deck slugs maps to its own CSS-variable palette identical to the seeded decks.json visual_style / TZ §21.2"
    - "A card with null/absent art renders an atmospheric deck-tinted CSS/SVG fallback (role=img) with no <img> tag and no network request"
    - "No banned brand-voice string (AI/нейросеть/модель/сгенерировано) appears in any added UI copy"
  artifacts:
    - path: "frontend/src/lib/queryClient.ts"
      provides: "Shared QueryClient instance with sane catalog defaults (staleTime)"
      contains: "QueryClient"
    - path: "frontend/src/stores/selection.ts"
      provides: "Zustand useSelection store {topic, deckSlug, spreadSlug, setTopic, setDeck, setSpread}"
      contains: "create<"
    - path: "frontend/src/theme/deckThemes.css"
      provides: "[data-deck=...] CSS-variable palette sets for all 6 decks + transition"
      contains: "data-deck"
    - path: "frontend/src/theme/useDeckTheme.ts"
      provides: "Effect hook that writes document.documentElement.dataset.deck from the selected deckSlug"
      contains: "dataset.deck"
    - path: "frontend/src/components/CardArtFallback.tsx"
      provides: "Per-deck atmospheric null-art placeholder (CSS/SVG, role=img)"
      contains: "role=\"img\""
  key_links:
    - from: "frontend/src/main.tsx"
      to: "frontend/src/lib/queryClient.ts"
      via: "QueryClientProvider client={queryClient} wraps <App>"
      pattern: "QueryClientProvider"
    - from: "frontend/src/theme/useDeckTheme.ts"
      to: "frontend/src/stores/selection.ts"
      via: "reads deckSlug from useSelection and writes the data-deck attribute"
      pattern: "useSelection"
    - from: "frontend/src/components/CardArtFallback.tsx"
      to: "frontend/src/theme/deckThemes.css"
      via: "consumes var(--deck-*) for the tinted placeholder"
      pattern: "var\\(--deck-"
---

<objective>
Lay the frontend foundation the catalog UI needs and ship two user-visible capabilities that stand on their own: (1) per-deck runtime theming (UI-02) — selecting a deck swaps a CSS-variable palette via a `data-deck` root attribute, and (2) the DECK-05 atmospheric null-art fallback. Also mount the `QueryClientProvider` (first TanStack Query consumer) and create the Zustand selection store + frontend test harness that Plan 03 builds on.

This is deliberately the wiring + presentation slice that has ZERO backend dependency, so it runs independently of Plan 01. Plan 03 (catalog UI) depends on both this plan and Plan 01.

Purpose: UI-02 theming and DECK-05 fallback are real, testable, deck-driven behaviors; the provider/store/test-harness gaps flagged in RESEARCH (Pitfall 3, Wave-0) are closed before any hook is written.
Output: 1 devDep addition (RTL), provider mount, selection store, theme CSS + hook, CardArtFallback, a renderWithClient test helper, and 3 frontend test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deck-spread-catalog/02-RESEARCH.md
@.planning/phases/02-deck-spread-catalog/02-VALIDATION.md

# Phase 1 frontend patterns to mirror (Zustand store, StrictMode root, Vitest+jsdom config, brand voice)
@frontend/src/stores/session.ts
@frontend/src/main.tsx
@frontend/src/App.tsx
@frontend/src/api/client.ts
@frontend/package.json

# Source of truth for the 6 palettes (must match exactly)
@backend/app/seed/data/decks.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount QueryClientProvider + Zustand selection store + frontend test harness (RTL)</name>
  <files>frontend/package.json, frontend/src/lib/queryClient.ts, frontend/src/main.tsx, frontend/src/stores/selection.ts, frontend/src/test/renderWithClient.tsx, frontend/src/stores/selection.test.ts</files>
  <read_first>
    - frontend/src/main.tsx (current StrictMode + createRoot tree; no provider yet)
    - frontend/src/stores/session.ts (Zustand `create` pattern + the "server state belongs to TanStack Query, never duplicated into Zustand" comment to mirror)
    - frontend/package.json (deps already present: @tanstack/react-query ^5, zustand ^5, vitest ^3.2.6, jsdom ^25; RTL is NOT installed)
    - 02-RESEARCH.md Pitfall 3 (no QueryClientProvider mounted), "Code Examples" (selection store + useQuery defaults), Validation Architecture Wave-0 (add @testing-library/react + jest-dom)
    - 02-VALIDATION.md Wave 0 Requirements (renderWithClient helper, QueryClientProvider mount)
  </read_first>
  <action>
    Add devDependencies `@testing-library/react` and `@testing-library/jest-dom` (and `@testing-library/dom` if required as a peer) to frontend/package.json, then `npm install`. These are the RTL libs RESEARCH flags as the Wave-0 gap for component-render tests — not an arbitrary substitution.

    Create `lib/queryClient.ts` exporting a single `queryClient = new QueryClient({ defaultOptions: { queries: { staleTime: 5 * 60_000, retry: 1, refetchOnWindowFocus: false } } })` (catalog is near-static).

    Edit `main.tsx`: wrap `<App />` in `<QueryClientProvider client={queryClient}>` inside the existing `<StrictMode>` (import from `@tanstack/react-query` and from `./lib/queryClient`). Do not otherwise change the boot.

    Create `stores/selection.ts`: a Zustand `useSelection` store holding `topic: string | null`, `deckSlug: string | null`, `spreadSlug: string | null` with `setTopic/setDeck/setSpread` setters (mirror session.ts style). Add the same "client-only selection; never store fetched catalog lists here" comment (ARCHITECTURE boundary). Export the `SelectionState` type.

    Create `src/test/renderWithClient.tsx`: a helper that renders a UI node wrapped in a fresh `QueryClientProvider` (new QueryClient per call so tests are isolated) using RTL's `render`; export `renderWithClient(ui)`.

    Write `stores/selection.test.ts` (vitest, no DOM needed): assert initial state is all-null and that `setDeck("moon_mirror")` updates `deckSlug` without touching `topic`/`spreadSlug` (immutable setter behavior).
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/stores/selection.test.ts && npm run build (build proves the provider mount type-checks)</automated>
  </verify>
  <done>RTL devDeps installed; lib/queryClient.ts exports a configured QueryClient; main.tsx wraps App in QueryClientProvider; useSelection store holds {topic,deckSlug,spreadSlug} + setters with no server-state mirroring; renderWithClient helper exists; selection.test.ts passes; `npm run build` green.</done>
</task>

<task type="auto">
  <name>Task 2: Per-deck runtime theming — deckThemes.css palettes + useDeckTheme effect hook (UI-02)</name>
  <files>frontend/src/theme/deckThemes.css, frontend/src/theme/useDeckTheme.ts, frontend/src/index.css, frontend/src/theme/useDeckTheme.test.ts</files>
  <read_first>
    - backend/app/seed/data/decks.json (the 6 visual_style.palette arrays + accent — the EXACT hex values to reproduce; do NOT invent new colors)
    - .planning/REFERENCE-TZ.md §21.2 "Цветовые палитры" (lines ~2598-2642 — same 6 palettes, confirm match with decks.json)
    - frontend/src/index.css (Tailwind v4 @theme / CSS-first setup already in repo — import deckThemes.css here)
    - 02-RESEARCH.md Pattern 4 (data-deck + CSS-variable sets, strategy A static CSS), Pitfall 4 (theme must be visibly applied — components read var(--deck-*), root bg reads --deck-bg, add transition)
  </read_first>
  <action>
    Create `theme/deckThemes.css`: a `:root` default set plus one `[data-deck="<slug>"]` block per deck defining `--deck-bg`, `--deck-accent`, `--deck-soft`, `--deck-deep` from each deck's `visual_style.palette` in decks.json (transcribe the exact hex — verified values: classic_arcana #1E1510/#C9A45C/#F3E3C3/#6B3F1D; moon_mirror #0B1026/#5E6AD2/#C7D2FE/#9BD5FF; shadow_arcana #08070A/#8B5CF6/#D6C7FF/#2A1838; heart_oracle #1A0710/#E8A0B8/#F6D7DE/#8F1D46; path_deck #101318/#C2A46D/#E5D3A3/#697386; forest_oracle #07130D/#D08A2D/#D7C58A/#1F5A3D). Add a `transition: background-color var(--duration, 400ms), color var(--duration, 400ms)` on the root/body selector so the swap is visibly smooth (UI-02 acceptance). `@import` deckThemes.css from index.css and make the app root background read `var(--deck-bg)`.

    Create `theme/useDeckTheme.ts`: a hook reading `deckSlug` from `useSelection` and, in a `useEffect`, setting `document.documentElement.dataset.deck = deckSlug ?? ""`. Export `useDeckTheme()`. (This is the runtime attribute flip that drives the CSS-var swap.)

    Write `theme/useDeckTheme.test.ts` (vitest + jsdom): render a tiny component that calls `useDeckTheme()`, drive `useSelection.getState().setDeck("moon_mirror")` (wrapped in act), and assert `document.documentElement.dataset.deck === "moon_mirror"`; then setDeck(null) -> dataset.deck === "" (no stale deck). This is the UI-02 automated proof; the visible-bg-change is the user smoke (Playwright not wired).
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/theme/useDeckTheme.test.ts && npm run build</automated>
  </verify>
  <done>deckThemes.css defines the 6 palettes (matching decks.json/§21.2) keyed by [data-deck] + a transition; index.css imports it and the root bg reads --deck-bg; useDeckTheme writes dataset.deck from the selection store; useDeckTheme.test.ts proves the attribute flips on setDeck and clears on null; build green.</done>
</task>

<task type="auto">
  <name>Task 3: DECK-05 atmospheric null-art fallback component (CardArtFallback)</name>
  <files>frontend/src/components/CardArtFallback.tsx, frontend/src/components/CardArtFallback.test.tsx</files>
  <read_first>
    - 02-RESEARCH.md Pattern 5 (CardArtFallback technique: gradient --deck-bg→--deck-deep, inline <svg> sigil stroked in --deck-accent, role="img" aria-label, no network) and DECK-05 row in the Test Map (null art -> fallback, no <img>)
    - frontend/src/theme/deckThemes.css (the --deck-* vars this consumes — Task 2 output)
    - frontend/src/App.tsx (brand-voice: ritual/oracle framing, zero AI strings)
  </read_first>
  <action>
    Create `components/CardArtFallback.tsx`: a component `CardArt({ src, alt, glyph })` where `src?: string | null` and `glyph?` is OPTIONAL (defaults to a deck-level sigil/initial when no per-card glyph is supplied — e.g. deck-preview tiles in Plan 02-03, which have no per-card glyph in Phase 2). When `src` is a non-empty string, render an `<img src alt width height loading="lazy" />`. When `src` is null/empty (always in Phase 2 — no deck_cards seeded), render the atmospheric fallback instead: a `<div role="img" aria-label={alt}>` with a radial-gradient/linear-gradient background from `var(--deck-bg)` to `var(--deck-deep)`, a subtle glow overlay, and an inline `<svg viewBox="0 0 100 160" aria-hidden>` sigil (the card `glyph` — e.g. arcana number/initial — drawn/stroked in `var(--deck-accent)`). Pure CSS/SVG, NO network request, NO broken `<img>`. Keep any visible text ritual-framed (no AI/нейросеть/модель strings).

    Write `CardArtFallback.test.tsx` (vitest + RTL, use renderWithClient or bare render): (a) `src={null}` -> the element with `role="img"` is present, its accessible name equals `alt`, and `container.querySelector("img")` is null (no img, no network); (b) `src="/x.png"` -> an `<img>` IS rendered with that src. This is the DECK-05 automated proof.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/components/CardArtFallback.test.tsx && npm run test -- --run && npm run build</automated>
  </verify>
  <done>CardArtFallback renders a deck-tinted role="img" CSS/SVG placeholder (no <img>, no network) when art is null and a real <img> when a src is given; CardArtFallback.test.tsx proves both branches; full `npm run test --run` + `npm run build` green; no banned brand-voice strings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seeded palette data → rendered CSS vars | `--deck-*` values are controlled (from decks.json/§21.2), never user input |
| Deck/spread copy → React render | React escapes by default; no `dangerouslySetInnerHTML` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering (XSS) | Rendering deck/card text + theme values | mitigate | React auto-escapes; this plan adds no `dangerouslySetInnerHTML`. CSS-var theme values are controlled constants (palettes), not user input. |
| T-02-08 | Information Disclosure | CardArtFallback network behavior | mitigate | Fallback is pure CSS/SVG — issues NO network request for absent art (asserted: no `<img>` when src null), so there is no broken-asset/SSRF surface. |
| T-02-05 | Information Disclosure | Added UI microcopy | mitigate (product/compliance) | Brand-voice rule: zero AI/нейросеть/модель/сгенерировано strings in any copy added by this plan (grep-checkable). |
| T-02-SC | Tampering | npm install (RTL devDeps) | mitigate | Only `@testing-library/react` + `@testing-library/jest-dom` (+ peer) added — both are the maintained, ubiquitous React testing libs named in RESEARCH's Validation Architecture (not [ASSUMED]/[SUS]); devDeps only, never shipped in the prod bundle. No [SLOP] packages. |
</threat_model>

<verification>
- `cd frontend && npm install` clean (RTL devDeps resolve).
- `cd frontend && npm run test -- --run` green (selection, useDeckTheme, CardArtFallback).
- `cd frontend && npm run build` green (provider mount + theme + component type-check; production bundle unaffected by test/dev code).
- Brand-voice: no `/ai|нейросет|модель|сгенерирован/i` match in the files added by this plan.
- User smoke (visual, documented in SUMMARY): run the app, select two different decks, confirm the background/accent visibly change (UI-02) and a card with no art shows the atmospheric placeholder (DECK-05).
</verification>

<success_criteria>
- UI-02: selecting a deck flips `document.documentElement.dataset.deck`, swapping the CSS-variable palette (bg/accent) with a visible transition; all 6 palettes match the seed/§21.2.
- DECK-05: a null-art card renders an atmospheric deck-tinted CSS/SVG fallback (role="img", no `<img>`, no network).
- TanStack Query is usable (QueryClientProvider mounted) and the Zustand selection store + RTL test harness exist for Plan 03.
- No brand-voice violations in added copy.
</success_criteria>

<output>
Create `.planning/phases/02-deck-spread-catalog/02-02-SUMMARY.md` when done.
</output>
