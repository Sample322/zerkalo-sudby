---
phase: 03-the-ritual-mock
plan: 06
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - frontend/src/components/result/ResultScreen.tsx
  - frontend/src/components/result/ResultScreen.test.tsx
autonomous: true
requirements: [READ-09, UI-01, UI-03, SAFE-06]
must_haves:
  truths:
    - "The result screen shows the question, topic, deck, spread, and date from the mock reading"
    - "One glass card renders per drawn card with name, position, orientation, short meaning, interpretation, and deck accent"
    - "An overall summary panel (linkage/main factor/attention/advice/closing) reveals as staggered blocks"
    - "«Ещё расклад» is fully wired: it returns to selection preserving question + topic"
    - "«Сохранить карточку» and «История» are present but visibly stubbed («скоро»), with no dead click or error"
  artifacts:
    - path: "frontend/src/components/result/ResultScreen.tsx"
      provides: "Full result screen rendering MockReading: meta row + per-card cards + summary panel + sticky actions (replaces the 03-01 stub)"
      min_lines: 50
  key_links:
    - from: "frontend/src/components/result/ResultScreen.tsx"
      to: "frontend/src/stores/selection.ts"
      via: "«Ещё расклад» → startReadingAgain() (preserves question+topic, D-04); reads the reading slot"
      pattern: "startReadingAgain"
    - from: "frontend/src/components/result/ResultScreen.tsx"
      to: "frontend/src/reading/types.ts"
      via: "renders all MockReading fields (cards[] + summary)"
      pattern: "MockReading|summary"
    - from: "frontend/src/components/result/ResultScreen.tsx"
      to: "frontend/src/reading/copy.ts"
      via: "result labels + action labels + «скоро» from the copy bank"
      pattern: "copy"
---

<objective>
Deliver the result slice (READ-09/D-12) by replacing the 03-01 `ResultScreen` stub with the real screen: a premium-dark result screen populated entirely from the `MockReading`. Header «Расклад готов»; a meta row (вопрос / тема / колода / расклад / дата); one glass card per drawn card (name, position, orientation, short meaning, interpretation, italic deck accent); an overall summary panel (связка / главный фактор / внимание / совет / завершающая фраза) revealed as staggered blocks (the "final gather"); and sticky actions — «Ещё расклад» fully wired to `startReadingAgain()` (D-04, preserves question + topic), with «Сохранить карточку» and «История» present but stubbed («скоро», D-12). The question is rendered as a text node (T-3-01/V5).

Purpose: This is the closing screen of the journey and the last piece that proves the whole mock flow end-to-end. Wiring «Ещё расклад» also closes the loop back to selection with state preserved. FlowRoot already routes `step==="result"` to this file (03-01), so this plan touches ONLY the result component + test — no FlowRoot edit.
Output: The real `ResultScreen.tsx` (replacing the stub).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-the-ritual-mock/03-CONTEXT.md
@.planning/phases/03-the-ritual-mock/03-RESEARCH.md
@.planning/phases/03-the-ritual-mock/03-PATTERNS.md
@.planning/phases/03-the-ritual-mock/03-UI-SPEC.md
@CLAUDE.md
@.planning/phases/03-the-ritual-mock/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ResultScreen — meta row + per-card glass cards + summary panel (replaces the stub)</name>
  <files>frontend/src/components/result/ResultScreen.tsx</files>
  <read_first>
    - frontend/src/components/result/ResultScreen.tsx — the 03-01 STUB to replace (keep the export name FlowRoot imports)
    - frontend/src/components/SpreadCard.tsx — analog for the glass info-card (title Heading + accent badge + meta rows on a deck-var glass surface) to mirror for each result card
    - frontend/src/components/CatalogScreen.tsx — analog for the screen shell + sections + the recommendation-banner eyebrow/Display/body style (mirror for the meta row тема/колода/дата + summary panel)
    - frontend/src/components/AuthGate.tsx — analog for the header block + in-character voice
    - frontend/src/components/CardArtFallback.tsx — `CardArt` for an optional card thumbnail on each result card
    - frontend/src/reading/types.ts — `MockReading` (every field rendered: question/topic/deckSlug/spreadSlug/createdAt + cards[] + summary) [from 03-01]
    - frontend/src/stores/selection.ts — the `reading` slot (the MockReading to render) [from 03-01]
    - .planning/phases/03-the-ritual-mock/03-UI-SPEC.md — "Screen 5 — Result" (header «Расклад готов»; meta row Label; per-card glass card fields; summary panel "final gather" staggered blocks) + Typography (Display/Heading/Body/Label) + Color (60/30/10, accent reserved-for) + Copywriting (result header)
    - .planning/phases/03-the-ritual-mock/03-PATTERNS.md — "components/result/ResultScreen.tsx" assignment (glass info-card + meta style excerpts) + Shared 1/3/4
    - .planning/phases/03-the-ritual-mock/03-RESEARCH.md — Security Domain V5 (render the question as text)
  </read_first>
  <action>
    Replace the 03-01 stub at `frontend/src/components/result/ResultScreen.tsx` with the real screen, keeping the SAME exported component name FlowRoot imports. Read the `MockReading` from the selection store's `reading` slot (consume, do not re-fetch). Header «Расклад готов» (Display). A meta row (Label style, eyebrow + value): вопрос (render `reading.question` as a text node; if null show a neutral «Общий расклад» label per D-13), тема, колода, расклад, дата (format `createdAt`). One glass card per `reading.cards[]` using the SpreadCard glass recipe (Shared 1 deck-var surface): card name (Heading), position title, orientation, short meaning (Body), interpretation (Body, max ~2–4 short paragraphs separated by `lg` per UI-SPEC Typography), and the deck mystical accent in italic (the "deck's voice"). Optionally include the `CardArt` thumbnail. Below the cards, a summary panel rendering all `reading.summary` fields (связка / главный фактор / внимание / совет / завершающая фраза) as staggered blocks (`opacity`+`y`, 200–280ms — the "final gather", UI-SPEC). Use `m.*` (LazyMotion), compositor-only. Reserve `pb-24` for the sticky actions added in Task 2. Render ALL user/text content as React text nodes — no `dangerouslySetInnerHTML` (T-3-01/V5).
    Do NOT edit FlowRoot — it already routes `step==="result"` to this component. (Sticky actions are Task 2.)
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `ResultScreen.tsx` renders the header, the meta row (question/topic/deck/spread/date), one glass card per `reading.cards[]` (name/position/orientation/shortMeaning/interpretation/deckAccent), and a summary panel covering all five `reading.summary` fields
    - `reading.question` is rendered as a React text node (no `dangerouslySetInnerHTML` in the file); null question shows the neutral general-reading label
    - The stub is replaced (same export name); `frontend/src/flow/FlowRoot.tsx` is NOT in this plan's diff
    - `cd frontend && npm run build` type-checks
  </acceptance_criteria>
  <done>The result screen renders every MockReading field (meta + per-card cards + full summary) on premium-dark glass, with the question shown safely as text — with no FlowRoot change.</done>
</task>

<task type="auto">
  <name>Task 2: Sticky actions — «Ещё расклад» wired (D-04) + «Сохранить карточку»/«История» stubbed (D-12)</name>
  <files>frontend/src/components/result/ResultScreen.tsx, frontend/src/components/result/ResultScreen.test.tsx</files>
  <read_first>
    - frontend/src/components/result/ResultScreen.tsx — the file from Task 1 (add the sticky action bar)
    - frontend/src/components/CatalogScreen.test.tsx — analog for the component test setup (renderWithClient if needed; store-driven assertions; SAFE-06 ban-list regex)
    - frontend/src/stores/selection.ts — `startReadingAgain` (D-04: back to selection, preserve question+topic) + the `reading` slot for the fixture [from 03-01]
    - frontend/src/lib/telegram.ts — `getSafeAreaInsets` for the sticky bar bottom padding (UI-04) [from 03-01]
    - frontend/src/reading/copy.ts — «Ещё расклад» / «Сохранить карточку» / «История» / «скоро» labels [from 03-01]
    - .planning/phases/03-the-ritual-mock/03-UI-SPEC.md — "Screen 5 — Result" sticky actions (Ещё расклад accent primary wired; Сохранить карточку + История secondary stubbed «скоро») + Copywriting (result CTA rows) + Telegram Adaptation (sticky CTA padded by safe-area inset)
    - .planning/phases/03-the-ritual-mock/03-PATTERNS.md — "components/result/ResultScreen.tsx" assignment (sticky actions: «Ещё расклад» wired → startReadingAgain; others visible-but-disabled «скоро»)
    - .planning/phases/03-the-ritual-mock/03-VALIDATION.md — Per-Task Verification Map (READ-09/D-05 + the result render/«ещё расклад» wired/stubbed expectation)
  </read_first>
  <action>
    Add a sticky bottom action bar to `ResultScreen.tsx` (pinned bottom, padded above the Telegram safe-area inset bottom via `getSafeAreaInsets()` — UI-04; never `env()`): «Ещё расклад» as the accent-filled primary (Color reserved-for #1) wired on tap to `startReadingAgain()` (D-04 — returns to selection preserving question + topic; deck/spread re-selectable). «Сохранить карточку» and «История» as secondary controls that are visibly present but disabled with a quiet «скоро» affordance (D-12) — no dead click, no error, no navigation. Use `m.*` (LazyMotion) with `whileTap` on the enabled primary. Labels from copy.ts.
    Create `frontend/src/components/result/ResultScreen.test.tsx`: seed the store `reading` slot with a `MockReading` fixture (3 cards + full summary) and a preserved question+topic. Assert: (1) all reading fields render — question text, topic, deck, spread, date, each card's name/position/orientation/shortMeaning/interpretation/deckAccent, and all five summary fields (READ-09); (2) tapping «Ещё расклад» calls `startReadingAgain` → store step becomes "selection" AND `question`/`topic` are unchanged (D-04); (3) «Сохранить карточку» and «История» are present and disabled (the «скоро» affordance), and tapping them does not change the step (D-12); (4) the rendered result copy contains no banned brand-voice token (SAFE-06 regex). Mirror CatalogScreen.test.tsx style.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/components/result/ResultScreen.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run test -- --run src/components/result/ResultScreen.test.tsx` passes: all READ-09 fields render; «Ещё расклад» → step "selection" with question+topic preserved (D-04); save/история disabled «скоро» and inert (D-12); copy passes SAFE-06 regex
    - «Ещё расклад» calls `startReadingAgain` (not a full reset); the sticky bar padding derives from `getSafeAreaInsets` (UI-04)
    - `ResultScreen.tsx` contains «Ещё расклад» wired and the two stubbed actions; no `dangerouslySetInnerHTML`
    - `cd frontend && npm run test -- --run && npm run build` (wave-merge gate) stays green
  </acceptance_criteria>
  <done>«Ещё расклад» closes the loop to selection with question+topic preserved; save/история are honestly stubbed; the full result screen is proven to render every MockReading field with brand-safe copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MockReading (client state) → result render | Ephemeral client data, including the user's question, rendered read-only |
| User question (echoed on result) → DOM | The untrusted free-text from selection is displayed here — must render as text |
| Telegram WebView (insets) → sticky action bar | Platform safe-area consumed read-only via the telegram.ts seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering (XSS) | question echoed in the meta row | mitigate | Render `reading.question` as a React text node; NO `dangerouslySetInnerHTML`/`innerHTML`. This is the screen where the stored question is most visibly re-displayed; when Phase 4 persists it, the value is already validated (10–500) and rendered text-only. |
| T-3-04 | Info-disclosure / policy | per-card + summary + action copy | mitigate | Card/summary text from the mock builder + copy.ts (both brand-safe); ResultScreen test asserts the on-screen copy passes the SAFE-06 ban-list regex |
| T-3-03 | Spoofing/Elevation | result rendering the mock reading; stubbed save/история | accept (contract guard) | Result displays mock data only; save/история are inert stubs (no persistence this phase, D-12). The real history/save + draw stay Phase-4/5 backend concerns — no client authority implied. |
</threat_model>

<verification>
- `cd frontend && npm run test -- --run src/components/result/ResultScreen.test.tsx && npm run build` green
- All READ-09 fields render; «Ещё расклад» → `startReadingAgain` (step "selection", question+topic preserved); save/история stubbed «скоро» and inert
- `reading.question` rendered as text; no `dangerouslySetInnerHTML`; sticky bar uses Telegram SDK insets; FlowRoot untouched
- Manual (HUMAN-UAT per 03-VALIDATION Manual-Only): on device confirm the summary "final gather" stagger feels smooth and the deck theme carries into the result
</verification>

<success_criteria>
- READ-09 satisfied: question/topic/deck/spread/date + per-card cards + summary + actions, all from the MockReading
- D-04 («ещё расклад» preserves question+topic) and D-12 (save/история stubbed, «ещё расклад» wired) both honored
- UI-01 premium-dark + UI-03 staggered reveal; question rendered text-safe; no FlowRoot edit (zero Wave-2 file conflict)
</success_criteria>

<output>
Create `.planning/phases/03-the-ritual-mock/03-06-SUMMARY.md` when done.
</output>
