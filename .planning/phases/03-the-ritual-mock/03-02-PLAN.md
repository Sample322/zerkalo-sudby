---
phase: 03-the-ritual-mock
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - frontend/src/components/onboarding/OnboardingFlow.tsx
  - frontend/src/components/onboarding/OnboardingFlow.test.tsx
autonomous: true
requirements: [ONB-01, ONB-02, ONB-03, ONB-04, SAFE-06]
must_haves:
  truths:
    - "On first launch the user sees a 3–4 screen onboarding with a plain-language reversed-cards explainer"
    - "A persistent «Пропустить» skips onboarding from any slide and jumps to selection"
    - "Completing or skipping onboarding sets the localStorage flag so it is not shown again"
    - "The reversed-cards explainer uses non-scary framing (задержка/сопротивление/напряжение), never «плохо»"
  artifacts:
    - path: "frontend/src/components/onboarding/OnboardingFlow.tsx"
      provides: "3–4 slide onboarding with skip + reversed-cards explainer + first-reading CTA (replaces the 03-01 stub)"
      min_lines: 40
  key_links:
    - from: "frontend/src/components/onboarding/OnboardingFlow.tsx"
      to: "frontend/src/hooks/useOnboardingSeen.ts"
      via: "markOnboardingSeen() on skip and on final CTA"
      pattern: "markOnboardingSeen"
    - from: "frontend/src/components/onboarding/OnboardingFlow.tsx"
      to: "frontend/src/stores/selection.ts"
      via: "goTo('selection') after seen"
      pattern: "goTo"
    - from: "frontend/src/components/onboarding/OnboardingFlow.tsx"
      to: "frontend/src/reading/copy.ts"
      via: "all slide + explainer + CTA strings sourced from the copy bank"
      pattern: "copy"
---

<objective>
Deliver the onboarding slice (ONB-01..04) by replacing the 03-01 `OnboardingFlow` stub with the real screen: a 3–4 slide, vertically-centered, full-bleed onboarding sequence with a persistent «Пропустить», a plain-language reversed-cards explainer (ONB-03), and a final «Сделать первый расклад» CTA. Completing or skipping persists the localStorage flag and advances the flow to selection; on a return visit FlowRoot (already wired in 03-01) starts at selection (ONB-04).

Purpose: This is the first screen a new user sees — it sets the brand voice and the felt-quality bar before the selection flow. It is a self-contained vertical slice: after this plan a first-launch user can read, skip, and exit onboarding end-to-end. FlowRoot already routes `step==="onboarding"` to this file (03-01), so this plan touches ONLY the onboarding component + its test — no FlowRoot edit.
Output: The real `OnboardingFlow.tsx` (replacing the stub).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-the-ritual-mock/03-CONTEXT.md
@.planning/phases/03-the-ritual-mock/03-RESEARCH.md
@.planning/phases/03-the-ritual-mock/03-PATTERNS.md
@.planning/phases/03-the-ritual-mock/03-UI-SPEC.md
@CLAUDE.md
@.planning/phases/03-the-ritual-mock/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the 3–4 slide OnboardingFlow with skip + reversed-cards explainer (replaces the stub)</name>
  <files>frontend/src/components/onboarding/OnboardingFlow.tsx</files>
  <read_first>
    - frontend/src/components/onboarding/OnboardingFlow.tsx — the 03-01 STUB to replace (confirm the export name FlowRoot imports, keep it identical)
    - frontend/src/components/AuthGate.tsx — analog for the centered-hero layout (`flex min-h-full flex-col items-center justify-center gap-6 px-6 text-center`, Display title + Body subtitle) and the in-character RU voice
    - frontend/src/components/DeckCarousel.tsx — analog for the index-map-over-array pattern and stable keys (never index for AnimatePresence children)
    - frontend/src/components/CatalogScreen.tsx — analog for the screen-level `<main>` + section composition and one-field `useSelection` selectors
    - frontend/src/components/DeckCard.tsx — analog for `motion`/`m.*` micro-interaction (`whileTap` scale) and deck-var styling
    - frontend/src/hooks/useOnboardingSeen.ts — `markOnboardingSeen()` (call on skip + final CTA) [from 03-01]
    - frontend/src/stores/selection.ts — `goTo` action [from 03-01]
    - frontend/src/reading/copy.ts — the onboarding slide copy + ONB-03 explainer strings [from 03-01]
    - .planning/phases/03-the-ritual-mock/03-UI-SPEC.md — "Screen 1 — Onboarding" + Copywriting Contract onboarding rows (verbatim copy) + Typography (Display 28/600, Body 16/400) + the deck-var theming note (use `--deck-accent`/`--deck-soft`, not legacy `--color-glow`)
    - .planning/phases/03-the-ritual-mock/03-RESEARCH.md — "Don't Hand-Roll" (no carousel lib — reuse the AnimatePresence/step-index primitive) + onboarding copy block
    - .planning/phases/03-the-ritual-mock/03-PATTERNS.md — "components/onboarding/OnboardingFlow.tsx" assignment + Shared 1/2/3/4
  </read_first>
  <action>
    Replace the 03-01 stub at `frontend/src/components/onboarding/OnboardingFlow.tsx` with the real screen, keeping the SAME exported component name FlowRoot already imports (do not rename, or FlowRoot breaks). Build a 3–4 slide sequence driven by a local `index` state + the same `AnimatePresence` primitive already used in FlowRoot (no carousel library — RESEARCH Don't Hand-Roll). Each slide is a vertically-centered hero (mirror AuthGate's centered layout classes) with a Display title (28px/600) + Body subtitle (16px/400) and a decorative accent sigil tinted `var(--deck-accent)`. Pull ALL strings from `reading/copy.ts` (do not inline new copy) — slides 1–3 + the ONB-03 reversed-cards explainer (folded into slide 3 or as a dedicated 4th slide) + the final CTA «Сделать первый расклад».
    Navigation: a «Далее» control advances the local index; a persistent «Пропустить» (ONB-02) is present on every slide. On the final CTA and on «Пропустить»: call `markOnboardingSeen()` then `goTo("selection")` (ONB-04 persists; flow advances). Use deck vars for color (per UI-SPEC, not legacy `--color-glow`); use `m.*` from `motion/react-m` for any animated element since this renders inside FlowRoot's LazyMotion (PATTERNS Shared 2 override / Pitfall 5). Compositor-only transitions (opacity/transform), screen tokens from UI-SPEC. Touch targets ≥44px (UI-SPEC spacing floor).
    Render any user-visible text as React text nodes only (no `dangerouslySetInnerHTML`). Do NOT edit FlowRoot — it already routes to this component.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/components/onboarding/OnboardingFlow.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `OnboardingFlow.tsx` renders a Display title + Body subtitle from `reading/copy.ts` and a «Пропустить» control on every slide, and keeps the exported name FlowRoot imports
    - The reversed-cards explainer text is present and (asserted via the ban-list regex in the test) contains no «плохо»/«приговор» and no banned brand-voice token
    - All onboarding copy is sourced from `reading/copy.ts` (no new string literals introduced in the component for slide/skip/CTA text)
    - `frontend/src/flow/FlowRoot.tsx` is NOT in this plan's diff (the stub swap is component-local)
  </acceptance_criteria>
  <done>A first-launch user sees the 3–4 slide onboarding with the plain-language reversed-cards explainer, can advance or skip from any slide, and the explainer voice is brand-safe — with no FlowRoot change.</done>
</task>

<task type="auto">
  <name>Task 2: Skip/advance interaction test + onboarding persistence wiring</name>
  <files>frontend/src/components/onboarding/OnboardingFlow.test.tsx</files>
  <read_first>
    - frontend/src/components/DeckCarousel.test.tsx — analog for a plain `render` + `fireEvent` interaction test asserting forwards-advance and nothing-broken
    - frontend/src/components/CatalogScreen.test.tsx — analog for the inline SAFE-06 ban-list regex `/ai|нейросет|модель|сгенерирован/i` and store-driven assertions
    - frontend/src/stores/selection.ts — `goTo`/`step` to assert the transition to "selection" [from 03-01]
    - frontend/src/hooks/useOnboardingSeen.ts — `hasSeenOnboarding` to assert the flag is set after skip/finish [from 03-01]
    - frontend/src/components/onboarding/OnboardingFlow.tsx — the component under test (from Task 1)
    - .planning/phases/03-the-ritual-mock/03-PATTERNS.md — Shared 6 (Vitest + RTL test pattern; component tests use plain `render`)
    - .planning/phases/03-the-ritual-mock/03-VALIDATION.md — Per-Task Verification Map (ONB-04/D-11 + SAFE-06 rows)
  </read_first>
  <action>
    Create `frontend/src/components/onboarding/OnboardingFlow.test.tsx` mirroring DeckCarousel.test.tsx's plain-render + fireEvent style. Reset the store in `beforeEach` (setState) and clear localStorage so the test starts pre-onboarding.
    Cases: (1) tapping «Пропустить» (ONB-02) sets the onboarding-seen flag (`hasSeenOnboarding()` true) AND transitions the store step to "selection" (ONB-04); (2) advancing through all slides via «Далее» reaches the final CTA «Сделать первый расклад», and tapping it sets the seen flag + transitions to "selection"; (3) the reversed-cards explainer text rendered on screen does not match the ban-list regex (SAFE-06) and contains none of «плохо»/«приговор»; (4) a nothing-broken render assertion (the first slide title is visible).
    Do not add watch-mode flags; the run command is `--run`.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/components/onboarding/OnboardingFlow.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run test -- --run src/components/onboarding/OnboardingFlow.test.tsx` passes all four cases
    - Skip and finish both assert `hasSeenOnboarding()` true and store step === "selection"
    - The SAFE-06 regex assertion over the on-screen explainer passes
    - `cd frontend && npm run test -- --run && npm run build` (wave-merge gate) stays green
  </acceptance_criteria>
  <done>Skip and finish paths are proven to persist the flag and advance to selection; the reversed-cards explainer is proven brand-safe by automated assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage (`zerkalo.onboarding_completed`) → app | Client-controlled cosmetic flag set by onboarding completion/skip |
| Static onboarding copy → DOM | Authored strings rendered as React text nodes (no user input on this screen) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-02 | Tampering | onboarding localStorage flag | accept | Cosmetic only (re-shows onboarding at worst); no trust placed in it — acceptable per RESEARCH Security Domain |
| T-3-04 | Info-disclosure / policy | onboarding + reversed-cards copy | mitigate | All strings sourced from `copy.ts` which is ban-list-gated (SAFE-06); this plan additionally asserts the rendered explainer is free of «плохо»/«приговор» and banned tokens |
| T-3-05 | Tampering (XSS) | slide text rendering | mitigate | Render all copy as React text nodes; no `dangerouslySetInnerHTML`/`innerHTML` (no user input here, but the rule holds uniformly) |
</threat_model>

<verification>
- `cd frontend && npm run test -- --run src/components/onboarding/OnboardingFlow.test.tsx` green
- `cd frontend && npm run test -- --run && npm run build` green (wave-merge gate)
- The stub at `onboarding/OnboardingFlow.tsx` is replaced (same export name); skip + finish set the localStorage flag and advance to selection; FlowRoot untouched
- Manual (HUMAN-UAT, routes per 03-VALIDATION Manual-Only): on device, confirm slide crossfades are smooth and the skip control is reachable on every slide
</verification>

<success_criteria>
- ONB-01 (3–4 screens incl. atmosphere + «не приговор» + first-reading CTA), ONB-02 (skip), ONB-03 (plain reversed-cards explainer), ONB-04 (persisted, not re-shown) all satisfied and tested where automatable
- The onboarding step renders through FlowRoot's existing route and uses the locked motion primitive + deck-var theming, with no FlowRoot edit (zero Wave-2 file conflict)
</success_criteria>

<output>
Create `.planning/phases/03-the-ritual-mock/03-02-SUMMARY.md` when done.
</output>
