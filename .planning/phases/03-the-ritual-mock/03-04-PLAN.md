---
phase: 03-the-ritual-mock
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - frontend/src/components/ritual/RitualScreen.tsx
  - frontend/src/components/ritual/Particles.tsx
  - frontend/src/components/ritual/RitualScreen.test.tsx
autonomous: true
requirements: [READ-07, UI-03, UI-01]
must_haves:
  truths:
    - "The ritual prep auto-advances through 3 beats (слышит вопрос → перемешиваются → рядом) over ~3s with a crossfade"
    - "A dimming overlay and a compositor-only particle field run underneath, tinted by the deck accent"
    - "On completion a success haptic fires and the flow advances to the reveal step"
    - "Tap-to-skip becomes active only after the first beat"
    - "Reveal/result card art is preloaded during the ritual so the next screen does not pop in"
  artifacts:
    - path: "frontend/src/components/ritual/RitualScreen.tsx"
      provides: "~3s 3-beat ritual timeline with dimming, particles, completion haptic, tap-to-skip (replaces the 03-01 stub)"
      min_lines: 40
    - path: "frontend/src/components/ritual/Particles.tsx"
      provides: "Fixed-count compositor-only particle field (transform/opacity only)"
      min_lines: 15
  key_links:
    - from: "frontend/src/components/ritual/RitualScreen.tsx"
      to: "frontend/src/lib/telegram.ts"
      via: "haptic.notify('success') on completion"
      pattern: "haptic\\.notify"
    - from: "frontend/src/components/ritual/RitualScreen.tsx"
      to: "frontend/src/stores/selection.ts"
      via: "goTo('reveal') after the final beat"
      pattern: "goTo\\(\"reveal\"\\)"
    - from: "frontend/src/components/ritual/RitualScreen.tsx"
      to: "frontend/src/components/ritual/Particles.tsx"
      via: "renders the particle field underneath the beats"
      pattern: "Particles"
---

<objective>
Deliver the ritual-prep slice (READ-07/D-08) by replacing the 03-01 `RitualScreen` stub with the real screen: an auto-advancing ~3s timeline across three beats («Колода слышит вопрос…» → «Карты перемешиваются…» → «Три знака уже рядом…») with a crossfade, a dimming overlay and a compositor-only deck-tinted particle field underneath, a completion haptic (`notify("success")`), and tap-to-skip enabled only after the first beat. The ritual preloads reveal/result card art during its ~3s so the next screen paints warm (no pop-in, D-01/Pitfall 6).

Purpose: This is the emotional centerpiece — the "shuffling ritual" that makes the reading feel like a ritual, not a page load. It is a self-contained slice: after «Начать расклад», the user watches the 3 beats and lands on the reveal. FlowRoot already routes `step==="ritual"` to this file (03-01), so this plan touches ONLY the ritual component(s) + test — no FlowRoot edit.
Output: The real `RitualScreen.tsx` (replacing the stub) + `Particles.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-the-ritual-mock/03-CONTEXT.md
@.planning/phases/03-the-ritual-mock/03-RESEARCH.md
@.planning/phases/03-the-ritual-mock/03-PATTERNS.md
@.planning/phases/03-the-ritual-mock/03-UI-SPEC.md
@CLAUDE.md
@.planning/phases/03-the-ritual-mock/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compositor-only deck-tinted particle field (D-10/Pitfall 2)</name>
  <files>frontend/src/components/ritual/Particles.tsx</files>
  <read_first>
    - frontend/src/components/DeckCard.tsx — analog for a `motion`/`m.*` element tinted with `var(--deck-accent)` via `color-mix`
    - frontend/src/theme/deckThemes.css — the deck vars (`--deck-accent`, `--deck-soft`) and the root defaults (pre-selection)
    - .planning/phases/03-the-ritual-mock/03-RESEARCH.md — Pattern 5 (Particles: fixed 12–16 `m.div` dots, per-particle y drift + opacity pulse + delay) + Pitfall 2 (banned animated props) + Open Question #2 (cap count, ~12–16)
    - .planning/phases/03-the-ritual-mock/03-UI-SPEC.md — Global animation contract (compositor-only; banned props list; particles capped ~12–16) + Color (accent reserved-for #4: particle field = opacity of an accent layer)
    - .planning/phases/03-the-ritual-mock/03-PATTERNS.md — "components/ritual/Particles.tsx" assignment + Shared 1/2
  </read_first>
  <action>
    Create `frontend/src/components/ritual/Particles.tsx`: a fixed count (~12–16, a module constant) of `m.div` dots positioned across the screen, each looping a small `y` drift and an `opacity` pulse with a per-particle delay/duration offset. Use `m.*` from `motion/react-m` (inside FlowRoot's LazyMotion — Pitfall 5). Tint each dot with `var(--deck-accent)` via `color-mix` (Shared 1). ONLY `transform` (`y`/`scale`) and `opacity` are animated — never `top`/`left`/`width`/`height`/`box-shadow`/`background-position` (D-10/Pitfall 2). Generate the particle set with `Array.from` keyed by a stable index-derived id. Keep the field `aria-hidden` (decorative). Cap the count via the constant rather than introducing any reduced-motion path (D-10 forbids the downgrade).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `Particles.tsx` renders a fixed number (~12–16) of `m.div` dots from `motion/react-m`, each animating only `y`/`scale`/`opacity` (no banned layout/paint props present in the file)
    - Dots are tinted via `var(--deck-accent)` (deck-themed) and the field is `aria-hidden`
    - `cd frontend && npm run build` type-checks
  </acceptance_criteria>
  <done>A reusable, deck-tinted, strictly compositor-only particle field exists, ready to render under the ritual beats.</done>
</task>

<task type="auto">
  <name>Task 2: RitualScreen ~3s 3-beat timeline + dimming + completion haptic + tap-to-skip + art preload (replaces the stub)</name>
  <files>frontend/src/components/ritual/RitualScreen.tsx, frontend/src/components/ritual/RitualScreen.test.tsx</files>
  <read_first>
    - frontend/src/components/ritual/RitualScreen.tsx — the 03-01 STUB to replace (keep the export name FlowRoot imports)
    - frontend/src/components/AuthGate.tsx — analog for the `useEffect` lifecycle discipline (ref guard against StrictMode double-run, `active` flag, return cleanup) — the beat timer mirrors this exactly
    - frontend/src/components/CatalogScreen.tsx — analog for the screen shell `<main>` composition + deck-var background
    - frontend/src/components/ritual/Particles.tsx — the particle field from Task 1 (rendered underneath the beats)
    - frontend/src/lib/telegram.ts — `haptic.notify("success")` for completion [from 03-01]
    - frontend/src/stores/selection.ts — `goTo("reveal")` (and `back` for the in-app back affordance) + the `reading` slot (to know which cards to preload) [from 03-01]
    - frontend/src/reading/copy.ts — the three ritual beat strings + the «Пропустить» skip label [from 03-01]
    - frontend/src/components/CardArtFallback.tsx — the card face (pure CSS/SVG, no network); mount it warm during the ritual so reveal/result paint composited (Pitfall 6)
    - .planning/phases/03-the-ritual-mock/03-RESEARCH.md — Pattern 4 (beat timer effect: setInterval advance, completion haptic, then goTo("reveal"); skip enabled when beat>=1) + Pitfall 6 (preload next-step art during the ritual)
    - .planning/phases/03-the-ritual-mock/03-UI-SPEC.md — "Screen 3 — Ritual prep" + Transition tokens (ritual beat crossfade ~1000ms/beat ~3s, nested AnimatePresence keyed on beat index, completion → haptic.notify("success") then goTo("reveal")) + Copywriting (beats + tap-to-skip «Пропустить» after beat 1)
    - .planning/phases/03-the-ritual-mock/03-PATTERNS.md — "components/ritual/RitualScreen.tsx" assignment (effect-with-cleanup) + Shared 1/3/4
    - .planning/phases/03-the-ritual-mock/03-VALIDATION.md — Manual-Only row for the ritual timeline + haptic (route timing/feel to HUMAN-UAT)
  </read_first>
  <action>
    Replace the 03-01 stub at `frontend/src/components/ritual/RitualScreen.tsx` with the real screen, keeping the SAME exported component name FlowRoot imports. A full-bleed dark canvas (deck-var background, generous `2xl` breathing per UI-SPEC spacing) renders `<Particles/>` and a dimming overlay (animate the overlay `opacity` only). Drive three beats via a `setInterval` advancing a `beat` index over ~3s (BEAT_MS constant ≈ 1000ms × 3 beats), with a nested `<AnimatePresence>` keyed on the beat index for the crossfade of the Display beat headline (strings from copy.ts). Use the AuthGate effect discipline: a `ref` guard so StrictMode's double-mount does not double-run the timeline, an `active` flag, and a cleanup that clears the interval/timeout. On reaching the final beat, fire `haptic.notify("success")` (READ-07 completion) then `goTo("reveal")` after the last beat's dwell. Tap-to-skip: render the «Пропустить» control (copy.ts) and accept a tap-anywhere skip ONLY when `beat >= 1` (D-08) → fire the completion haptic + `goTo("reveal")`. Use `m.*` (LazyMotion). All animation compositor-only.
    Art preload (Pitfall 6/D-10): during the ritual, mount the reveal/result card faces warm — render the `CardArt` fallback for the drawn cards (from the store `reading` slot) in a visually-hidden/offscreen layer (it is pure CSS/SVG, no network) so the reveal's first paint is already composited; if real art URLs ever exist, preload them too. Do not block the timeline on preload.
    Do NOT edit FlowRoot — it already routes `step==="ritual"` to this component.
    Create `RitualScreen.test.tsx` (jsdom; use fake timers): assert (1) the first beat headline renders on mount and the skip control is NOT active before beat 1; (2) advancing fake timers past the beats transitions the store step to "reveal"; (3) after the first beat, a skip tap transitions to "reveal" early; (4) `haptic.notify` is invoked on completion (spy/mock the telegram seam). Keep timing-feel/haptic-on-device as Manual-Only (do not assert 60fps/feel).
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/components/ritual/RitualScreen.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run test -- --run src/components/ritual/RitualScreen.test.tsx` passes: first beat renders, skip inactive before beat 1, timer-advance → step "reveal", post-beat-1 skip → "reveal", completion calls `haptic.notify`
    - `RitualScreen.tsx` renders `<Particles/>` + a dimming overlay (opacity-animated) and uses the AuthGate-style ref-guarded effect with cleanup; all animated values are transform/opacity
    - Beat strings come from `reading/copy.ts`; `frontend/src/flow/FlowRoot.tsx` is NOT in this plan's diff (stub swap is component-local)
    - `cd frontend && npm run build` type-checks and the full suite stays green
  </acceptance_criteria>
  <done>The ~3s 3-beat ritual runs with dimming + particles, fires the completion haptic, advances to reveal, supports tap-to-skip after beat 1, and warms the next screen's art; logic is timer-tested while feel routes to HUMAN-UAT — with no FlowRoot change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram WebView (HapticFeedback) → ritual | Platform haptic API consumed read-only via the telegram.ts seam (no-op outside Telegram) |
| Static ritual beat copy → DOM | Authored strings rendered as React text nodes (no user input on this screen) |
| Timer/effect lifecycle → flow transition | Internal; StrictMode double-mount guarded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-04 | Info-disclosure / policy | ritual beat copy | mitigate | Beat strings sourced from `copy.ts` (SAFE-06 ban-list gated upstream); no fatalistic framing in the "shuffling" beats |
| T-3-05 | Tampering (XSS) | beat headline rendering | mitigate | Rendered as React text nodes; no `dangerouslySetInnerHTML`/`innerHTML` |
| T-3-07 | Denial-of-service (self) | beat timer / StrictMode double-run | mitigate | Ref-guarded effect + cleanup (AuthGate pattern) prevents duplicate intervals/timeouts and a runaway double-advance; no resource leak across mounts |
</threat_model>

<verification>
- `cd frontend && npm run test -- --run src/components/ritual/RitualScreen.test.tsx && npm run build` green
- Particles animate only transform/opacity; RitualScreen effect is ref-guarded with cleanup; completion calls `haptic.notify`
- The ritual stub is replaced (same export name); beats come from copy.ts; FlowRoot untouched
- Manual (HUMAN-UAT per 03-VALIDATION Manual-Only): on device confirm the 3 beats + dimming + particles feel smooth, the completion haptic fires, and tap-to-skip works after beat 1; confirm no pop-in at the ritual→reveal boundary
</verification>

<success_criteria>
- READ-07 satisfied: 3 beats + dimming + particles + completion haptic, ~3s, tap-to-skip after beat 1
- UI-03 micro-animations (shuffle/particles/dimming) via `motion`, compositor-only (D-10); UI-01 dark premium canvas
- Next-step art preloaded so the reveal does not pop in; no FlowRoot edit (zero Wave-2 file conflict)
</success_criteria>

<output>
Create `.planning/phases/03-the-ritual-mock/03-04-SUMMARY.md` when done.
</output>
