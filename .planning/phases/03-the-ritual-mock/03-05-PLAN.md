---
phase: 03-the-ritual-mock
plan: 05
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - frontend/src/components/reveal/FlipCard.tsx
  - frontend/src/components/reveal/RevealScreen.tsx
  - frontend/src/components/reveal/RevealScreen.test.tsx
autonomous: true
requirements: [READ-08, UI-03, UI-01]
must_haves:
  truths:
    - "Each spread position renders one face-down card the user taps to flip with a 3D rotateY animation"
    - "After the first flip, a «Раскрыть все» control appears and staggers-flips the remaining cards"
    - "Each card shows its name, orientation, and a short in-character phrase before the interpretation"
    - "Each flip fires a light haptic on completion"
    - "Advancing from reveal leads to the result screen"
  artifacts:
    - path: "frontend/src/components/reveal/FlipCard.tsx"
      provides: "Single 3D rotateY flip card (compositor-only) with per-flip haptic"
      min_lines: 25
    - path: "frontend/src/components/reveal/RevealScreen.tsx"
      provides: "Per-card flip orchestration + «Раскрыть все» stagger + per-card phrase/interpretation reveal (replaces the 03-01 stub)"
      min_lines: 40
  key_links:
    - from: "frontend/src/components/reveal/FlipCard.tsx"
      to: "frontend/src/lib/telegram.ts"
      via: "haptic.impact('light') on flip completion"
      pattern: "haptic\\.impact"
    - from: "frontend/src/components/reveal/RevealScreen.tsx"
      to: "frontend/src/reading/types.ts"
      via: "renders MockReading.cards[] one FlipCard per card"
      pattern: "MockReading|cards"
    - from: "frontend/src/components/reveal/RevealScreen.tsx"
      to: "frontend/src/stores/selection.ts"
      via: "reads the reading slot + goTo('result')"
      pattern: "goTo\\(\"result\"\\)"
---

<objective>
Deliver the flip-reveal slice (READ-08/D-09) by replacing the 03-01 `RevealScreen` stub with the real screen: one face-down `FlipCard` per spread position that the user taps to flip with a 3D `rotateY` animation (compositor-only, spring 260/26), a «Раскрыть все» control that appears after the first flip and staggers-flips the rest (`delayChildren: stagger(0.12)`), a short in-character phrase before each card's interpretation, a «Прочитать значение» that reveals the interpretation block with staggered text, and a per-flip light haptic. Advancing leads to the result screen.

Purpose: This is the payoff moment — the cards turning over one by one is the core "reveal" interaction. It is a self-contained slice consuming the `MockReading` produced at selection. FlowRoot already routes `step==="reveal"` to this file (03-01), so this plan touches ONLY the reveal component(s) + test — no FlowRoot edit.
Output: `FlipCard.tsx` + the real `RevealScreen.tsx` (replacing the stub).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-the-ritual-mock/03-CONTEXT.md
@.planning/phases/03-the-ritual-mock/03-RESEARCH.md
@.planning/phases/03-the-ritual-mock/03-PATTERNS.md
@.planning/phases/03-the-ritual-mock/03-UI-SPEC.md
@CLAUDE.md
@.planning/phases/03-the-ritual-mock/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FlipCard — 3D rotateY flip, dual faces, per-flip haptic (READ-08/D-09)</name>
  <files>frontend/src/components/reveal/FlipCard.tsx</files>
  <read_first>
    - frontend/src/components/CardArtFallback.tsx — the `CardArt` face to reuse verbatim for the card front (120×192, radius 12, no art seeded → fallback always renders)
    - frontend/src/components/DeckCard.tsx — analog for `motion.button`/`m.*` + accent glow; note the edge-glow must be opacity of an accent layer, not animated box-shadow blur (Pitfall 2)
    - frontend/src/lib/telegram.ts — `haptic.impact("light")` for the per-flip haptic [from 03-01]
    - frontend/src/reading/types.ts — `MockReadingCard` (the card props: name/orientation/positionTitle) [from 03-01]
    - .planning/phases/03-the-ritual-mock/03-RESEARCH.md — Pattern 2 (3D flip: perspective:1000 on parent, m.div animate rotateY 0→180 spring 260/26, both faces absolutely stacked with backfaceVisibility:hidden, face at rotateY(180deg), onAnimationComplete → haptic.impact when flipped)
    - .planning/phases/03-the-ritual-mock/03-UI-SPEC.md — Transition tokens (card flip spring 260/26; perspective:1000; transformStyle preserve-3d; backfaceVisibility hidden; onAnimationComplete → haptic.impact("light")) + Screen 4 (рубашка → position title → flip → name + orientation; frame 120×192 radius 12; edge-glow = accent layer opacity)
    - .planning/phases/03-the-ritual-mock/03-PATTERNS.md — "components/reveal/FlipCard.tsx" assignment + Shared 1/2
  </read_first>
  <action>
    Create `frontend/src/components/reveal/FlipCard.tsx`: props include the card (`MockReadingCard` or its needed subset), a `flipped` boolean, and an `onFlip` callback (controlled by the parent RevealScreen so it can orchestrate "open all"). Parent wrapper gets `perspective: 1000`. The flipping element is an `m.div` (from `motion/react-m`) with `animate={{ rotateY: flipped ? 180 : 0 }}`, spring `stiffness 260, damping 26`, `transformStyle: "preserve-3d"`, sized 120×192. Two absolutely-stacked faces, each `backfaceVisibility: "hidden"`: the back (рубашка) at 0° and the front (the `CardArt` fallback) at `transform: rotateY(180deg)`. `onAnimationComplete` fires `haptic.impact("light")` only when `flipped` (per-flip haptic, UI-03). Edge-glow = animate the `opacity` of a pre-rendered accent layer, never `box-shadow` blur (Pitfall 2). Tap target ≥44px (120×192 satisfies it). Use `m.*` (LazyMotion). Render the card name/orientation as text nodes.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `FlipCard.tsx` uses `m.div` with `animate` `rotateY` (spring 260/26), `perspective:1000` on the parent, two `backfaceVisibility:"hidden"` faces (front at `rotateY(180deg)`), reusing `CardArt` for the front
    - `onAnimationComplete` calls `haptic.impact("light")` when flipped; no animated `box-shadow`/layout props in the file (edge-glow via opacity)
    - `cd frontend && npm run build` type-checks
  </acceptance_criteria>
  <done>A single tap-to-flip 3D card exists, compositor-only, with the card face reused from CardArt and a per-flip light haptic.</done>
</task>

<task type="auto">
  <name>Task 2: RevealScreen — per-card orchestration, «Раскрыть все» stagger, phrase + interpretation reveal (replaces the stub)</name>
  <files>frontend/src/components/reveal/RevealScreen.tsx, frontend/src/components/reveal/RevealScreen.test.tsx</files>
  <read_first>
    - frontend/src/components/reveal/RevealScreen.tsx — the 03-01 STUB to replace (keep the export name FlowRoot imports)
    - frontend/src/components/reveal/FlipCard.tsx — the card from Task 1 (one per spread position)
    - frontend/src/components/DeckCarousel.tsx — analog for map-with-stable-key (key on a stable card id, never index — AnimatePresence exit detection)
    - frontend/src/components/CatalogScreen.tsx — analog for the screen shell `<main>` + `.map` over a collection + deck-var background + in-app structure
    - frontend/src/components/CatalogScreen.test.tsx — analog for the interaction-test style (first action reveals a control, then tapping it changes state); reuse the SAFE-06 ban-list regex if asserting copy
    - frontend/src/reading/types.ts — `MockReading.cards[]` shape [from 03-01]
    - frontend/src/stores/selection.ts — the `reading` slot (the MockReading to render) + `goTo("result")` + `back` [from 03-01]
    - frontend/src/reading/copy.ts — the per-card short-phrase sample bank + «Открыть карту»/«Раскрыть все»/«Прочитать значение» labels [from 03-01]
    - .planning/phases/03-the-ritual-mock/03-RESEARCH.md — Pattern 3 (stagger: `delayChildren: stagger(0.12)` from `motion/react`, container/child variants; cascade not pop) + Anti-Patterns (array index as key)
    - .planning/phases/03-the-ritual-mock/03-UI-SPEC.md — "Screen 4 — Reveal" (per-card: рубашка → position title → tap → name + orientation + short phrase → «Прочитать значение» reveals interpretation with staggered text; «Раскрыть все» after first flip; edge-glow accent opacity) + Transition tokens (staggered «раскрыть все» + staggered text reveal 200–280ms opacity+y)
    - .planning/phases/03-the-ritual-mock/03-PATTERNS.md — "components/reveal/RevealScreen.tsx" assignment + Shared 3/4
    - .planning/phases/03-the-ritual-mock/03-VALIDATION.md — Manual-Only row for flip/«раскрыть все» feel + the READ-08 component test expectation
  </read_first>
  <action>
    Replace the 03-01 stub at `frontend/src/components/reveal/RevealScreen.tsx` with the real screen, keeping the SAME exported component name FlowRoot imports. Read the `MockReading` from the selection store's `reading` slot (consume, do not re-fetch). Render the screen shell (deck-var background) with one `<FlipCard/>` per `reading.cards[]`, keyed by a stable card id (never index — Anti-Pattern). Maintain a per-card flipped set in local state; tapping a card flips it. Before the first flip, show «Открыть карту» guidance; after the FIRST flip, render «Раскрыть все» (copy.ts) which sets all cards flipped via a parent variant using `delayChildren: stagger(0.12)` (`stagger` from `motion/react`) so the rest cascade (Pattern 3), never a simultaneous pop. For each flipped card, show its name + orientation + the short in-character phrase (copy.ts sample bank) and a «Прочитать значение» that reveals the interpretation block with staggered text (`opacity`+`y`, 200–280ms). Provide an advance control → `goTo("result")` and an in-app back (`back()`). Use `m.*` (LazyMotion); all animation compositor-only.
    Do NOT edit FlowRoot — it already routes `step==="reveal"` to this component.
    Create `RevealScreen.test.tsx` (seed the store `reading` slot with a small `MockReading` fixture — 3 cards): assert (1) all cards render face-down and «Раскрыть все» is NOT present initially; (2) tapping the first card flips it and «Раскрыть все» then appears (READ-08/D-09); (3) tapping «Раскрыть все» flips the remaining cards (all become flipped); (4) a flipped card shows its short phrase before the interpretation, and the rendered phrase/name copy contains no banned token (SAFE-06 regex). Mirror CatalogScreen.test.tsx interaction style.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/components/reveal/RevealScreen.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run test -- --run src/components/reveal/RevealScreen.test.tsx` passes: face-down initial + no «Раскрыть все»; first flip reveals «Раскрыть все»; «Раскрыть все» flips the rest; short phrase precedes interpretation; copy passes the SAFE-06 regex
    - `RevealScreen.tsx` keys FlipCards on a stable card id (not index) and uses `delayChildren: stagger(...)` for «Раскрыть все»
    - All labels/phrases from `reading/copy.ts`; `frontend/src/flow/FlowRoot.tsx` is NOT in this plan's diff (stub swap is component-local)
    - `cd frontend && npm run build` type-checks and the full suite stays green
  </acceptance_criteria>
  <done>Cards reveal one-by-one with a 3D flip, «Раскрыть все» staggers the rest after the first, each card shows its phrase before the interpretation, and the user can advance to the result; interactions are tested while flip-feel routes to HUMAN-UAT — with no FlowRoot change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MockReading (client state from selection) → reveal | Ephemeral client data consumed read-only; not from network this phase |
| Telegram WebView (HapticFeedback) → flip | Platform haptic consumed via the telegram.ts seam (no-op outside Telegram) |
| Per-card phrase/interpretation copy → DOM | Authored strings rendered as React text nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-05 | Tampering (XSS) | card name/phrase/interpretation rendering | mitigate | All card text (including any future persisted question echoed here) rendered as React text nodes; no `dangerouslySetInnerHTML`/`innerHTML` |
| T-3-04 | Info-disclosure / policy | per-card phrase + interpretation copy | mitigate | Phrases sourced from `copy.ts`; interpretation text from the mock builder (also brand-safe); RevealScreen test asserts the on-screen copy passes the SAFE-06 ban-list regex |
| T-3-03 | Spoofing/Elevation | reveal consuming the mock reading | accept (contract guard) | Reveal treats the MockReading as display data only; the real draw stays a Phase-4 backend concern (D-14) — no client authority is implied by rendering it |
</threat_model>

<verification>
- `cd frontend && npm run test -- --run src/components/reveal/RevealScreen.test.tsx && npm run build` green
- FlipCard: rotateY spring, dual hidden faces, per-flip `haptic.impact`; RevealScreen: stable keys + `stagger` for «Раскрыть все»
- The reveal stub is replaced (same export name); phrases/labels from copy.ts; FlowRoot untouched
- Manual (HUMAN-UAT per 03-VALIDATION Manual-Only): on device confirm tap-to-flip and «раскрыть все» read as a ritual cascade (not abrupt), and the per-flip haptic fires
</verification>

<success_criteria>
- READ-08 satisfied: staggered flip-reveal, «Открыть карту»/«Раскрыть все» after the first, short phrase before each interpretation, per-flip haptic
- UI-03 (flip/edge-glow/staggered text via `motion`, compositor-only) + UI-01 honored; no FlowRoot edit (zero Wave-2 file conflict)
</success_criteria>

<output>
Create `.planning/phases/03-the-ritual-mock/03-05-SUMMARY.md` when done.
</output>
