---
phase: 03-the-ritual-mock
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - frontend/src/components/CatalogScreen.tsx
  - frontend/src/components/CatalogScreen.test.tsx
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03, HOME-04, HOME-05, HOME-06, HOME-07, UI-01, UI-04, SAFE-06]
must_haves:
  truths:
    - "The user types a free-text question (10–500 chars), with a gentle hint when 1–9 chars and no hint when empty"
    - "An empty question is valid and yields a general reading (no blocking)"
    - "Topic (7), deck, and spread are all selectable on one screen, with the topic recommendation surfaced"
    - "«Начать расклад» is enabled only when topic+deck+spread are chosen; otherwise it is disabled with a gentle hint"
    - "Tapping «Начать расклад» builds the reading via the createReading seam and advances to the ritual step"
    - "The sticky CTA respects the Telegram safe-area inset and is sized against viewportStableHeight (keyboard-safe)"
  artifacts:
    - path: "frontend/src/components/CatalogScreen.tsx"
      provides: "Selection screen extended with question input + sticky «Начать расклад» CTA + createReading wiring"
      contains: "Начать расклад"
  key_links:
    - from: "frontend/src/components/CatalogScreen.tsx"
      to: "frontend/src/reading/createReading.ts"
      via: "«Начать расклад» handler builds the mock reading (the Phase-4 seam)"
      pattern: "createReading"
    - from: "frontend/src/components/CatalogScreen.tsx"
      to: "frontend/src/stores/selection.ts"
      via: "setQuestion + canStart gate + goTo('ritual')"
      pattern: "goTo\\(\"ritual\"\\)|setQuestion"
    - from: "frontend/src/components/CatalogScreen.tsx"
      to: "frontend/src/components/SpreadCard.tsx"
      via: "SpreadCard onSelect → setSpread (HOME-06)"
      pattern: "setSpread"
---

<objective>
Extend the existing `CatalogScreen` into the full selection slice (HOME-01..07): add the free-text question input (10–500, empty-valid, gentle too-short hint), wire the already-present topic chips / deck carousel / recommendation banner / spread list into the flow, wire `SpreadCard.onSelect` to `setSpread` (HOME-06, currently unwired), and add the sticky «Начать расклад» CTA gated on `canStart` (HOME-07) that calls the `createReading()` seam and advances to the ritual. The question is the one untrusted input this phase: length-validated and rendered as text only (V5/T-3-01). The CTA is keyboard-safe via Telegram SDK insets + `viewportStableHeight` (UI-04).

Purpose: This is the heart of the flow — where the user expresses intent and triggers the ritual. It reuses ~all of Phase-2's selection UI verbatim and adds only the question field, the gating CTA, and the single Phase-4 boundary call.
Output: An extended `CatalogScreen.tsx` (the `selection` step) that produces a `MockReading` and transitions to `ritual`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-the-ritual-mock/03-CONTEXT.md
@.planning/phases/03-the-ritual-mock/03-RESEARCH.md
@.planning/phases/03-the-ritual-mock/03-PATTERNS.md
@.planning/phases/03-the-ritual-mock/03-UI-SPEC.md
@CLAUDE.md
@.planning/phases/03-the-ritual-mock/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the question input + wire SpreadCard selection into CatalogScreen (HOME-01/02/06/D-13)</name>
  <files>frontend/src/components/CatalogScreen.tsx</files>
  <read_first>
    - frontend/src/components/CatalogScreen.tsx — the file to EXTEND in place (keep the header → topics → recommendation → decks → spreads scaffold, `useDeckTheme()`, `pb-24` CTA reserve, the four-way isPending/isError/data/empty section pattern)
    - frontend/src/components/SpreadCard.tsx — already accepts `onSelect` (currently unwired); wire it to `setSpread` and pass `selected` state if available
    - frontend/src/components/TopicChip.tsx — reused verbatim (HOME-03), already wired to setTopic
    - frontend/src/stores/selection.ts — `setQuestion`, `question`, the question-validity helper, `setSpread` [from 03-01]
    - frontend/src/reading/copy.ts — the placeholder «О чём спросим колоду?», the too-short hint, the empty-state helper, the gating hint [from 03-01]
    - .planning/phases/03-the-ritual-mock/03-UI-SPEC.md — "Screen 2 — Selection" (question input spec: multiline glass field, Body 16px, `--deck-soft` text, accent focus ring, render as text node) + Copywriting Contract (empty-state + too-short hint rows) + Typography/Color
    - .planning/phases/03-the-ritual-mock/03-PATTERNS.md — "components/CatalogScreen.tsx (MODIFY)" assignment (existing scaffold + what to add) + Shared 1 (glass surface) + Shared 3 (selectors) + Shared 4 (voice)
    - .planning/phases/03-the-ritual-mock/03-RESEARCH.md — Security Domain V5 (question = untrusted text → length-validate + render as text, no dangerouslySetInnerHTML)
  </read_first>
  <action>
    In `CatalogScreen.tsx`, add a question `<textarea>` section near the top of `<main>` (after the atmospheric header, before/around the topic chips per UI-SPEC Screen 2 order). Glass surface using the Shared-1 recipe (deck-var `color-mix` gradient + soft border), Body 16px text in `--deck-soft`, accent `focus-visible` ring. Placeholder «О чём спросим колоду?» from copy.ts. Bind value to the store `question` via `setQuestion` (one-field selectors, Shared 3). Render its value as a React-controlled text node ONLY — never `dangerouslySetInnerHTML`/`innerHTML` (T-3-01/V5).
    Validation UI (D-13): empty → show the optional neutral helper «Можно спросить колоду о чём-то конкретном или сделать общий расклад.» (NOT an error, HOME-02); 1–9 chars → show the soft too-short hint «Попробуй сказать чуть подробнее…» (HOME-01); 10–500 → no hint; >500 → clamp + hint. Derive the hint state from the store's question-validity helper (do not re-implement the rule). The hint is a Label-style line, never blocking.
    Wire `SpreadCard.onSelect` → `setSpread` (HOME-06) and reflect the selected spread (border/active) using the existing recommended/selected styling. Keep TopicChip (HOME-03), DeckCarousel (HOME-05), and the recommendation banner (HOME-04) exactly as they are.
    Do not add the CTA yet (Task 2). Keep `pb-24` reserve intact.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `CatalogScreen.tsx` contains a `<textarea>` bound to the store `question` via `setQuestion`, rendered as a controlled React value (no `dangerouslySetInnerHTML`/`innerHTML` anywhere in the file)
    - The empty-question helper and the 1–9 too-short hint strings come from `reading/copy.ts`; the hint visibility is derived from the store question-validity helper
    - `SpreadCard` is rendered with `onSelect={setSpread}` (HOME-06 wired)
    - `cd frontend && npm run build` type-checks
  </acceptance_criteria>
  <done>The selection screen accepts a free-text question with correct empty/too-short behavior, renders it safely as text, and the spread is now selectable; topic/deck/recommendation reuse is intact.</done>
</task>

<task type="auto">
  <name>Task 2: Sticky «Начать расклад» CTA — canStart gate, keyboard-safe insets, createReading→ritual (HOME-07/UI-01/UI-04)</name>
  <files>frontend/src/components/CatalogScreen.tsx, frontend/src/components/CatalogScreen.test.tsx</files>
  <read_first>
    - frontend/src/components/CatalogScreen.tsx — the file from Task 1 (add the sticky CTA + the start handler)
    - frontend/src/components/CatalogScreen.test.tsx — the existing test to EXTEND (it already uses `renderWithClient` + `vi.stubGlobal("fetch", ...)`; mirror that setup)
    - frontend/src/test/renderWithClient.tsx — the shared harness for components needing TanStack Query
    - frontend/src/stores/selection.ts — `canStart` helper, `setQuestion`, `goTo` [from 03-01]
    - frontend/src/reading/createReading.ts — the seam to call on tap [from 03-01]
    - frontend/src/reading/types.ts — `MockReading` (held in local state / passed to ritual→reveal→result) [from 03-01]
    - frontend/src/lib/telegram.ts — `getSafeAreaInsets`/`getContentSafeAreaInsets` for the CTA bottom padding [from 03-01]
    - frontend/src/api/spreads.ts — `Spread.positions` (passed to createReading as the spread's positions) — already available via the spreads query
    - .planning/phases/03-the-ritual-mock/03-UI-SPEC.md — "Screen 2 — Selection" CTA spec (sticky bottom, accent fill, full-width minus md gutters, padded above `--tg-safe-area-inset-bottom`, sized vs viewportStableHeight) + "Telegram Adaptation" + Color (accent reserved-for #1: the single primary sticky CTA)
    - .planning/phases/03-the-ritual-mock/03-RESEARCH.md — Pitfall 3 (iOS keyboard overlaps bottom-fixed CTA: use viewportStableHeight + `--tg-safe-area-inset-bottom`, never 100vh/env()) + "Selection gating & the Phase-4 seam"
    - .planning/phases/03-the-ritual-mock/03-PATTERNS.md — "components/CatalogScreen.tsx (MODIFY)" CTA notes + Shared 2 (whileTap)
    - .planning/phases/03-the-ritual-mock/03-VALIDATION.md — Per-Task Verification Map (HOME-01/02 + T-3-01 row)
  </read_first>
  <action>
    Add the sticky «Начать расклад» CTA (copy from copy.ts) pinned to the bottom of the selection screen, accent-filled (Color reserved-for #1), full-width minus `md` gutters, with bottom padding equal to the Telegram safe-area inset bottom read via `getSafeAreaInsets()`/`getContentSafeAreaInsets()` (apply as inline padding or a CSS var you set — UI-04; never `env()`/`100vh`). Size the scrollable region against `viewportStableHeight` so the iOS keyboard cannot hide the CTA (Pitfall 3). Use `m.*` (LazyMotion boundary) with `whileTap` scale for tap feedback.
    Gate the CTA `disabled` on `!canStart` (HOME-07: topic+deck+spread). When disabled, show the quiet gating hint «Выбери тему, колоду и расклад — и колода будет готова.» from copy.ts (no dead click, no error).
    On tap (enabled): read `question` (null if empty per D-13), `topic`, `deckSlug`, `spreadSlug`, `reversalsEnabled`, and the chosen spread's `positions` (from the spreads query data) → call `createReading({...})` → store the resulting `MockReading` where the ritual/reveal/result will consume it (component-level state lifted to the flow, or the selection store per the architecture guard — NOT TanStack Query), then `goTo("ritual")`. Use the UI-SPEC error string «Колода замолчала на мгновение…» as the failure-state copy if the seam rejects (try/catch; the mock will not reject, but shape the handler so the Phase-4 swap inherits the error UX).
    Extend `CatalogScreen.test.tsx` (mirror its `renderWithClient` + `vi.stubGlobal("fetch")` setup): (1) CTA disabled when topic/deck/spread incomplete and the gating hint is shown (HOME-07); (2) with topic+deck+spread set in the store, the CTA enables and tapping it transitions the store step to "ritual" (createReading invoked); (3) typing a 1–9-char question shows the too-short hint while an empty question shows no hint (HOME-01/02/D-13); (4) the file contains no `dangerouslySetInnerHTML` (T-3-01 guard — assert via a source check or a render that the textarea value is a text node).
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/components/CatalogScreen.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run test -- --run src/components/CatalogScreen.test.tsx` passes: CTA gating (HOME-07), enabled-tap → `createReading` called + step → "ritual", question hint behavior (empty vs 1–9, HOME-01/02/D-13)
    - The CTA bottom padding derives from `getSafeAreaInsets`/`getContentSafeAreaInsets` (Telegram SDK insets), not `env()`; the scroll region references `viewportStableHeight` (keyboard-safe, Pitfall 3)
    - `CatalogScreen.tsx` contains «Начать расклад» and calls `createReading` then `goTo("ritual")`; no `dangerouslySetInnerHTML` in the file
    - `cd frontend && npm run build` type-checks and the full suite stays green
  </acceptance_criteria>
  <done>«Начать расклад» is correctly gated, keyboard-safe, and on tap produces a MockReading via the seam and advances to the ritual; the untrusted question is length-validated and rendered as text only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User free-text question → app | The single untrusted input this phase: entered in the textarea, held in client state, no network egress (mock) |
| Telegram WebView (insets/viewport) → CTA layout | Platform-owned safe-area + viewport metrics consumed read-only via the telegram.ts seam |
| Spread `positions` (server, Phase-2 Query) → createReading | Trusted server catalog data already validated by Phase-2; consumed, not mirrored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering (XSS) | question `<textarea>` + its value | mitigate | Controlled React value rendered as a text node; NO `dangerouslySetInnerHTML`/`innerHTML`; length-validated 10–500 (empty allowed) via the store helper (HOME-01/D-13). The value is stored client-side only this phase; the Phase-4 persistence path inherits a pre-validated, text-safe string. |
| T-3-03 | Spoofing/Elevation | «Начать расклад» → createReading seam | accept (contract guard) | The mock build is client-only; the seam keeps the real draw + limit check as a Phase-4 BACKEND responsibility. The handler must not encode client-authority over card draw/limits into the contract (D-14). |
| T-3-06 | Denial-of-view (UX) | sticky CTA vs iOS keyboard | mitigate | Size against `viewportStableHeight` + pad by Telegram safe-area inset bottom (Pitfall 3); never `100vh`/`env()` — keeps the CTA reachable when the keyboard is open. |
| T-3-04 | Info-disclosure / policy | hint + CTA + error copy | mitigate | All strings from `copy.ts` (SAFE-06 ban-list gated upstream); error-state copy is the soft in-character «Колода замолчала…», no stacktrace. |
</threat_model>

<verification>
- `cd frontend && npm run test -- --run src/components/CatalogScreen.test.tsx && npm run build` green
- CTA gating (HOME-07), question validation (HOME-01/02/D-13), and the createReading→ritual transition are covered by tests
- No `dangerouslySetInnerHTML` in `CatalogScreen.tsx`; CTA uses Telegram SDK insets + viewportStableHeight
- Manual (HUMAN-UAT per 03-VALIDATION Manual-Only): on iOS Telegram, focus the question field and confirm «Начать расклад» stays reachable; confirm the deck theme is reflected on the screen
</verification>

<success_criteria>
- HOME-01..07 all satisfied: question entry (10–500/empty-valid/too-short hint), 7 topics, deck carousel, recommendation, spread pick, gated start CTA
- UI-01 (sticky bottom CTA, premium-dark) + UI-04 (SDK insets, keyboard-safe) honored
- The single Phase-4 seam (`createReading`) is the only path from selection to ritual; the untrusted question is text-safe and length-validated
</success_criteria>

<output>
Create `.planning/phases/03-the-ritual-mock/03-03-SUMMARY.md` when done.
</output>
