---
phase: 05-history-profile
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/integration/_history_helpers.py
  - backend/tests/integration/test_readings_list.py
  - backend/tests/integration/test_readings_detail.py
  - backend/tests/integration/test_readings_delete.py
  - backend/tests/integration/test_history_personalization_gate.py
  - backend/tests/integration/test_settings_patch.py
  - backend/tests/integration/test_readings_auth.py
  - backend/tests/integration/test_me.py
autonomous: true
requirements: [HIST-01, HIST-02, HIST-03, HIST-04, HIST-05, HIST-06, PROF-01, PROF-02]
must_haves:
  truths:
    - "The shared create_completed_reading helper persists a COMPLETED reading + reading_cards for a user without re-driving the full POST"
    - "Each Wave-0 test file is collectable (imports resolve, no syntax errors) and either xfails or skips cleanly until the endpoint exists"
    - "The 4 load-bearing invariant tests exist as named, runnable test functions"
  artifacts:
    - path: "backend/tests/integration/_history_helpers.py"
      provides: "create_completed_reading(session, user, *, deck_slug, spread_slug, question) integration helper reusing the fakes-backed ReadingService"
      contains: "create_completed_reading"
    - path: "backend/tests/integration/test_readings_list.py"
      provides: "HIST-01/02/06 red tests (auto_save, shape_and_order, last_ten_cap)"
      contains: "last_ten_cap"
    - path: "backend/tests/integration/test_readings_detail.py"
      provides: "HIST-03 immutability red tests"
      contains: "immutable"
    - path: "backend/tests/integration/test_readings_delete.py"
      provides: "HIST-04 soft-delete + restore + excluded_from_list red tests"
      contains: "excluded_from_list"
    - path: "backend/tests/integration/test_history_personalization_gate.py"
      provides: "HIST-05 closed-gate regression lock"
      contains: "history_personalization"
    - path: "backend/tests/integration/test_settings_patch.py"
      provides: "PROF-02 partial-update + round-trip + reversals_source red tests"
      contains: "reversals_source"
  key_links:
    - from: "backend/tests/integration/_history_helpers.py"
      to: "app.services.reading.ReadingService"
      via: "fakes-backed service (FakeSafety+FakeLLM) creating a completed reading"
      pattern: "ReadingService\\("
    - from: "backend/tests/integration/_history_helpers.py"
      to: "tests.integration.test_readings_flow"
      via: "reuse _output_for_indices / _spread_position_indices"
      pattern: "_output_for_indices"
---

<objective>
Build the Wave-0 test substrate for Phase 5 — the shared `create_completed_reading` integration helper plus every backend red-test file the Wave-1/2 endpoint slices implement against. This is the interface-first foundation that mirrors the Phase-4 Wave-0 pattern (`04-01`): the tests describe the exact endpoint contracts (list shape, immutable detail, soft-delete+restore, the closed consent gate, settings round-trip) before any endpoint exists, so each later slice has a precise, runnable target.

Purpose: lock the 4 load-bearing invariants (VALIDATION.md) and the IDOR/cross-user tests as named test functions BEFORE implementation, so the endpoint slices cannot silently drift from the contract. The shared helper means list/detail/delete tests do not each re-drive the full `POST /api/readings`.
Output: one helper module + 5 new test files + 2 extended test files, all collectable, all red/skipping until their endpoint lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-history-profile/05-CONTEXT.md
@.planning/phases/05-history-profile/05-RESEARCH.md
@.planning/phases/05-history-profile/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared create_completed_reading helper</name>
  <files>backend/tests/integration/_history_helpers.py</files>
  <read_first>
    - backend/tests/integration/conftest.py (the `auth_session` savepoint fixture, `FakeLLM`/`FakeSafety`, `seeded_catalog`, `_ensure_deck_cards`)
    - backend/tests/integration/test_readings_flow.py (the `_make_user`, `_output_for_indices`, `_spread_position_indices`, `_limits` helpers to REUSE — do not duplicate their logic)
    - backend/app/services/reading.py (ReadingService constructor seam: `safety=`/`llm=` inject FakeSafety/FakeLLM; `create_reading(session, user, ReadingCreate)` returns a completed ReadingOut)
    - backend/app/schemas/reading.py (`ReadingCreate` field names: question/topic/deck_slug/spread_slug/reversals_enabled)
    - backend/app/models/reading.py (Reading: status, deleted_at, user_id; ReadingStatus.COMPLETED)
  </read_first>
  <action>
    Create `backend/tests/integration/_history_helpers.py` (a non-`test_`-prefixed module so pytest does not collect it as tests). Export an async `create_completed_reading(session, user, *, deck_slug="classic_arcana", spread_slug="three_keys", topic="choice", question="Что мне поможет принять важное решение на этой неделе?")` that builds a fakes-backed `ReadingService(safety=FakeSafety(), llm=FakeLLM(_output_for_indices(indices)))` — where `indices` comes from the imported `_spread_position_indices(session, spread_slug)` — calls `create_reading`, and returns the resulting `ReadingOut` (carrying `reading_id`). Import `_output_for_indices` / `_spread_position_indices` from `tests.integration.test_readings_flow` and `FakeLLM` / `FakeSafety` from `tests.integration.conftest` (single source of truth — never re-declare the fakes or the output builder). Also export an async `make_user_with_limits(session, *, free_weekly_limit=100, free_used=0)` thin wrapper that delegates to the existing `_make_user` style but allows a high `free_weekly_limit` so a single test can create 12 completed readings without hitting the Phase-4 quota gate (the helper inserts the User + a UserLimits row). Document at the top WHY the high limit (the last-10 cap test in test_readings_list needs ≥12 completed readings; the Phase-4 quota check would otherwise block the 4th). Use only `select()` 2.0 style + `AsyncSession`; no lazy loads.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/integration/_history_helpers.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/integration/_history_helpers.py` exists and imports `_output_for_indices`, `_spread_position_indices` from `tests.integration.test_readings_flow` and `FakeLLM`, `FakeSafety` from `tests.integration.conftest` (no re-declaration).
    - It exports `create_completed_reading` and `make_user_with_limits`.
    - `cd backend && uv run pytest tests/integration/_history_helpers.py --collect-only -q` reports 0 collected tests and no import/collection error (or skips cleanly when Postgres is down — the module itself must import without error).
    - The module file is NOT prefixed `test_` (pytest must not collect it as a test module).
  </acceptance_criteria>
  <done>The shared helper module imports cleanly, reuses the existing fakes + output builder, and exposes the two creation helpers the list/detail/delete tests consume.</done>
</task>

<task type="auto">
  <name>Task 2: List + last-10 cap + cross-user (IDOR) red tests</name>
  <files>backend/tests/integration/test_readings_list.py, backend/tests/integration/test_readings_auth.py</files>
  <read_first>
    - backend/tests/integration/test_readings_auth.py (the `auth_client` + `_bearer` + `fake_service` override pattern to mirror for the HTTP-level cross-user test)
    - backend/tests/integration/test_readings_flow.py (`_make_user`, `_limits`)
    - backend/tests/integration/_history_helpers.py (Task 1: `create_completed_reading`, `make_user_with_limits`)
    - .planning/phases/05-history-profile/05-VALIDATION.md ("The 4 load-bearing invariants" + the Per-Task Verification Map rows for HIST-01/02/06 and the cross-user IDOR row)
    - backend/app/models/reading.py (Reading.status, deleted_at — the COMPLETED-only + soft-delete filters the list must honor, A5)
  </read_first>
  <action>
    Create `backend/tests/integration/test_readings_list.py` with three red tests against the (not-yet-existing) `GET /api/readings`, using `auth_client` + `_bearer` (HTTP) where a Bearer is asserted, and the `auth_session` + `create_completed_reading` helper to seed rows:
    (a) `test_auto_save_appears_in_list` (HIST-01) — create one completed reading for the JWT user, then `GET /api/readings` returns ≥1 item whose `reading_id` matches;
    (b) `test_shape_and_order` (HIST-02) — create 2+ completed readings; assert each list item carries ONLY the light fields (`reading_id`, `created_at`, `question`, `deck_name`, `spread_name`, `card_thumbnails`, `summary_short`) and NO full per-card `interpretation`/`cards` array, newest-first;
    (c) `test_last_ten_cap` (HIST-06, load-bearing invariant #2) — via `make_user_with_limits(free_weekly_limit=100)` create 12 completed readings, assert `GET /api/readings` returns exactly 10, AND the 11th/12th oldest are STILL fetchable directly by id (assert the rows exist in the DB by id — data retained, not pruned).
    Then EXTEND `backend/tests/integration/test_readings_auth.py` with `test_cross_user_detail_and_delete_404` (IDOR) — user A creates a reading; user B (a second Bearer) requesting `GET /api/readings/{a_id}` and `DELETE /api/readings/{a_id}` each get 404 (not 403, not 200). Mark all four as expected-red until their endpoint exists using `pytest.mark.xfail(reason="GET/DELETE /api/readings pending 05-02/05-04", strict=False)` so the suite stays green now and flips to xpass when implemented. Reuse `seeded_catalog` so they skip cleanly without Postgres.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/integration/test_readings_list.py tests/integration/test_readings_auth.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_readings_list.py` defines `test_auto_save_appears_in_list`, `test_shape_and_order`, `test_last_ten_cap`.
    - `test_readings_auth.py` gains `test_cross_user_detail_and_delete_404` asserting 404 for a non-owned id on BOTH detail and delete.
    - `test_last_ten_cap` asserts exactly 10 returned AND the older rows remain fetchable by id (retain-not-prune).
    - `--collect-only` succeeds with no import error; the new tests are marked `xfail(strict=False)` so a full `uv run pytest -q` stays green.
    - The tests reuse `create_completed_reading` / `make_user_with_limits` (no re-driving the full POST inline).
  </acceptance_criteria>
  <done>The list, last-10-cap, and cross-user IDOR contracts exist as named xfail tests that will xpass once 05-02/05-04 land.</done>
</task>

<task type="auto">
  <name>Task 3: Detail, delete/restore, consent-gate, settings, and /me red tests</name>
  <files>backend/tests/integration/test_readings_detail.py, backend/tests/integration/test_readings_delete.py, backend/tests/integration/test_history_personalization_gate.py, backend/tests/integration/test_settings_patch.py, backend/tests/integration/test_me.py</files>
  <read_first>
    - backend/tests/integration/test_me.py (the existing `_auth_and_get_token` + assertions to EXTEND for PROF-01)
    - backend/tests/integration/test_readings_flow.py (`_make_user`, `_limits`, `_output_for_indices`)
    - backend/tests/integration/_history_helpers.py (Task 1)
    - backend/app/services/prompt_engine.py (confirm `build(...)` has NO history parameter — the HIST-05 gate the test LOCKS; read the signature only)
    - backend/app/schemas/auth.py (`SettingsOut` fields reversals_enabled/allow_history_personalization/onboarding_completed — the PATCH round-trip targets)
    - backend/app/models/user.py (the 3 boolean flags + defaults: reversals_enabled=True, allow_history_personalization=False, onboarding_completed=False)
    - .planning/phases/05-history-profile/05-VALIDATION.md (invariants #1/#3/#4 + the PROF-01/02 + HIST-03/04/05 rows)
  </read_first>
  <action>
    Create four red-test files + extend `test_me.py`:
    (1) `test_readings_detail.py` — `test_detail_immutable` (HIST-03): create a completed reading, `GET /api/readings/{id}` twice, assert both responses are byte-identical (same cards/summary, no regeneration) and match the created `ReadingOut`; `test_detail_deleted_404` (HIST-04/Pitfall 4): a soft-deleted reading → `GET /{id}` returns 404.
    (2) `test_readings_delete.py` — `test_soft_delete_sets_deleted_at` (HIST-04): `DELETE /{id}` → the `readings` row's `deleted_at` is non-null (assert via the session) and the row is gone from `GET /api/readings`; `test_excluded_from_list` (load-bearing invariant #1): create 2, delete 1, list omits exactly the deleted one; `test_restore_unsets_deleted_at` (D-03): after delete, `POST /api/readings/{id}/restore` nulls `deleted_at` and the row reappears in the list.
    (3) `test_history_personalization_gate.py` — `test_prompt_has_no_history_even_with_flag_on` (HIST-05, load-bearing invariant #4): set the user's `allow_history_personalization=True` AND create a prior completed reading, then assemble a prompt via `PromptEngine.build(...)` for a new reading and assert the built `(system, user)` text contains NONE of the prior reading's question/summary text; assert (by signature introspection or by call) that `build` accepts no `history`/`history_context` keyword. The test LOCKS the closed gate so a future v2 author cannot wire history in silently.
    (4) `test_settings_patch.py` — `test_partial_patch_round_trip` (PROF-02, invariant #3): `PATCH /api/me/settings` with only `{allow_history_personalization: true}` flips that flag and LEAVES reversals_enabled/onboarding_completed untouched; a follow-up `GET /api/me` reflects it; `test_patch_user_from_jwt_not_body` (V4/Spoofing): a forged `user_id` in the PATCH body is ignored (only the JWT user is mutated); `test_reversals_source` (D-09): after `PATCH reversals_enabled=false`, a subsequently created reading uses upright-only orientation (reversals sourced from the persisted flag — assert all drawn orientations are `upright`).
    Extend `test_me.py` with `test_me_returns_settings_block` (PROF-01): `GET /api/me` returns `settings` with all three flags AND `user` with name/photo fields, asserting NO schema change is required (the block already exists).
    Mark every test whose endpoint does not yet exist `xfail(strict=False, reason="<endpoint> pending 05-02/03/04")`; the `test_me_returns_settings_block` assertion that the settings block already exists should PASS today (not xfail) since `GET /api/me` is live. Reuse `seeded_catalog`/`auth_session` so all skip cleanly without Postgres.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/integration/test_readings_detail.py tests/integration/test_readings_delete.py tests/integration/test_history_personalization_gate.py tests/integration/test_settings_patch.py tests/integration/test_me.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - All four new files + the extended `test_me.py` collect with no import error.
    - `test_excluded_from_list`, `test_partial_patch_round_trip`, `test_prompt_has_no_history_even_with_flag_on`, `test_detail_immutable`, `test_restore_unsets_deleted_at`, `test_reversals_source` each exist as named functions.
    - `test_me_returns_settings_block` is NOT xfail and PASSES against the live `GET /api/me` when Postgres is up (it only asserts the existing `{user, limits, settings}` shape).
    - `cd backend && uv run pytest -q` overall stays green (new endpoint-dependent tests are xfail).
  </acceptance_criteria>
  <done>Detail-immutability, soft-delete/restore/exclusion, the closed consent gate, the settings round-trip, and the /me settings-block contracts all exist as named tests; the only non-xfail addition (/me settings block) passes against the live endpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → DB | The helper writes real rows in the savepoint-isolated `auth_session`; nothing persists between tests (outer rollback). No production trust boundary is crossed — this plan adds tests only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-IDOR | Information Disclosure / Elevation | cross-user `GET`/`DELETE /api/readings/{id}` | mitigate | `test_cross_user_detail_and_delete_404` (Task 2) asserts a non-owned id → 404 on both verbs; this is the executable lock the 05-02/05-04 endpoints must satisfy. |
| T-05-CONSENT | Information Disclosure / Privacy | PromptEngine + history | mitigate | `test_prompt_has_no_history_even_with_flag_on` (Task 3) locks HIST-05: the prompt carries no prior-reading text even with the flag ON. |
| T-05-SPOOF | Spoofing / Elevation | `PATCH /api/me/settings` body | mitigate | `test_patch_user_from_jwt_not_body` (Task 3) asserts a forged body `user_id` is ignored. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No installs — pytest already pinned (`backend/pyproject.toml`); RESEARCH Package Legitimacy Audit: "Phase 5 installs ZERO new packages". No legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd backend && uv run pytest -q` collects all new tests and stays green (endpoint-dependent tests xfail; `_history_helpers.py` is not collected as tests; the `/me` settings-block test passes).
- The 4 load-bearing invariants each map to a named test: invariant #1 → `test_excluded_from_list`; #2 → `test_last_ten_cap`; #3 → `test_partial_patch_round_trip`; #4 → `test_prompt_has_no_history_even_with_flag_on`.
- The IDOR cross-user test exists in the extended `test_readings_auth.py`.
</verification>

<success_criteria>
- The shared helper + 5 new test files + 2 extended files exist and are collectable.
- Every Wave-1/2 endpoint slice has a precise red target (xfail → xpass on implementation).
- No new dependencies; full backend suite stays green.
</success_criteria>

<output>
Create `.planning/phases/05-history-profile/05-01-SUMMARY.md` when done.
</output>
