---
phase: 05-history-profile
plan: 05
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - frontend/src/flow/steps.ts
  - frontend/src/stores/selection.ts
  - frontend/src/flow/FlowRoot.tsx
  - frontend/src/api/readings.ts
  - frontend/src/hooks/useReadings.ts
  - frontend/src/reading/copy.ts
  - frontend/src/reading/copy.test.ts
  - frontend/src/components/history/HistoryScreen.tsx
  - frontend/src/components/profile/ProfileScreen.tsx
  - frontend/src/components/history/HistoryScreen.test.tsx
  - frontend/src/components/CatalogScreen.tsx
  - frontend/src/components/result/ResultScreen.tsx
autonomous: true
requirements: [HIST-01, HIST-02, HIST-06]
must_haves:
  truths:
    - "The Home (selection) header has History + Profile icon entry points (D-10); the result-screen «история» action is un-stubbed and routes to History (D-10)"
    - "The History screen lists completed readings (date/question/deck/spread/thumbnails/short summary) newest-first via GET /api/readings (HIST-02)"
    - "An empty history shows the §9.6 «Пока здесь тихо…» copy"
    - "In-app back from History → Home (D-11); the step-machine carries history/profile/readingDetail destinations"
    - "All new history/profile/settings copy passes the BANNED_BRAND_TOKENS scan (SAFE-06)"
  artifacts:
    - path: "frontend/src/flow/steps.ts"
      provides: "Step union extended with history | profile | readingDetail"
      contains: "readingDetail"
    - path: "frontend/src/hooks/useReadings.ts"
      provides: "useReadingsList TanStack Query hook (stable key)"
      contains: "useReadingsList"
    - path: "frontend/src/components/history/HistoryScreen.tsx"
      provides: "History list screen (list-item cards + empty state + back-to-Home)"
      contains: "HistoryScreen"
    - path: "frontend/src/components/profile/ProfileScreen.tsx"
      provides: "ProfileScreen stub registered in FlowRoot (body filled by 05-07)"
      contains: "ProfileScreen"
  key_links:
    - from: "frontend/src/components/history/HistoryScreen.tsx"
      to: "GET /api/readings"
      via: "useReadingsList → fetchReadings → apiFetch"
      pattern: "useReadingsList"
    - from: "frontend/src/flow/FlowRoot.tsx"
      to: "HistoryScreen / ProfileScreen"
      via: "SCREENS registry maps the new step tokens (stub seam — bodies in 05-06/07)"
      pattern: "history:|profile:|readingDetail:"
    - from: "frontend/src/components/CatalogScreen.tsx"
      to: "goTo('history') / goTo('profile')"
      via: "header icons (D-10)"
      pattern: "goTo\\(\"history\"\\)"
---

<objective>
Establish the frontend history/profile foundation AND ship the first FE vertical slice (the History list). This plan owns ALL shared frontend seams so 05-06 (detail+delete) and 05-07 (profile/settings) touch only their own screen files — mirroring the Phase-3 FlowRoot-stub pattern (the foundation registers stub screens; later plans replace only their own file, removing the multi-writer conflict). After this plan a real user can: tap a History icon on Home, see their past completed readings (or the empty-state copy), and tap back to return Home.

Purpose: HIST-01/02/06 surfaced in the UI; the navigation spine (step union + FlowRoot registration + Home header icons + un-stubbed «история» CTA) that 05-06/07 build on. The `api/readings.ts` + `useReadings.ts` modules + ALL new brand-safe copy strings land here so downstream plans import without editing shared files.
Output: extended step-machine, `useReadingsList` hook, `fetchReadings` client wrapper, the History screen, the Profile/detail-mode screen STUBS registered in FlowRoot, Home header icons, the un-stubbed result «история» action, and all new copy (SAFE-06-scanned).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-history-profile/05-CONTEXT.md
@.planning/phases/05-history-profile/05-RESEARCH.md
@frontend/src/flow/steps.ts
@frontend/src/flow/FlowRoot.tsx
@frontend/src/stores/selection.ts
@frontend/src/hooks/useDecks.ts
@frontend/src/api/client.ts
@frontend/src/reading/copy.ts
@frontend/src/components/result/ResultScreen.tsx
@frontend/src/components/CatalogScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nav spine — Step union, selection store, FlowRoot registration, screen stubs, copy</name>
  <files>frontend/src/flow/steps.ts, frontend/src/stores/selection.ts, frontend/src/flow/FlowRoot.tsx, frontend/src/components/history/HistoryScreen.tsx, frontend/src/components/profile/ProfileScreen.tsx, frontend/src/reading/copy.ts, frontend/src/reading/copy.test.ts</files>
  <read_first>
    - frontend/src/flow/steps.ts (the `Step` string-literal union + `STEP_ORDER` + `next` — extend, do NOT convert to an enum, TS rule)
    - frontend/src/stores/selection.ts (the `step`/`history`/`goTo`/`back` machine; add a `detailReadingId` slot for which reading the detail screen renders — RESEARCH component-responsibilities table)
    - frontend/src/flow/FlowRoot.tsx (the `SCREENS: Record<Step, () => Element>` registry + the `m.*` from `motion/react-m` + `LazyMotion` rule — register the 3 new screens here, mirroring how Phase-3 registered Wave-2 stubs; the onboarding initial-step gate `useEffect` is owned by 05-07 — do NOT touch it here)
    - frontend/src/reading/copy.ts (the `BANNED_BRAND_TOKENS` + the existing section layout; add the new history/profile/settings strings here — the SINGLE SAFE-06 source)
    - frontend/src/reading/copy.test.ts (the module-wide scan that auto-covers new strings; extend only if a targeted assertion is wanted)
    - .planning/phases/05-history-profile/05-RESEARCH.md (Code Example: extending the Step union; the §9.6 empty-state copy «Пока здесь тихо…»; the personalization explainer guidance — describe as «история раскладов»/«колода помнит», NEVER the mechanism, Pitfall 6)
  </read_first>
  <action>
    (a) `frontend/src/flow/steps.ts` — extend `Step` to `... | "history" | "profile" | "readingDetail"`. Leave `STEP_ORDER` as the linear ritual order (these new steps are NOT part of the forward ritual flow; they are navigated via `goTo`, not `next`) — add a comment that history/profile/readingDetail are off-flow destinations reached by `goTo`/back, never `next`.
    (b) `frontend/src/stores/selection.ts` — add `detailReadingId: string | null` to the state + a `setDetailReadingId(id: string | null)` setter (mutates ONLY that slot). Default `null`. This is which reading `ResultScreen` renders in detail mode (05-06 reads it).
    (c) `frontend/src/components/history/HistoryScreen.tsx`, `frontend/src/components/profile/ProfileScreen.tsx` — create both. `HistoryScreen` gets its real body in Task 2. `ProfileScreen` is a minimal STUB this plan (a brand-safe placeholder returning an element; its real body lands in 05-07) — same pattern Phase-3 used for Wave-2 screen stubs. The `readingDetail` step reuses the existing `ResultScreen` (no new file) — register it in FlowRoot pointing at `ResultScreen` (05-06 extends ResultScreen to read `detailReadingId`).
    (d) `frontend/src/flow/FlowRoot.tsx` — add the three entries to `SCREENS`: `history: HistoryScreen`, `profile: ProfileScreen`, `readingDetail: ResultScreen`. Import the new screens. Do NOT touch the existing five entries or the onboarding initial-step gate (05-07 owns the onboarding-gate change).
    (e) `frontend/src/reading/copy.ts` — add a new section with: the §9.6 empty-history copy (`HISTORY_EMPTY = "Пока здесь тихо. Первый расклад появится в истории, когда колода даст ответ."`), `HISTORY_HEADER`, `HISTORY_BACK` (reuse `NAV_BACK` if suitable), an optional `HISTORY_LAST_TEN_NOTE` (the D-04 «последние 10» note, brand-safe), `HISTORY_GENERAL` (the no-question list label), `HISTORY_DELETED_NOTICE` + `HISTORY_DELETE_UNDO` (consumed by 05-06's snackbar), plus the profile/settings strings 05-07 will consume (`PROFILE_HEADER`, `SETTINGS_REVERSALS_LABEL`, `SETTINGS_PERSONALIZATION_LABEL`, `SETTINGS_PERSONALIZATION_EXPLAINER` — phrased as «история раскладов»/«колода помнит», a short privacy note, NEVER «AI/нейросеть/модель/ИИ»). Centralizing them here means 05-06/07 import without re-editing copy.ts.
    Run the existing `copy.test.ts` module-wide scan (it auto-covers the new strings); add a targeted assertion only if helpful.
  </action>
  <verify>
    <automated>cd frontend && node node_modules/vitest/vitest.mjs run src/reading/copy.test.ts src/stores/selection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `Step` includes `history | profile | readingDetail`; `selection.ts` exposes `detailReadingId` + `setDetailReadingId`.
    - `FlowRoot.SCREENS` maps all three new tokens (`readingDetail` → `ResultScreen`); the existing five entries + onboarding gate are unchanged.
    - `HistoryScreen.tsx` + `ProfileScreen.tsx` exist (ProfileScreen as a brand-safe stub); both render an element (FlowRoot registry is `() => Element`, never null).
    - All new copy lives in `copy.ts`; `copy.test.ts` (module-wide `BANNED_BRAND_TOKENS` scan) passes — zero banned tokens including the personalization explainer.
    - `selection.test.ts` stays green (the new slot does not break existing machine tests).
  </acceptance_criteria>
  <done>The step-machine carries the three off-flow destinations, FlowRoot registers all three screens (stubs where bodies come later), and all new brand-safe copy is centralized and SAFE-06-clean.</done>
</task>

<task type="auto">
  <name>Task 2: useReadingsList hook + fetchReadings client + History list screen + Home/Result entry points</name>
  <files>frontend/src/api/readings.ts, frontend/src/hooks/useReadings.ts, frontend/src/components/history/HistoryScreen.tsx, frontend/src/components/history/HistoryScreen.test.tsx, frontend/src/components/CatalogScreen.tsx, frontend/src/components/result/ResultScreen.tsx</files>
  <read_first>
    - frontend/src/hooks/useDecks.ts + useSpreads.ts (the `useQuery` shape: `queryKey`, `queryFn`, `staleTime`; the v5 conventions — mirror for `useReadingsList`)
    - frontend/src/api/client.ts (`apiFetch` Bearer seam — every fetch goes through it)
    - frontend/src/api/decks.ts (the `fetch*` wrapper shape: typed response, ok-check; mirror for `fetchReadings`)
    - frontend/src/components/CardArtFallback.tsx (the `CardArt` thumbnail component for the list-item miniatures — reuse, A2: empty thumbnails fall back to the CSS/SVG art)
    - frontend/src/components/result/ResultScreen.tsx (the glass-card list-item visual language + `getSafeAreaInsets` + the `formatDate` DD.MM.YYYY helper to mirror; the «история»/«сохранить» disabled «скоро» stubs — un-stub the «история» one only, D-10; «сохранить карточку» stays «скоро»)
    - frontend/src/components/CatalogScreen.tsx (where to add the atmospheric-header History/Profile icons — D-10; `goTo` is already imported)
    - frontend/src/reading/copy.ts (Task 1: HISTORY_EMPTY/HISTORY_HEADER/etc.)
    - frontend/src/test/renderWithClient.tsx (the fresh-QueryClient test wrapper for the component test)
    - .planning/phases/05-history-profile/05-RESEARCH.md (Pattern 1 = the list-item fields; Pitfall 5 = pick a stable list query key `["readings","list"]` and reuse it everywhere; D-01 = single stable key, no filters)
    - .planning/phases/05-history-profile/05-VALIDATION.md (the HIST-02 component test row: renders date/question/deck/spread/thumbnails/short summary; empty state = §9.6 copy)
  </read_first>
  <action>
    (a) `frontend/src/api/readings.ts` — add a typed `ReadingListItem` interface (snake_case, mirroring the backend `ReadingListItemOut`: reading_id, created_at, question, deck_name, spread_name, card_thumbnails, summary_short) + `fetchReadings(): Promise<ReadingListItem[]>` calling `apiFetch("/api/readings?limit=10")` and parsing the JSON (ok-check → throw on non-OK, mirroring `createReading`'s reject style). Keep types explicit (no `any`, TS rule).
    (b) `frontend/src/hooks/useReadings.ts` — add `useReadingsList()` = `useQuery({ queryKey: ["readings","list"], queryFn: fetchReadings, staleTime: 30_000 })`. Use the SINGLE stable key `["readings","list"]` (no filter params, D-01) so 05-06's delete mutation can target it (Pitfall 5).
    (c) `frontend/src/components/history/HistoryScreen.tsx` — render the list: header (`HISTORY_HEADER`), an in-app back affordance → `back()` (D-11, returns Home), and the list. Loading → an in-character line; error → an in-character line; `data.length === 0` → the §9.6 `HISTORY_EMPTY` copy; otherwise map each `ReadingListItem` to a glass list-item card showing date (DD.MM.YYYY), question (or `HISTORY_GENERAL` when null/empty), deck_name, spread_name, the `card_thumbnails` via `CardArt` (empty → CSS fallback), and `summary_short`. Tapping a list-item card calls `setDetailReadingId(item.reading_id)` then `goTo("readingDetail")` (the detail render is wired in 05-06; setting the id + navigating is correct now). Optionally surface the `HISTORY_LAST_TEN_NOTE`. Reuse the `GLASS`/`formatDate` visual patterns from ResultScreen (keep it cohesive). All strings come from copy.ts (SAFE-06). No `dangerouslySetInnerHTML` — the question is a text node.
    (d) `frontend/src/components/CatalogScreen.tsx` — add two icon buttons to the top of the selection surface (an atmospheric header row, D-10): a History icon → `goTo("history")` and a Profile icon → `goTo("profile")`. Keep them unobtrusive (small, accent-tinted) so the immersive feel is preserved; the ritual/reveal/result stay chrome-free (do NOT add a bottom tab bar).
    (e) `frontend/src/components/result/ResultScreen.tsx` — un-stub the «история» action (D-10): replace the disabled «скоро» history button with an enabled button that calls `goTo("history")` (drop the `RESULT_SOON_BADGE` on the history one only; «сохранить карточку» stays a «скоро» stub — that is Phase 8). Read `goTo` from the selection store.
    (f) `frontend/src/components/history/HistoryScreen.test.tsx` — a vitest component test using `renderWithClient`, mocking `apiFetch`/`fetchReadings`: (i) renders a list of items with their fields; (ii) the empty case renders `HISTORY_EMPTY`. Mirror `ResultScreen.test.tsx` patterns.
  </action>
  <verify>
    <automated>cd frontend && node node_modules/vitest/vitest.mjs run src/components/history/HistoryScreen.test.tsx src/reading/copy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `fetchReadings` calls `apiFetch("/api/readings?limit=10")` and returns typed `ReadingListItem[]`; `useReadingsList` uses the stable key `["readings","list"]`.
    - `HistoryScreen` renders date/question/deck/spread/thumbnails/short-summary per item, the §9.6 empty-state copy when empty, and a back affordance that returns Home (`back()`); a list-item tap sets `detailReadingId` + `goTo("readingDetail")`.
    - `CatalogScreen` shows History + Profile header icons wired to `goTo("history")`/`goTo("profile")`; no bottom tab bar is added.
    - `ResultScreen` «история» is enabled and calls `goTo("history")`; «сохранить карточку» remains a «скоро» stub.
    - `HistoryScreen.test.tsx` passes (list render + empty state); `copy.test.ts` passes.
  </acceptance_criteria>
  <done>The History list is live end-to-end (icon → list → back), backed by `useReadingsList` against `GET /api/readings`, with the empty-state copy and the un-stubbed result «история» entry; the component test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mini App → API | All list fetches go through `apiFetch` (Bearer JWT, the single attachment seam); the list response is rendered as text nodes (no raw-HTML sink). |
| user copy → UI | New strings pass the SAFE-06 `BANNED_BRAND_TOKENS` scan (brand-voice boundary). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-XSS | Tampering / Information Disclosure | history list-item rendering | mitigate | The question + summary render as React text nodes (no `dangerouslySetInnerHTML`); mirrors the ResultScreen T-3-01 control. |
| T-05-BRAND | Information Disclosure (brand) | new history/profile copy | mitigate | All new strings live in `copy.ts`; `copy.test.ts` module-wide `BANNED_BRAND_TOKENS` scan blocks «AI/нейросеть/модель/ИИ» (Pitfall 6, the personalization explainer especially). |
| T-05-AUTH | Spoofing | list fetch identity | mitigate | The JWT rides via `apiFetch`; the client never sends a user_id — the server scopes by the token (05-02). |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No installs (RESEARCH Package Legitimacy Audit: zero new packages; no toast/router lib added — `motion` + the step-machine only). |
</threat_model>

<verification>
- `cd frontend && node node_modules/vitest/vitest.mjs run` — full frontend suite green (HistoryScreen + copy + selection tests pass; baseline 80 unaffected).
- The step-machine carries history/profile/readingDetail; FlowRoot registers all three; CatalogScreen + ResultScreen expose the History entry points (D-10).
- HUMAN-UAT note: the live History list feel (touch, atmospheric header icons, fade) needs a running stack + Telegram — a deploy-time smoke like Phases 2–4. The automated component test runs headless; `autonomous` stays true.
</verification>

<success_criteria>
- A real user can open History from Home, browse completed readings (or the empty state), and go back Home (D-10/D-11).
- All shared FE seams (step union, FlowRoot registration, api/hooks/copy) are in place for 05-06/07.
- HIST-01/02/06 surfaced in the UI; no new dependencies.
</success_criteria>

<output>
Create `.planning/phases/05-history-profile/05-05-SUMMARY.md` when done.
</output>
