---
phase: 05-history-profile
plan: 03
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - backend/app/schemas/auth.py
  - backend/app/api/users.py
  - backend/app/services/prompt_engine.py
autonomous: true
requirements: [PROF-01, PROF-02, HIST-05]
must_haves:
  truths:
    - "PATCH /api/me/settings partial-updates each of reversals_enabled / allow_history_personalization / onboarding_completed; an omitted field is left untouched (PROF-02)"
    - "The settings change round-trips: GET /api/me reflects it (PROF-02)"
    - "GET /api/me returns {user, limits, settings} unchanged — name/photo + settings, NO schema change (PROF-01)"
    - "The PATCH is JWT-scoped — a forged body user_id is ignored"
    - "Phase 5 captures consent + keeps the gate closed ONLY — no history-injection path is built (D-06; the feature is v2/ENG-02). History is never assembled into the §18 prompt regardless of allow_history_personalization (HIST-05 — closed gate locked by test + comment)"
  artifacts:
    - path: "backend/app/schemas/auth.py"
      provides: "SettingsPatch all-optional partial-update request schema"
      contains: "class SettingsPatch"
    - path: "backend/app/api/users.py"
      provides: "PATCH /api/me/settings handler (exclude_unset, user from JWT)"
      contains: "patch_settings"
    - path: "backend/app/services/prompt_engine.py"
      provides: "HIST-05/D-06 human-visible lock comment near build() — history is intentionally not a parameter"
      contains: "HIST-05"
  key_links:
    - from: "backend/app/api/users.py"
      to: "User row (current_user)"
      via: "setattr only the model_dump(exclude_unset=True) keys, commit, return SettingsOut"
      pattern: "exclude_unset"
    - from: "backend/app/api/users.py"
      to: "get_current_user"
      via: "the patched user is the JWT user, never the body"
      pattern: "get_current_user"
---

<objective>
Add the settings write path and lock the privacy gate: `PATCH /api/me/settings` (partial update of the three boolean flags) + the HIST-05 regression lock that guarantees history never reaches the §18 prompt. `GET /api/me` needs NO schema change (it already returns `{user, limits, settings}` — PROF-01 is satisfied; the count/subscription fields are present but the UI hides them, D-08). (The D-09 "reversals sourced from the persisted flag" change lives in 05-04, which owns `reading.py` — it depends on this PATCH endpoint to set the flag.)

Purpose: PROF-01/02 + HIST-05. Per D-06, Phase 5's scope is **consent flag + gate ONLY** — it persists the consent and guarantees history is never fed into the §18 prompt unless the flag is ON (which it currently never is, because no history path exists); the actual "feed history into the prompt / повторный анализ" feature is **v2 (ENG-02)** and is NOT built here. HIST-05 is therefore a NEGATIVE requirement already satisfied by construction (PromptEngine has no history parameter); this plan persists the consent flag and adds the test + a human-visible comment that LOCK the closed gate so a future v2/ENG-02 author cannot wire history in without the gate.
Output: `SettingsPatch` schema + `patch_settings` handler + the HIST-05/D-06 lock-comment; the settings round-trip + the HIST-05 gate test turn green; zero history-injection plumbing is added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-history-profile/05-CONTEXT.md
@.planning/phases/05-history-profile/05-RESEARCH.md
@backend/app/api/users.py
@backend/app/schemas/auth.py
@backend/app/models/user.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SettingsPatch schema + PATCH /api/me/settings (partial, JWT-scoped)</name>
  <files>backend/app/schemas/auth.py, backend/app/api/users.py</files>
  <read_first>
    - backend/app/api/users.py (the existing `GET /api/me` handler + `get_current_user`/`get_session` deps + `MeResponse`/`SettingsOut` usage to mirror)
    - backend/app/schemas/auth.py (`SettingsOut` = the 3 flags response shape to REUSE; the `from_attributes=True` + `Field` conventions; add `SettingsPatch` here)
    - backend/app/models/user.py (the 3 boolean columns + defaults: reversals_enabled=True, allow_history_personalization=False, onboarding_completed=False)
    - .planning/phases/05-history-profile/05-RESEARCH.md (Pattern 4 + the `PATCH /api/me/settings` code example: all-optional schema, `model_dump(exclude_unset=True)`, return `SettingsOut`)
    - backend/tests/integration/test_settings_patch.py (05-01 red tests: test_partial_patch_round_trip, test_patch_user_from_jwt_not_body)
    - backend/tests/integration/test_me.py (test_me_returns_settings_block — must keep passing)
  </read_first>
  <behavior>
    - PATCH with `{allow_history_personalization: true}` flips ONLY that flag; reversals_enabled + onboarding_completed are unchanged.
    - The response is the full SettingsOut (all 3 flags) reflecting the new state; a follow-up GET /api/me shows the same.
    - A forged `user_id` in the body has no effect — only the JWT user's row is mutated.
    - An empty PATCH body (no fields) is a no-op that returns the current settings (200).
  </behavior>
  <action>
    In `backend/app/schemas/auth.py` add `SettingsPatch(BaseModel)` with all-optional fields: `reversals_enabled: bool | None = None`, `allow_history_personalization: bool | None = None`, `onboarding_completed: bool | None = None` (each a one-line RU `Field(description=...)`). Add it to `__all__`. Do NOT add a `user_id` field (identity is never in the body). The `allow_history_personalization` flag is the D-05/D-06 consent toggle (default OFF — the model default); persisting it is Phase 5's entire HIST-05 obligation on the write side.
    In `backend/app/api/users.py` add `@router.patch("/me/settings", response_model=SettingsOut)` `async def patch_settings(body: SettingsPatch, current_user: User = Depends(get_current_user), session: AsyncSession = Depends(get_session)) -> SettingsOut`: iterate `body.model_dump(exclude_unset=True).items()` and `setattr(current_user, field, value)` for ONLY the provided keys (so an omitted flag is untouched — the partial-update invariant); `await session.commit()`; return `SettingsOut.model_validate(current_user)`. The patched user is `current_user` (the JWT `sub`) — the body is never consulted for identity. Mirror the existing `get_me` import/dep style.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/integration/test_settings_patch.py -k "round_trip or jwt_not_body" tests/integration/test_me.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `SettingsPatch` exists with three all-optional booleans, no `user_id`, in `__all__`.
    - `PATCH /api/me/settings` applies only `model_dump(exclude_unset=True)` keys, commits, returns `SettingsOut`.
    - `test_partial_patch_round_trip` passes: a single-field patch flips that flag and leaves the other two untouched; `GET /api/me` reflects it.
    - `test_patch_user_from_jwt_not_body` passes: a forged body `user_id` changes nothing but the JWT user.
    - `test_me.py` stays green (no `MeResponse`/`GET /api/me` schema change — PROF-01).
  </acceptance_criteria>
  <done>The partial, JWT-scoped settings write path exists and round-trips, persisting the D-05/D-06 consent flag; `GET /api/me` is unchanged; the partial-update invariant (#3) holds.</done>
</task>

<task type="auto">
  <name>Task 2: HIST-05/D-06 closed-gate lock (comment + regression test green)</name>
  <files>backend/app/services/prompt_engine.py</files>
  <read_first>
    - backend/app/services/prompt_engine.py (confirm `build(...)` signature has NO history/history_context parameter and reads no prior readings — the gate this task LOCKS; read the signature + the body's data sources only)
    - backend/app/models/user.py (User.allow_history_personalization — default False; the flag that, even when True, must NOT pull history into the prompt — D-06)
    - .planning/phases/05-history-profile/05-RESEARCH.md (Pattern 5 = HIST-05 closed by absence — do NOT add any history_context path; adding plumbing is the footgun HIST-05/D-06 guards against)
    - backend/tests/integration/test_history_personalization_gate.py (05-01 red test: test_prompt_has_no_history_even_with_flag_on)
  </read_first>
  <action>
    Make `test_prompt_has_no_history_even_with_flag_on` pass WITHOUT adding any history path (RESEARCH Pattern 5 + D-06 — Phase 5 is consent-flag-and-gate ONLY; the gate is closed by absence). The test asserts `PromptEngine.build(...)` carries no prior-reading text even with `allow_history_personalization=True`, and that `build` accepts no `history`/`history_context` keyword. Since `build`'s signature already lacks any history parameter, this task is the human-visible LOCK: add a short, clearly-worded comment in `backend/app/services/prompt_engine.py` directly above the `build` method documenting: "HIST-05 / D-06 (negative requirement): history is intentionally NOT a parameter here — Phase 5 is consent-flag-and-gate ONLY; the consent gate is closed by ABSENCE. Do NOT add a `history`/`history_context` parameter, a prior-reading fetch, or a history prompt branch; the actual history-personalization feature is v2 (ENG-02) and must reintroduce the consent gate when it does." Do NOT add any parameter, fetch, or branch. Confirm by running the test that the assembled prompt contains none of the prior reading's question/summary text.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/integration/test_history_personalization_gate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `PromptEngine.build` still has NO history/history_context parameter (grep: no `history` kwarg added); a clearly-worded HIST-05/D-06 comment marks the closed gate directly above `build`.
    - `test_prompt_has_no_history_even_with_flag_on` passes (the assembled prompt contains no prior-reading content with the flag ON).
    - No prior-reading fetch or history prompt branch was introduced anywhere in the prompt assembly (D-06 — the history feature stays v2/ENG-02).
  </acceptance_criteria>
  <done>The HIST-05/D-06 closed gate is locked: no history path added, the regression test is green, and a human-visible comment guards the seam against a future accidental wire-in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | The Bearer JWT is the only identity; the `SettingsPatch` body is a closed 3-boolean schema (no user_id, no free text). |
| prompt assembly → LLM | The §18 prompt is the privacy boundary for HIST-05/D-06: history must never cross it without consent (today it never crosses at all). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SPOOF | Spoofing / Elevation | `PATCH /api/me/settings` body | mitigate | The patched user is `get_current_user` (JWT `sub`); `SettingsPatch` has no `user_id`. `test_patch_user_from_jwt_not_body` locks it. |
| T-05-CONSENT | Information Disclosure / Privacy | PromptEngine + history (HIST-05/D-06) | mitigate | `PromptEngine.build` has no history parameter; the consent flag defaults OFF (D-05); `test_prompt_has_no_history_even_with_flag_on` + the lock-comment guard the closed gate. No history-injection plumbing is added (D-06 — feature is v2/ENG-02). |
| T-05-VAL | Tampering | unexpected PATCH keys | mitigate | Pydantic `SettingsPatch` is a closed schema; `exclude_unset` writes only provided known booleans (`select()` parameterization elsewhere). |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No installs (RESEARCH Package Legitimacy Audit: zero new packages). |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/integration/test_settings_patch.py -k "round_trip or jwt_not_body" tests/integration/test_history_personalization_gate.py tests/integration/test_me.py -x -q` — all green.
- Grep confirms `PromptEngine.build` gained no `history` parameter (HIST-05/D-06 closed by absence) and a HIST-05/D-06 lock-comment is present.
- `cd backend && uv run pytest -q` — full suite green.
</verification>

<success_criteria>
- `PATCH /api/me/settings` partial-updates the three flags, round-trips, and is JWT-scoped; `GET /api/me` unchanged (PROF-01/02).
- HIST-05/D-06 closed gate locked by test + comment; no history path added (the feature stays v2/ENG-02).
- No new dependencies.
</success_criteria>

<output>
Create `.planning/phases/05-history-profile/05-03-SUMMARY.md` when done.
</output>
