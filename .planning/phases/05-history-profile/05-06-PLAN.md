---
phase: 05-history-profile
plan: 06
type: execute
wave: 4
depends_on: ["05-04", "05-05"]
files_modified:
  - frontend/src/api/readings.ts
  - frontend/src/hooks/useReadings.ts
  - frontend/src/components/result/ResultScreen.tsx
  - frontend/src/components/history/HistoryScreen.tsx
  - frontend/src/components/history/UndoSnackbar.tsx
  - frontend/src/components/result/ResultScreen.test.tsx
  - frontend/src/components/history/HistoryScreen.delete.test.tsx
  - frontend/src/reading/createReading.ts
autonomous: true
requirements: [HIST-03, HIST-04]
must_haves:
  truths:
    - "Tapping a history item reopens it in ResultScreen detail mode with a light opacity fade-in (no re-ritual / no reveal chrome) — content matches what was originally generated (HIST-03)"
    - "Reopen detail → back returns to History (D-11)"
    - "Swiping a list card optimistically removes it + DELETEs it; an undo snackbar (~5s) restores it via the restore endpoint (HIST-04 / D-03)"
    - "If undo is not tapped within ~5s the optimistic removal stands (server already soft-deleted)"
  artifacts:
    - path: "frontend/src/components/result/ResultScreen.tsx"
      provides: "detail mode — renders the reading from useReadingDetail(detailReadingId) with a fade-in; back → History"
      contains: "detailReadingId"
    - path: "frontend/src/hooks/useReadings.ts"
      provides: "useReadingDetail / useDeleteReading / useRestoreReading (optimistic, stable key)"
      contains: "useDeleteReading"
    - path: "frontend/src/components/history/UndoSnackbar.tsx"
      provides: "motion AnimatePresence snackbar with a 5s timer (no toast lib)"
      contains: "UndoSnackbar"
  key_links:
    - from: "frontend/src/components/result/ResultScreen.tsx"
      to: "GET /api/readings/{id}"
      via: "useReadingDetail(detailReadingId) when step === readingDetail"
      pattern: "useReadingDetail"
    - from: "frontend/src/hooks/useReadings.ts"
      to: "DELETE /api/readings/{id} + POST /api/readings/{id}/restore"
      via: "useDeleteReading optimistic onMutate/onError + useRestoreReading; key ['readings','list']"
      pattern: "\\[\"readings\", \"list\"\\]"
    - from: "frontend/src/components/history/HistoryScreen.tsx"
      to: "UndoSnackbar"
      via: "swipe → optimistic remove → snackbar → undo restores"
      pattern: "UndoSnackbar"
---

<objective>
Make history readings reopenable and deletable. Reopen reuses `ResultScreen` in DETAIL mode — when `step === "readingDetail"` it renders the reading fetched by `useReadingDetail(detailReadingId)` (the immutable `GET /api/readings/{id}`) with a light opacity fade-in (cards stagger in), NOT a re-ritual/re-reveal (D-02). Delete is swipe-to-delete on the list card + an undo snackbar (~5s) backed by the soft-delete + restore endpoints, with a TanStack Query optimistic cache update (D-03). No toast library, no router — `motion` + the step-machine only.

Purpose: HIST-03 (immutable reopen) + HIST-04 (soft-delete + undo). This completes the history experience: browse (05-05) → reopen → delete/undo. All new copy strings already live in copy.ts (05-05) — this plan imports them and does NOT edit copy.ts.
Output: `useReadingDetail`/`useDeleteReading`/`useRestoreReading` hooks, `ResultScreen` detail mode, the swipe + `UndoSnackbar`, and the detail + delete/undo component tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-history-profile/05-CONTEXT.md
@.planning/phases/05-history-profile/05-RESEARCH.md
@frontend/src/components/result/ResultScreen.tsx
@frontend/src/hooks/useReadings.ts
@frontend/src/api/readings.ts
@frontend/src/stores/selection.ts
@frontend/src/reading/createReading.ts
@frontend/src/reading/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: useReadingDetail + ResultScreen detail mode (immutable reopen, fade-in)</name>
  <files>frontend/src/api/readings.ts, frontend/src/hooks/useReadings.ts, frontend/src/components/result/ResultScreen.tsx, frontend/src/reading/createReading.ts, frontend/src/components/result/ResultScreen.test.tsx</files>
  <read_first>
    - frontend/src/components/result/ResultScreen.tsx (05-05 left the «история» action enabled; it reads `reading` from the store. Detail mode must instead read the reading fetched by id when `step === "readingDetail"`, mapping the `ReadingOut` → the `MockReading` shape it already renders; the existing `summaryContainer`/`summaryItem` opacity-stagger is the D-02 fade-in to extend to the card list)
    - frontend/src/stores/selection.ts (05-05: `detailReadingId` + `step` + `back`; detail → back returns to History because History pushed onto `history[]` before `goTo("readingDetail")`)
    - frontend/src/reading/createReading.ts (REUSE its `mapCard`/`mapSummary` `ReadingOut → MockReading` mapping logic — extract/share it so detail does not re-implement the snake→camel mapping; the `ReadingOutResponse` type already exists in reading/types.ts)
    - frontend/src/reading/types.ts (`ReadingOutResponse` / `MockReading` shapes)
    - frontend/src/api/readings.ts (05-05: `ReadingListItem` + `fetchReadings` — add the detail fetcher here)
    - frontend/src/api/client.ts (`apiFetch`)
    - frontend/src/hooks/useDecks.ts (the `useQuery` enabled-gating style for `enabled: Boolean(id)`)
    - .planning/phases/05-history-profile/05-RESEARCH.md (Pattern 2 = immutable detail; the D-02 fade-in via the existing `opacity`-stagger variant)
    - .planning/phases/05-history-profile/05-VALIDATION.md (HIST-03 component row: reopen → ResultScreen detail with fade-in, no ritual chrome)
  </read_first>
  <action>
    (a) `frontend/src/api/readings.ts` — add `fetchReadingDetail(id: string): Promise<ReadingOutResponse>` calling `apiFetch(\`/api/readings/${id}\`)` (ok-check → throw on non-OK). To avoid duplicating the snake→camel mapping, export a shared `mapReadingOutToMock(...)` (extracted from `createReading.ts`'s `mapCard`/`mapSummary`); refactor `createReading.ts` to import this shared mapper so there is ONE mapping (DRY). Detail's meta fields (question/topic/deckSlug/spreadSlug/createdAt) that the §14.5 ReadingOut body may not carry are sourced from the History list item the user tapped (the list item already has question/deck_name/spread_name/created_at) — pass those through so the displayed meta stays consistent with the list; the per-card + summary content comes from the immutable detail response.
    (b) `frontend/src/hooks/useReadings.ts` — add `useReadingDetail(id: string | null)` = `useQuery({ queryKey: ["readings","detail", id], queryFn: () => fetchReadingDetail(id!), enabled: Boolean(id), staleTime: Infinity })` (immutable → never restale; HIST-03 "no regeneration" is server-guaranteed).
    (c) `frontend/src/components/result/ResultScreen.tsx` — branch on `step`: when `step === "readingDetail"`, read `detailReadingId` from the store, call `useReadingDetail(detailReadingId)`, and render the fetched reading (mapped to `MockReading`) instead of the store `reading`; show a loading line while pending. Apply the light fade-in: wrap the card list (and reuse the summary stagger) in the existing `opacity`-stagger variant so reopen is a gentle fade, NOT the flip-reveal. Replace the live-flow sticky actions in detail mode with a single back affordance → `back()` (D-11, returns to History) — detail must be chrome-free of «Ещё расклад»/«сохранить»/«история» (those belong to the live result). When `step === "result"` (live flow) the screen behaves exactly as before (05-05). Keep everything as text nodes (no raw HTML).
    (d) `frontend/src/components/result/ResultScreen.test.tsx` — extend with a detail-mode render test: with `step === "readingDetail"` + a mocked `useReadingDetail`, reopen renders the fetched reading + a back affordance + NONE of the live-flow CTAs.
  </action>
  <verify>
    <automated>cd frontend && node node_modules/vitest/vitest.mjs run src/components/result/ResultScreen.test.tsx src/reading/createReading.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `useReadingDetail(id)` queries `GET /api/readings/{id}` keyed `["readings","detail",id]`, gated on `id`, `staleTime: Infinity`.
    - The `ReadingOut → MockReading` mapping is shared (one `mapReadingOutToMock`); `createReading.ts` imports it (no duplicate mapper) and its existing test stays green.
    - In `readingDetail` mode `ResultScreen` renders the fetched (immutable) reading with the opacity fade-in and a back → History affordance, and NONE of the live-flow CTAs; in `result` mode it is unchanged.
    - `ResultScreen.test.tsx` passes (extended with the detail-mode render test).
  </acceptance_criteria>
  <done>Reopening a history item renders the immutable stored reading in ResultScreen with a light fade-in and back-to-History, reusing the shared mapper; the live result path + createReading test are untouched/green.</done>
</task>

<task type="auto">
  <name>Task 2: Swipe-to-delete + UndoSnackbar (optimistic delete + restore)</name>
  <files>frontend/src/api/readings.ts, frontend/src/hooks/useReadings.ts, frontend/src/components/history/UndoSnackbar.tsx, frontend/src/components/history/HistoryScreen.tsx, frontend/src/components/history/HistoryScreen.delete.test.tsx</files>
  <read_first>
    - frontend/src/hooks/useReadings.ts (Task 1 + 05-05: the stable list key `["readings","list"]` — the delete/restore mutations MUST target it, Pitfall 5)
    - frontend/src/components/history/HistoryScreen.tsx (05-05: the list-item cards — add the swipe gesture + render the snackbar)
    - frontend/src/api/client.ts (`apiFetch` — DELETE + POST restore)
    - frontend/src/api/readings.ts (add `deleteReading(id)` + `restoreReading(id)` wrappers)
    - frontend/src/components/result/ResultScreen.tsx (the `motion` `m.*` + `AnimatePresence` usage to mirror for the snackbar + the swipe drag — `motion/react-m` under LazyMotion; `whileTap`/`drag` patterns)
    - frontend/src/reading/copy.ts (05-05 centralized `HISTORY_DELETED_NOTICE` + `HISTORY_DELETE_UNDO` — IMPORT them; do NOT edit copy.ts here)
    - frontend/src/test/renderWithClient.tsx (the fresh-QueryClient wrapper for the delete test)
    - .planning/phases/05-history-profile/05-RESEARCH.md (Pattern 3 = the canonical TanStack v5 optimistic `onMutate`/`onError`/snapshot-rollback recipe; Open Question 1 = `POST /api/readings/{id}/restore`; the snackbar = `motion` `AnimatePresence` + `setTimeout`, NO toast lib)
    - .planning/phases/05-history-profile/05-VALIDATION.md (HIST-04 component row: swipe → optimistic remove + undo snackbar; undo restores the cached item)
  </read_first>
  <action>
    (a) `frontend/src/api/readings.ts` — add `deleteReading(id: string): Promise<void>` (`apiFetch(\`/api/readings/${id}\`, { method: "DELETE" })`, throw on non-2xx) and `restoreReading(id: string): Promise<void>` (`apiFetch(\`/api/readings/${id}/restore\`, { method: "POST" })`, throw on non-2xx).
    (b) `frontend/src/hooks/useReadings.ts` — add `useDeleteReading()` as a TanStack v5 optimistic mutation (Pattern 3): `onMutate(id)` → `cancelQueries({ queryKey: ["readings","list"] })`, snapshot `getQueryData(["readings","list"])`, optimistically `setQueryData` to remove the item by `reading_id`, return `{ prev, removed }` (keep the removed item + its index for restore); `onError` → roll back to `prev`. Add `useRestoreReading()` whose `mutationFn` calls `restoreReading(id)` and, on success, re-inserts the item into the cached list (or invalidates `["readings","list"]` to refetch). Both target the SINGLE key `["readings","list"]` (Pitfall 5).
    (c) `frontend/src/components/history/UndoSnackbar.tsx` — a `motion` `AnimatePresence` element with a 5s `setTimeout` (cleared on unmount/undo) showing `HISTORY_DELETED_NOTICE` + an «Отменить» button (`HISTORY_DELETE_UNDO`). Props: `onUndo()`, `onDismiss()`. Build it with the existing `motion` primitives — do NOT add a toast library (RESEARCH Don't-Hand-Roll).
    (d) `frontend/src/components/history/HistoryScreen.tsx` — add a horizontal swipe gesture to each list-item card (motion `drag="x"` with a threshold, or a small swipe-detector): on swipe past the threshold call `useDeleteReading().mutate(id)` and show the `UndoSnackbar`. «Отменить» calls `useRestoreReading().mutate(id)` and dismisses the snackbar; after ~5s with no undo the snackbar auto-dismisses and the optimistic removal stands (the server already soft-deleted). Only one snackbar at a time. Keep the swipe accessible (the tap-to-open still works; swipe is the delete affordance).
    (e) `frontend/src/components/history/HistoryScreen.delete.test.tsx` — a vitest test (renderWithClient + mocked `deleteReading`/`restoreReading`): (i) triggering delete optimistically removes the item from the rendered list and shows the snackbar; (ii) «Отменить» restores the item (re-rendered) and calls `restoreReading`. Trigger the delete via the component's delete handler (a simulated swipe or the exposed handler) — assert on the cache/DOM outcome, not the gesture mechanics.
  </action>
  <verify>
    <automated>cd frontend && node node_modules/vitest/vitest.mjs run src/components/history/HistoryScreen.delete.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `deleteReading`/`restoreReading` call `DELETE /api/readings/{id}` and `POST /api/readings/{id}/restore` via `apiFetch`.
    - `useDeleteReading` is an optimistic mutation (snapshot + `setQueryData` remove + `onError` rollback) on the key `["readings","list"]`; `useRestoreReading` re-inserts/invalidates the same key.
    - `UndoSnackbar` is a `motion` `AnimatePresence` element with a 5s `setTimeout` and an «Отменить» action; NO toast library is imported (grep: package.json unchanged).
    - Swiping a list card optimistically removes it + shows the snackbar; «Отменить» restores it; auto-dismiss after ~5s leaves the removal.
    - `HistoryScreen.delete.test.tsx` passes (optimistic remove + undo restore).
  </acceptance_criteria>
  <done>Swipe-to-delete with an undo snackbar works against the soft-delete + restore endpoints via an optimistic TanStack cache update, built with motion only; the delete/undo test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mini App → API | Detail/delete/restore fetches go through `apiFetch` (Bearer); the server is authoritative for ownership + soft-delete state (the optimistic cache is UI-only). |
| optimistic cache → server | The 5s undo window is a pure client timer; the server already soft-deleted on `DELETE`, so the optimistic removal and the server state agree (A3). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-IDOR-UI | Information Disclosure / Elevation | reopen/delete another user's id | mitigate | The id comes from the user's own list (05-05, server-scoped); the server re-checks ownership and 404s a non-owned id (05-04). The UI cannot reach another user's reading. |
| T-05-XSS | Tampering / Information Disclosure | detail render | mitigate | The detail reading renders as React text nodes (the same ResultScreen control, no `dangerouslySetInnerHTML`). |
| T-05-STALE | Tampering | optimistic cache mismatch | mitigate | The delete/restore mutations target the single stable key `["readings","list"]` (Pitfall 5); rollback on error restores the snapshot. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No installs — the snackbar uses `motion` (already pinned); no toast/router lib (RESEARCH Don't-Hand-Roll + Package Legitimacy Audit). |
</threat_model>

<verification>
- `cd frontend && node node_modules/vitest/vitest.mjs run` — full frontend suite green (ResultScreen detail + HistoryScreen.delete + createReading + the rest).
- Grep confirms no toast/router dependency was added (package.json unchanged); the snackbar is `motion` + `setTimeout`.
- HUMAN-UAT note: the swipe-delete/undo + reopen fade FEEL needs a live device + Telegram (touch + animation), a deploy-time smoke like Phases 2–4. The automated component tests run headless; `autonomous` stays true.
</verification>

<success_criteria>
- Reopen renders the immutable stored reading with a fade-in and back-to-History (HIST-03).
- Swipe-to-delete + 5s undo works via soft-delete + restore with an optimistic cache update (HIST-04 / D-03).
- No new dependencies (motion + step-machine only).
</success_criteria>

<output>
Create `.planning/phases/05-history-profile/05-06-SUMMARY.md` when done.
</output>
