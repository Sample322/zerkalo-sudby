---
phase: 05-history-profile
plan: 02
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - backend/app/schemas/reading.py
  - backend/app/services/reading.py
  - backend/app/api/readings.py
autonomous: true
requirements: [HIST-01, HIST-02, HIST-06]
must_haves:
  truths:
    - "A completed reading auto-saved by Phase 4 appears in GET /api/readings (HIST-01)"
    - "GET /api/readings returns light items (date/question/deck/spread/thumbnails/short summary), newest-first, NO full interpretation (HIST-02)"
    - "A free user with 12 completed readings sees only the last 10; older rows stay in the DB and remain fetchable by id (HIST-06)"
    - "Soft-deleted and non-COMPLETED readings are excluded from the list"
    - "The list is scoped to the JWT user — never the request body"
  artifacts:
    - path: "backend/app/schemas/reading.py"
      provides: "ReadingListItemOut light list-item schema"
      contains: "class ReadingListItemOut"
    - path: "backend/app/services/reading.py"
      provides: "ReadingService.list_readings(session, user, *, limit, offset) with the FREE_HISTORY_CAP seam"
      contains: "FREE_HISTORY_CAP"
    - path: "backend/app/api/readings.py"
      provides: "GET /api/readings thin router (Bearer, user from JWT)"
      contains: "list_readings"
  key_links:
    - from: "backend/app/api/readings.py"
      to: "ReadingService.list_readings"
      via: "thin router delegates; user from get_current_user"
      pattern: "list_readings"
    - from: "backend/app/services/reading.py"
      to: "Reading (user_id, deleted_at, status)"
      via: "select().where(user_id == user.id, deleted_at IS NULL, status == COMPLETED).order_by(created_at desc)"
      pattern: "deleted_at\\.is_\\(None\\)"
---

<objective>
Surface the auto-saved reading history: add `GET /api/readings` — a paginated, user-scoped, newest-first, soft-delete-excluding, COMPLETED-only, last-10-capped list returning a LIGHT list-item schema (date/question/deck/spread/card-thumbnails/short-summary, NOT the full interpretation). This is the first vertical slice: after it, the History screen (05-05) has real data to render and HIST-01 (auto-save, already satisfied at the DB level by Phase 4) becomes observable.

Purpose: HIST-01/02/06. The list is the entry point to the whole history experience; correctness here (user-scoping, soft-delete exclusion, the last-10 retain-not-prune cap) protects privacy (IDOR) and the Phase-6/7 subscription reveal.
Output: `ReadingListItemOut` schema + `ReadingService.list_readings` + the `GET /api/readings` router, with the `FREE_HISTORY_CAP=10` constant as the explicit Phase-6 seam.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-history-profile/05-CONTEXT.md
@.planning/phases/05-history-profile/05-RESEARCH.md
@backend/app/services/reading.py
@backend/app/api/readings.py
@backend/app/schemas/reading.py
@backend/app/models/reading.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ReadingListItemOut light schema + list_readings service (last-10 cap)</name>
  <files>backend/app/schemas/reading.py, backend/app/services/reading.py</files>
  <read_first>
    - backend/app/schemas/reading.py (the `from_attributes=True` + RU `Field(description=...)` conventions; `ReadingOut` is the HEAVY schema — do NOT reuse it for the list)
    - backend/app/services/reading.py (the `select()` 2.0 style, `_resolve_deck`/`_get_limits` query patterns, `selectinload`, the `ReadingInputError` convention; the explicit `select(ReadingCard)` style in `_persist_output` — REUSE this, do NOT add a `Reading.cards` relationship, Pitfall 1)
    - backend/app/models/reading.py (Reading: user_id index, deleted_at, status, created_at, question, summary_short; ReadingCard: reading_id, deck_card_id, position_index; ReadingStatus.COMPLETED)
    - backend/app/models/deck.py (Deck.title — the human "колода" name; DeckCard.thumbnail_url — the §9.6 миниатюры source, A2)
    - backend/app/models/spread.py (SpreadType.title — the human "расклад" name)
    - .planning/phases/05-history-profile/05-RESEARCH.md (Pattern 1 query shape + the ReadingListItemOut field list; Pitfall 1 async lazy-load; Pitfall 3 cap×offset interaction; Open Question 2 = hardcode FREE_HISTORY_CAP)
    - backend/tests/integration/test_readings_list.py (05-01 red tests this must turn green: test_auto_save_appears_in_list, test_shape_and_order, test_last_ten_cap)
  </read_first>
  <behavior>
    - list_readings returns newest-first ReadingListItemOut[] scoped to user.id, excluding deleted_at-set and non-COMPLETED rows.
    - With 12 completed readings and limit=10/offset=0 it returns exactly 10; offset>=FREE_HISTORY_CAP returns []; the effective window is bounded by `min(limit, FREE_HISTORY_CAP - offset)`.
    - Each item has reading_id, created_at, question (None/"" → None), deck_name (Deck.title), spread_name (SpreadType.title), card_thumbnails (list[str] from deck_cards.thumbnail_url, may be empty), summary_short — and carries NO full interpretation.
    - The older (11th/12th) rows remain present in the DB (the cap is a query bound, never a delete).
  </behavior>
  <action>
    In `backend/app/schemas/reading.py` add `ReadingListItemOut(BaseModel)` (model_config from_attributes) with exactly: `reading_id: str`, `created_at: datetime`, `question: str | None`, `deck_name: str`, `spread_name: str`, `card_thumbnails: list[str]`, `summary_short: str | None` — each with a brief RU `Field(description=...)` per §9.6. Add it to `__all__`. Do NOT add any `max_length` and do NOT include per-card interpretation fields (RESEARCH anti-pattern: the list is "короткий итог" only).
    In `backend/app/services/reading.py` add a module constant `FREE_HISTORY_CAP = 10` with a clear seam comment ("Phase 6/7 swaps this for a tier-derived limit; older rows stay in the DB — display-cap, not prune"). Add an async method `list_readings(self, session, user, *, limit=10, offset=0) -> list[ReadingListItemOut]`:
      * compute the effective limit `eff = min(limit, FREE_HISTORY_CAP - offset)`; if `eff <= 0` return `[]`;
      * `select(Reading).where(Reading.user_id == user.id, Reading.deleted_at.is_(None), Reading.status == ReadingStatus.COMPLETED).order_by(Reading.created_at.desc()).offset(offset).limit(eff)`;
      * for the page's reading ids, issue ONE explicit `select(ReadingCard)...` joined to `DeckCard` (by `reading_cards.deck_card_id`) to gather `thumbnail_url` per reading, grouped in Python by `reading_id` and ordered by `position_index` (the established explicit-select style — NO lazy relationship access);
      * resolve `deck_name`/`spread_name` via joins to `Deck.title` / `SpreadType.title` (either join in the main select or a small id→title map; keep it one extra query max);
      * map each row → `ReadingListItemOut` (normalize empty `question` to None).
    Keep it pure read (no writes, no commit). User is passed in (never derived from a body).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/integration/test_readings_list.py -k "shape_and_order or last_ten_cap" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `ReadingListItemOut` exists with exactly the 7 §9.6 fields and is in `__all__`; it has NO interpretation/cards field.
    - `FREE_HISTORY_CAP = 10` is a module constant with the Phase-6 seam comment.
    - `list_readings` returns newest-first, user-scoped, `deleted_at IS NULL`, `status == COMPLETED` items; `offset >= FREE_HISTORY_CAP` → `[]`; 12 completed → exactly 10.
    - The card-thumbnail load uses an explicit `select(ReadingCard)` (grep: no `Reading.cards` relationship is added).
    - `uv run pytest tests/integration/test_readings_list.py -k "shape_and_order or last_ten_cap"` passes (was xfail in 05-01).
  </acceptance_criteria>
  <done>The light list schema + the capped, user-scoped, soft-delete-excluding service query exist; the shape/order and last-10-cap red tests pass; no `Reading.cards` lazy relationship was introduced.</done>
</task>

<task type="auto">
  <name>Task 2: GET /api/readings thin router + auto-save visibility</name>
  <files>backend/app/api/readings.py</files>
  <read_first>
    - backend/app/api/readings.py (the existing `POST /api/readings` thin-router + `get_reading_service` dependency + `get_current_user`/`get_session` deps to mirror; the `ReadingInputError → 404` mapping)
    - backend/app/services/reading.py (Task 1: `list_readings`)
    - backend/app/schemas/reading.py (Task 1: `ReadingListItemOut`)
    - .planning/phases/05-history-profile/05-RESEARCH.md (Code Examples: the `GET /api/readings` thin-router with `limit: int = Query(10, ge=1, le=10)`, `offset: int = Query(0, ge=0)`, user from `get_current_user`)
    - backend/tests/integration/test_readings_list.py (test_auto_save_appears_in_list — the HIST-01 visibility test)
  </read_first>
  <action>
    In `backend/app/api/readings.py` add `@router.get("/readings", response_model=list[ReadingListItemOut])` `async def list_readings(...)` mirroring the POST handler: `limit: int = Query(10, ge=1, le=10)`, `offset: int = Query(0, ge=0)`, `user: User = Depends(get_current_user)`, `session = Depends(get_session)`, `service = Depends(get_reading_service)`. Delegate to `service.list_readings(session, user, limit=limit, offset=offset)`. No business logic in the router. Import `ReadingListItemOut` from the schemas. The user comes ONLY from `get_current_user` (the JWT `sub`); the body/query never carries identity. Leave a one-line seam comment that the optional `topic`/`deck_slug` filter params (D-01) are intentionally NOT surfaced in MVP (the free list is ≤10).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/integration/test_readings_list.py tests/integration/test_readings_auth.py -k "auto_save or cross_user" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/readings` is registered on the readings router with `response_model=list[ReadingListItemOut]`, bounded `limit` (ge=1,le=10) and `offset` (ge=0).
    - The handler delegates to `service.list_readings` and derives the user from `get_current_user` (no body/query user_id).
    - `test_auto_save_appears_in_list` passes (a completed reading appears in the list).
    - The cross-user list-path stays correct: a second user's list never includes user A's reading (the `test_cross_user_detail_and_delete_404` GET-detail half is 05-04; the list-scoping is asserted by `test_shape_and_order` running per-user).
  </acceptance_criteria>
  <done>`GET /api/readings` serves the light, user-scoped list; HIST-01 auto-save is observable; the endpoint is a thin router with no business logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | The Bearer JWT is the only identity; `limit`/`offset` are untrusted query params validated by FastAPI; no body. |
| API → DB | All reads are `select()`-parameterized (no string-built SQL). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure / Elevation | `GET /api/readings` user scoping | mitigate | `list_readings` filters `Reading.user_id == get_current_user().id`; no body/query user_id is read. Asserted by per-user `test_shape_and_order`. |
| T-05-02 | Information Disclosure | soft-delete leak in list | mitigate | `Reading.deleted_at.is_(None)` on the list query (Pitfall 4); deleted rows never appear. |
| T-05-03 | Information Disclosure | over-reading internals in list | mitigate | The light `ReadingListItemOut` omits internal columns (`generation_error`/`prompt_version`/`model_name`); only §9.6 fields are exposed (T-04-08/27 inheritance). |
| T-05-05 | Tampering | unbounded paging past the free cap | mitigate | `min(limit, FREE_HISTORY_CAP - offset)` + `Query(le=10)`; `offset >= cap` → `[]` (Pitfall 3). |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No installs (RESEARCH Package Legitimacy Audit: zero new packages). |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/integration/test_readings_list.py -x -q` — all three list tests pass (auto_save, shape_and_order, last_ten_cap).
- Grep confirms no `relationship(` was added to `Reading` for cards (Pitfall 1 avoided).
- `cd backend && uv run pytest -q` — full backend suite green (baseline + the now-xpassing list tests).
</verification>

<success_criteria>
- `GET /api/readings` returns the light, newest-first, user-scoped, COMPLETED-only, soft-delete-excluding, last-10-capped list.
- HIST-01/02/06 satisfied; older rows retained in the DB.
- No new dependencies; the cap constant is the explicit Phase-6 seam.
</success_criteria>

<output>
Create `.planning/phases/05-history-profile/05-02-SUMMARY.md` when done.
</output>
