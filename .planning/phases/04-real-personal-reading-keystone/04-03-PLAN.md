---
phase: 04-real-personal-reading-keystone
plan: 03
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - backend/app/services/llm.py
  - backend/app/services/safety.py
  - backend/tests/unit/test_safety_routing.py
  - backend/tests/unit/test_llm_service.py
autonomous: true
requirements: [READ-03, READ-04, SAFE-01, SAFE-02, SAFE-04, SAFE-05]

must_haves:
  truths:
    - "LLMService wraps ONE messages.parse call returning a schema-validated ReadingOutput, with tenacity = exactly one corrective retry that escalates Haiku→Sonnet (D-12), a per-attempt timeout, and reraises the real exception on total failure"
    - "LLMService extracts input/output tokens, latency, model id (alias claude-haiku-4-5/claude-sonnet-4-6, not a dated snapshot), and stop_reason so the caller can log a generation_logs row per call (ANALYTICS-02)"
    - "SafetyService.classify runs a regex pre-filter first (instant crisis catch + empty→normal, zero cost) then a tiny Haiku classify call returning a SafetyVerdict for the undecided cases (SAFE-01)"
    - "classify maps to the routing the orchestrator uses: crisis→refusal, abusive→redirect, *_sensitive→safety_modifier flag, normal→generate (SAFE-02)"
  artifacts:
    - path: "backend/app/services/llm.py"
      provides: "LLMService.generate — messages.parse + tenacity retry/timeout + usage extraction"
      contains: "messages.parse"
    - path: "backend/app/services/safety.py"
      provides: "SafetyService.classify — regex pre-filter + Haiku classify → SafetyVerdict"
      contains: "SafetyCategory"
  key_links:
    - from: "backend/app/services/llm.py"
      to: "backend/app/core/llm_client.py"
      via: "calls the AsyncAnthropic singleton's messages.parse"
      pattern: "messages.parse"
    - from: "backend/app/services/safety.py"
      to: "backend/app/services/llm.py"
      via: "the classify call reuses the AsyncAnthropic client wrapper for a tiny enum-output parse"
      pattern: "SafetyVerdict"
---

<objective>
Build the Anthropic call layer the keystone depends on: `LLMService` (the swappable `messages.parse` wrapper with the locked resilience contract — one corrective Haiku→Sonnet retry + per-attempt timeout + usage/latency/stop_reason extraction; READ-03/04, D-12) and `SafetyService` (the mandatory classifier: a regex pre-filter that short-circuits crisis instantly + a tiny Haiku classify call returning a `SafetyVerdict`; SAFE-01/02). Both are unit-tested against a mocked AsyncAnthropic client — no real API call.

Purpose: This is the "how we talk to Claude" layer. LLMService guarantees a schema-valid `ReadingOutput` on success and a clean failure signal on exhaustion; SafetyService decides BEFORE the draw whether to generate at all. Plan 05 composes both inside `ReadingService` in the locked gate→generate order.
Output: `services/llm.py`, `services/safety.py`, the implemented `tests/unit/test_safety_routing.py` (skip removed), and a new `tests/unit/test_llm_service.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-personal-reading-keystone/04-RESEARCH.md
@.planning/phases/04-real-personal-reading-keystone/04-VALIDATION.md
@backend/app/core/llm_client.py
@backend/app/schemas/reading.py
@backend/app/core/config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LLMService — messages.parse wrapper + tenacity 1-retry (Haiku→Sonnet) + timeout + usage extraction</name>
  <files>backend/app/services/llm.py, backend/tests/unit/test_llm_service.py</files>
  <read_first>
    - .planning/phases/04-real-personal-reading-keystone/04-RESEARCH.md (Pattern 4 — RETRYABLE = (ValidationError, anthropic.APIStatusError, anthropic.APIConnectionError, TimeoutError); `@retry(stop=stop_after_attempt(2), wait=wait_fixed(0.5), retry=retry_if_exception_type(RETRYABLE), reraise=True)`; attempt 1 = Haiku, attempt 2 = Sonnet keyed off attempt number; per-call `timeout=20.0`; temperature≈0.7; max_tokens≈1500; `response.parsed_output` → ReadingOutput (raises ValidationError); `response.usage.input_tokens/output_tokens`; `response.stop_reason` guard for refusal/max_tokens; Code Examples → the verified messages.parse shape; Pitfall 2 (refusal/max_tokens→ValidationError) + Pitfall 6 (AsyncAnthropic + await, tenacity async support))
    - .planning/REFERENCE-TZ.md §29.2 (validate ALL LLM output by JSON schema; save generation errors) and the resilience rule (one corrective retry → fail, no templated fallback)
    - backend/app/schemas/reading.py (ReadingOutput is the `output_format`; the validated return type)
    - backend/app/core/llm_client.py (the AsyncAnthropic singleton to call; tests monkeypatch this)
    - CLAUDE.md (LLM layer — Haiku 4.5 default / Sonnet 4.6 escalation on validation failure; alias model ids, log the resolved version, never a dated snapshot; tenacity retry+timeout; `messages.parse` not prompt-and-parse)
  </read_first>
  <behavior>
    - generate(system, user_prompt) with a mock client returning a valid ReadingOutput on attempt 1 → returns the ReadingOutput; model used == claude-haiku-4-5; one attempt. [test_success_first_attempt_haiku]
    - mock raises ValidationError on attempt 1, returns valid on attempt 2 → succeeds; attempt 2 used claude-sonnet-4-6 (D-12 escalation). [test_corrective_retry_escalates_to_sonnet]
    - mock raises ValidationError twice → generate raises (reraise=True) after exactly 2 attempts. [test_exhausted_reraises]
    - the result object/return exposes input_tokens, output_tokens, latency_ms, model_name, stop_reason for the caller's generation_logs row. [test_usage_and_metadata_extracted]
    - a non-retryable exception type is NOT retried (single attempt). [test_non_retryable_not_retried]
  </behavior>
  <action>
    Create `backend/app/services/llm.py` with an injectable `LLMService` (constructor takes the AsyncAnthropic client, default = the `core/llm_client.py` singleton — so Plan 05 + tests substitute a fake) exposing async `generate(system: str, user_prompt: str) -> GenerationResult`. Define a small frozen `GenerationResult` carrying `output: ReadingOutput`, `model_name: str`, `input_tokens: int`, `output_tokens: int`, `latency_ms: int`, `stop_reason: str` (the exact fields Plan 05 writes to `generation_logs` — ANALYTICS-02). Internals per RESEARCH Pattern 4: a tenacity-decorated coroutine `@retry(stop=stop_after_attempt(2), wait=wait_fixed(0.5), retry=retry_if_exception_type(RETRYABLE), reraise=True)` where the model is chosen by attempt number (attempt 1 → `claude-haiku-4-5`, attempt 2 → `claude-sonnet-4-6`, D-12 — read the attempt count via the retry state or a small counter wrapper); each attempt calls `await client.messages.parse(model=..., max_tokens=1500, system=system, messages=[{"role":"user","content":user_prompt}], output_format=ReadingOutput, temperature=0.7, timeout=20.0)`, reads `response.parsed_output` (raises ValidationError → retry trigger), checks `response.stop_reason` (refusal/max_tokens → treat as a retry trigger by raising a retryable), and computes `latency_ms` (monotonic clock around the call) + reads `response.usage.input_tokens/output_tokens`. Use model ALIASES, never dated snapshots (CLAUDE.md); the alias string is what gets logged. `RETRYABLE = (ValidationError, anthropic.APIStatusError, anthropic.APIConnectionError, TimeoutError)`. On exhaustion the real exception reraises (Plan 05 turns it into the honest fail). These timings/temperature/max_tokens are RESEARCH's delegated starting config (A3) — keep them as named module constants so they are tunable.
    Create `backend/tests/unit/test_llm_service.py` implementing the five behaviours with a fake AsyncAnthropic whose `messages.parse` is an AsyncMock scripted per case (return an object with `.parsed_output`, `.usage`, `.stop_reason`, or raise). Assert the model id per attempt, the attempt count, the reraise, and the extracted metadata. No network.
  </action>
  <verify>
    <automated>cd backend && pytest -q tests/unit/test_llm_service.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q tests/unit/test_llm_service.py` exits 0.
    - `llm.py` contains `messages.parse`, `stop_after_attempt(2)`, `retry_if_exception_type`, and `reraise=True`; uses `claude-haiku-4-5` then `claude-sonnet-4-6` (grep both alias strings; no dated `-YYYYMMDD` snapshot).
    - corrective retry escalates to Sonnet (asserted); double failure reraises after exactly 2 attempts; usage/latency/model/stop_reason exposed on the result.
    - `LLMService` is injectable (client passed in) so it is mockable; uses `AsyncAnthropic` (async), not the sync client.
    - `ruff check app/services/llm.py` → 0.
  </acceptance_criteria>
  <done>The resilient single-call wrapper guarantees a schema-valid ReadingOutput on success, escalates Haiku→Sonnet on the one corrective retry, exposes the audit metadata, and reraises cleanly on exhaustion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SafetyService — regex pre-filter + Haiku classify → SafetyVerdict + routing</name>
  <files>backend/app/services/safety.py, backend/tests/unit/test_safety_routing.py</files>
  <read_first>
    - .planning/phases/04-real-personal-reading-keystone/04-RESEARCH.md (Pattern 2 — Stage 1 regex pre-filter (RU/EN crisis keywords → instant crisis_sensitive; empty question → normal with NO call); Stage 2 tiny Haiku `messages.parse` with `output_format=SafetyVerdict` (enum-only) for undecided cases; routing table crisis→refusal / abusive→redirect / *_sensitive→safety_modifier / normal→generate; classify call is logged like every LLM call; Anti-Pattern: classifying inside the main call cannot gate before draw — rejected)
    - .planning/REFERENCE-TZ.md §20.1 (general bans), §20.2 (sensitive-topic list), §20.4 (the 7 classifier categories — the exact SafetyCategory set), §15.1 (allowed soft formulations vs banned categorical ones)
    - backend/app/schemas/reading.py (SafetyCategory + SafetyVerdict from Plan 01 — the classify output_format)
    - backend/app/services/llm.py (the AsyncAnthropic client wrapper to reuse for the tiny classify parse — from Task 1; classify is its own small parse, mockable)
    - backend/tests/unit/test_safety_routing.py (the Wave-0 skipped stub from Plan 01 to implement)
    - CLAUDE.md / 04-CONTEXT.md (D-03 crisis short-circuits BEFORE draw/charge — locked; D-05 sensitive → silent safety_modifier; D-06 abusive → redirect; D-14 reading always Russian simplifies classify)
  </read_first>
  <behavior>
    - an obvious crisis question (RU/EN self-harm/violence keyword) → category crisis_sensitive via the regex pre-filter with NO classify call made. [test_regex_prefilter_catches_crisis_no_call]
    - an empty/None question (HOME-02) → normal via the pre-filter, NO classify call. [test_empty_question_is_normal_no_call]
    - an undecided question → a Haiku classify call is made and its SafetyVerdict.category is returned. [test_undecided_calls_classify]
    - a routing helper maps each category to its action: crisis→refusal, abusive_or_manipulative→redirect, any *_sensitive→safety_modifier flag (continue), normal→generate. [test_routing_actions]
    - the classify call uses output_format=SafetyVerdict (enum-constrained) — not prompt-and-parse. [test_classify_uses_structured_output]
  </behavior>
  <action>
    Create `backend/app/services/safety.py` with an injectable `SafetyService` (takes the same AsyncAnthropic client / a callable to the classify parse, default = the singleton — so Plan 05 + tests substitute a fake) exposing async `classify(question: str | None) -> SafetyVerdict`. Stage 1 — a module-level compiled RU/EN crisis keyword regex (self-harm/suicide/violence high-signal terms per §20.4) that returns `SafetyVerdict(category=crisis_sensitive)` immediately on a hit, and treats empty/whitespace/None as `normal` (HOME-02, A6) — both with NO API call. Stage 2 — for everything else, one tiny `messages.parse(model="claude-haiku-4-5", output_format=SafetyVerdict, ...)` with a short system instruction listing the 7 §20.4 categories and the §20.2 sensitive list; return its `parsed_output`. Add a pure `route(verdict) -> SafetyAction` helper (an enum or small mapping) encoding crisis→REFUSAL, abusive_or_manipulative→REDIRECT, {relationship,financial,health,legal}_sensitive→SAFETY_MODIFIER, normal→GENERATE — this is what Plan 05 branches on. The classify call returns metadata the caller can log as a `generation_logs` row with status like `classify` (ANALYTICS-02) — expose the usage/model the same way Task 1 does, or return a small result the orchestrator logs. Keep the regex + routing pure/synchronous so they unit-test without a client.
    Implement `backend/tests/unit/test_safety_routing.py` (remove the Wave-0 skip): the five behaviours, with a fake client whose classify parse is an AsyncMock returning a scripted SafetyVerdict for the undecided case, and asserting NO call happens on the regex-decided + empty cases. Cover SAFE-02 (sensitive→SAFETY_MODIFIER) and SAFE-04/05 routing (crisis→REFUSAL not a prediction). No network.
  </action>
  <verify>
    <automated>cd backend && pytest -q tests/unit/test_safety_routing.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q tests/unit/test_safety_routing.py` exits 0; the Wave-0 skip is removed.
    - `safety.py` contains a `SafetyCategory`/`SafetyVerdict` usage and `output_format=SafetyVerdict` on the classify call (structured output, not `json.loads`).
    - regex pre-filter catches crisis with NO classify call; empty/None → normal with NO call; undecided → classify call made.
    - `route(...)` maps crisis→REFUSAL, abusive→REDIRECT, *_sensitive→SAFETY_MODIFIER, normal→GENERATE (asserted).
    - `SafetyService` is injectable (client/callable passed in) so it is mockable.
    - `ruff check app/services/safety.py` → 0.
  </acceptance_criteria>
  <done>The mandatory classifier decides crisis instantly via regex and everything else via a tiny structured Haiku call, exposing the routing Plan 05 gates the draw on — honoring the locked "classify before draw/charge" ordering (the routing is computed before any CardDraw).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user question → classify call | The user's free-text question is untrusted input; it is data inside a fixed system frame, never an instruction to the model. |
| user question → generation prompt | Prompt-injection attempts must not redirect the model; structured output + safety pre-classify constrain it. |
| LLM response → backend | The model's output must be schema-validated before any persistence; non-schema output is a failure, not data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Tampering / Elevation | Prompt injection via the user's question | mitigate | Question is data inside the fixed §16 system frame; `messages.parse` constrains the response to `ReadingOutput`; SafetyService pre-classifies abusive/manipulative (→ redirect). |
| T-04-16 | (Safety/abuse) | Crisis question reaches generation and is served a fatalistic prediction | mitigate | Regex pre-filter catches the highest-signal crisis terms with zero latency + the Haiku classifier covers the rest; `route()` returns REFUSAL — Plan 05 short-circuits BEFORE CardDraw (D-03 locked). |
| T-04-17 | Tampering | Model returns malformed/non-schema JSON | mitigate | `messages.parse` constrained decoding + `parsed_output` ValidationError → tenacity one corrective retry (Sonnet) → reraise (no parse-and-repair; §29.2). |
| T-04-18 | DoS / cost | Classify call cost/latency on every reading | mitigate | Regex pre-filter removes the round-trip for the clearest cases; the classify call is a tiny enum-only Haiku parse (fraction of a cent); logged for audit. |
</threat_model>

<verification>
- `cd backend && pytest -q tests/unit/test_llm_service.py tests/unit/test_safety_routing.py` → green (safety skip removed).
- Grep `app/services/llm.py` for `claude-haiku-4-5` AND `claude-sonnet-4-6` AND `reraise=True` → present; no dated model snapshot.
- Grep `app/services/safety.py` for `output_format=SafetyVerdict` → present; no `json.loads` of model output.
- `cd backend && ruff check app/services/llm.py app/services/safety.py` → 0.
</verification>

<success_criteria>
- READ-03/04 / D-12: one `messages.parse` returns a validated ReadingOutput; one corrective retry escalates Haiku→Sonnet; exhaustion reraises (no templated fallback); usage/latency/model/stop_reason exposed for ANALYTICS-02.
- SAFE-01: regex pre-filter + tiny Haiku classify → SafetyVerdict over the 7 §20.4 categories.
- SAFE-02/04/05: routing maps crisis→refusal, abusive→redirect, sensitive→safety_modifier, normal→generate — the gate Plan 05 runs before the draw.
</success_criteria>

<output>
Create `.planning/phases/04-real-personal-reading-keystone/04-03-SUMMARY.md` when done.
</output>
