---
phase: 04-real-personal-reading-keystone
plan: 06
type: execute
wave: 3
depends_on: ["04-05"]
files_modified:
  - frontend/src/reading/createReading.ts
  - frontend/src/reading/types.ts
  - frontend/src/components/CatalogScreen.tsx
  - frontend/src/reading/createReading.test.ts
autonomous: false
requirements: [READ-01, READ-11]

must_haves:
  truths:
    - "createReading() POSTs to /api/readings via the apiFetch Bearer seam and returns the SAME MockReading shape, so the ritual/reveal/result UI is untouched — the swap is mechanical (D-07, Phase-3 D-05 seam)"
    - "the backend ReadingOut maps onto MockReading: per-card name/positionTitle/orientation/shortMeaning/interpretation/deckAccent + summary connection→linkage / main_factor→mainFactor / attention_point→attention / advice→softAdvice / closing_phrase→closingPhrase"
    - "the ritual covers the real latency with NO spinner; reveal happens only once schema-valid JSON resolves; a failed POST surfaces the §9.8 «Колода замолчала» copy and does NOT advance the step (D-07/D-08)"
    - "on failure the user can Повторить (same reading) and Сменить колоду (back to selection, question preserved per Phase-3 D-04) — not just a single back (D-08)"
  artifacts:
    - path: "frontend/src/reading/createReading.ts"
      provides: "real async POST /api/readings via apiFetch, ReadingOut→MockReading mapping"
      contains: "apiFetch"
  key_links:
    - from: "frontend/src/reading/createReading.ts"
      to: "frontend/src/api/client.ts"
      via: "apiFetch('/api/readings', {method:'POST'}) attaches the Bearer JWT"
      pattern: "apiFetch"
    - from: "frontend/src/components/CatalogScreen.tsx"
      to: "frontend/src/reading/createReading.ts"
      via: "the CTA awaits createReading then setReading→goTo('ritual'); failure shows §9.8 + retry/change-deck"
      pattern: "createReading"
---

<objective>
Complete the user-facing slice: swap the Phase-3 mock `createReading()` body for a real authenticated `POST /api/readings` (via the existing `apiFetch` Bearer seam), preserving the exact `MockReading` return type so the ritual/reveal/result screens are untouched (D-07 "mechanical swap"), map the backend `ReadingOut` onto `MockReading`, and wire the D-08 failure UX (Повторить + Сменить колоду, question preserved) on the existing selection screen. The ritual covers the now-real latency with no spinner; reveal waits on the promise.

Purpose: This is where the Core Value becomes visible to the user — the same ritual, now backed by a real per-deck reading. The seam was designed in Phase 3 to make this swap mechanical; we honor the locked Phase-3 D-05 architecture (the reading lives in the store `reading` slot, the seam keeps its signature) and only change the data source.
Output: a real `createReading.ts`, the `ReadingOut`→`MockReading` mapping, the failure UX on `CatalogScreen.tsx`, the updated `createReading.test.ts`, and the two manual UAT checkpoints (per-deck felt-quality + live-API smoke + ritual/failure UX + crisis tone) from 04-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-personal-reading-keystone/04-RESEARCH.md
@.planning/phases/04-real-personal-reading-keystone/04-VALIDATION.md
@.planning/phases/04-real-personal-reading-keystone/04-05-SUMMARY.md
@frontend/src/reading/createReading.ts
@frontend/src/reading/types.ts
@frontend/src/reading/copy.ts
@frontend/src/api/client.ts
@frontend/src/components/CatalogScreen.tsx
@frontend/src/stores/selection.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swap createReading() mock → real POST /api/readings + ReadingOut→MockReading mapping</name>
  <files>frontend/src/reading/createReading.ts, frontend/src/reading/types.ts, frontend/src/reading/createReading.test.ts</files>
  <read_first>
    - .planning/phases/04-real-personal-reading-keystone/04-RESEARCH.md (Pattern 6 — replace the createReading body with an apiFetch POST PRESERVING the MockReading return type; the ReadingOut→MockReading field map: connection→linkage, attention_point→attention, soft_advice/advice→softAdvice, closing_phrase→closingPhrase; reveal awaits the promise; failure copy = READING_ERROR §9.8; NOTE the Pattern-6 TanStack-mutation suggestion CONFLICTS with the locked Phase-3 D-05 "reading lives in the store, NEVER in Query" guard — keep the mechanical seam swap (same signature, store slot unchanged), do not re-architect into a Query mutation)
    - .planning/phases/04-real-personal-reading-keystone/04-05-SUMMARY.md (the actual ReadingOut JSON shape the endpoint returns — field names + nesting — to map from)
    - frontend/src/reading/createReading.ts (the CURRENT mock implementation + CreateReadingParams signature to PRESERVE; the rng param is mock-only and is dropped for the real call)
    - frontend/src/reading/types.ts (MockReading/MockReadingCard/MockReadingSummary — the EXACT target shape the mapping must produce, unchanged) 
    - frontend/src/api/client.ts (apiFetch — the Bearer seam to POST through; reads jwt from useSession)
    - frontend/src/lib/api.ts (API_BASE)
    - frontend/src/reading/copy.ts (BANNED_BRAND_TOKENS — the brand-safe assertion the test reuses; READING_ERROR §9.8)
    - frontend/src/reading/createReading.test.ts (the CURRENT mock tests — rewrite to mock apiFetch/fetch and assert the POST shape + mapping + error mapping; the reversals/rng tests are mock-only and are removed since orientation is now server-decided)
    - CLAUDE.md / 04-CONTEXT.md (D-14 reading always Russian; SAFE-06 no AI/brand words; D-07 mechanical swap)
  </read_first>
  <behavior>
    - createReading(params) calls apiFetch('/api/readings', {method:'POST', body: JSON.stringify({question, topic, deck_slug, spread_slug, reversals_enabled})}) and resolves to a MockReading. [test_posts_to_readings_endpoint]
    - the request body uses the §14.5 field names (deck_slug/spread_slug/reversals_enabled), and question is null for an empty general reading (HOME-02/D-13). [test_request_body_shape]
    - a 200 ReadingOut is mapped onto MockReading: every per-card field + the 5 summary fields populated via the documented name map (connection→linkage etc.). [test_maps_readingout_to_mockreading]
    - a non-OK response / failed generation rejects (so the caller's catch shows §9.8 and does not advance). [test_failure_rejects]
    - the mapped copy is brand-safe (no banned token in the assembled card+summary text). [test_mapped_copy_brand_safe]
  </behavior>
  <action>
    Rewrite the body of `frontend/src/reading/createReading.ts` to call the real endpoint via `apiFetch` while PRESERVING the exported `createReading` signature and the `MockReading` return type (the swap is mechanical — Pattern 6 / Phase-3 D-05). Build the request body with the §14.5 field names (`question` (null when empty — D-13), `topic`, `deck_slug`, `spread_slug`, `reversals_enabled`); the mock-only `rng` param is dropped (orientation is now server-decided — D-13 70/30 on the backend). On a non-OK status or a soft failure body (status field indicating failed), throw so the existing `CatalogScreen` catch surfaces `READING_ERROR` (§9.8) and stays on selection. On 200, map `ReadingOut` → `MockReading`: per-card `name/positionTitle/orientation/shortMeaning/interpretation/deckAccent` (+ keep `shortPhrase` — sourced from the backend per-card field or the existing copy bank if the backend does not provide it) and summary `connection→linkage`, `main_factor→mainFactor`, `attention_point→attention`, `advice/soft_advice→softAdvice`, `closing_phrase→closingPhrase`; pass through `question/topic/deckSlug/spreadSlug/createdAt`. Add a small typed `interface ReadingOutResponse` (the backend contract) at the top or in `types.ts`; do NOT use `any` (web TS rules). Keep the reading in the store `reading` slot unchanged (do NOT move to TanStack Query — that would violate the locked Phase-3 D-05 guard; note this in a comment).
    Rewrite `frontend/src/reading/createReading.test.ts` (Vitest): mock the global `fetch`/`apiFetch` to return a scripted `ReadingOut` (success) or a non-OK / failed body (error); assert the POST URL + method + body field names, the full mapping, the null-question general case, the rejection on failure, and the brand-safety of the mapped text (reuse `BANNED_BRAND_TOKENS`). Remove the now-obsolete mock-only reversals/rng tests (orientation is server-side).
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run src/reading/createReading.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/reading/createReading.test.ts` exits 0.
    - `createReading.ts` contains `apiFetch` and POSTs to `/api/readings` with the §14.5 body field names; returns a `MockReading` (signature/return type unchanged).
    - The `ReadingOut`→`MockReading` mapping populates every per-card + 5 summary field via the documented name map; no `any` used.
    - A failed/non-OK response rejects; the mapped copy passes the SAFE-06 ban-list.
    - The reading still lives in the store `reading` slot (createReading is NOT converted to a TanStack mutation — Phase-3 D-05 guard preserved).
  </acceptance_criteria>
  <done>The seam now fetches a real per-deck reading and maps it to the unchanged MockReading shape; the reveal/result UI renders real content with zero UI changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Failure UX on the selection screen — Повторить + Сменить колоду (question preserved), reveal awaits the promise</name>
  <files>frontend/src/components/CatalogScreen.tsx, frontend/src/reading/createReading.test.ts</files>
  <read_first>
    - .planning/phases/04-real-personal-reading-keystone/04-RESEARCH.md (Pattern 6 — reveal awaits the promise (D-07): if it resolves before the ritual ends, hold; if after, the last beat loops, NO spinner; failure → Повторить (same reading) + Сменить колоду (back to selection, question preserved); reuses Phase-3 D-04 question-preservation)
    - .planning/REFERENCE-TZ.md §9.8 (the EXACT failure copy already in READING_ERROR) 
    - frontend/src/components/CatalogScreen.tsx (the CURRENT CTA handler lines: the try/await createReading → setReading → goTo("ritual"); the catch that already surfaces READING_ERROR but currently only does NOT advance — extend it to offer Повторить + Сменить колоду; the question is already in the store and preserved on back per D-04)
    - frontend/src/reading/copy.ts (READING_ERROR; add Повторить / «Сменить колоду» button labels here if not present — every new user-facing string lives in copy.ts for the SAFE-06 scan; mirror RESULT_AGAIN_CTA style)
    - frontend/src/stores/selection.ts (setReading/goTo/the preserved question + the reading slot; the in-app back/D-04 question preservation behavior)
    - frontend/src/flow/steps.ts (Step union + goTo target "selection"/"ritual")
    - 04-CONTEXT.md (D-07 ritual covers latency no spinner; D-08 Повторить + Сменить колоду, NOT just back; limit not consumed so retry is free)
  </read_first>
  <behavior>
    - on a createReading rejection the handler renders the §9.8 READING_ERROR copy AND two affordances: Повторить (re-invokes createReading with the same params) and Сменить колоду (returns to deck selection with the question preserved). [test_failure_offers_retry_and_change_deck]
    - the CTA does NOT advance to ritual/reveal on failure (stays on selection). [test_no_advance_on_failure]
    - Повторить re-runs the same request (same question/topic/deck/spread). [test_retry_reuses_same_params]
    - Сменить колоду keeps the question in the store (D-04 preservation). [test_change_deck_preserves_question]
  </behavior>
  <action>
    Extend the `CatalogScreen.tsx` CTA flow (do NOT touch the ritual/reveal/result components — they read the store slot unchanged): keep the happy path `await createReading(...) → setReading(reading) → goTo("ritual")` (setReading BEFORE goTo so the slot is populated on first paint — the existing invariant). In the `catch`, set a local error state that renders the existing `READING_ERROR` line PLUS a Повторить button (re-invokes the same handler with the unchanged params) and a «Сменить колоду» button (clears/keeps as needed and `goTo("selection")` / focuses deck selection with the store question preserved — reuse the Phase-3 D-04 question-preservation; do NOT clear `question`). No spinner is added — the ritual itself covers latency (the reveal already awaits the promise via the store hand-off; D-07). Add any new button labels (e.g. `READING_RETRY = "Повторить"`, `READING_CHANGE_DECK = "Сменить колоду"`) to `reading/copy.ts` so they are covered by the SAFE-06 scan, and reference them (do not inline raw strings). Keep all client state in the selection store (no TanStack Query for the reading — Phase-3 D-05 guard).
    Add the four behaviours to `createReading.test.ts` (or a sibling `CatalogScreen.test.tsx` if component-level RTL is the established pattern — mirror the existing `CatalogScreen.test.tsx`): assert failure offers both affordances + does not advance, retry reuses params, and «Сменить колоду» preserves the question.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run src/reading/createReading.test.ts src/components/CatalogScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run` over the touched test files exits 0.
    - On failure the screen shows the §9.8 copy + Повторить + Сменить колоду; it does NOT advance to ritual/reveal.
    - Повторить re-runs the same request; Сменить колоду returns to selection with the question preserved (D-04).
    - New button labels live in `reading/copy.ts` and pass the SAFE-06 ban-list (no AI/brand words); no raw inline failure strings.
    - No spinner added; the reading is not mirrored into TanStack Query (Phase-3 D-05 guard).
  </acceptance_criteria>
  <done>The failure path is humane and recoverable: the ritual covers the wait with no spinner, and a failed generation offers a free Повторить + a question-preserving Сменить колоду (D-07/D-08).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT — per-deck felt-quality, live-API smoke, ritual/failure UX, crisis tone</name>
  <files></files>
  <read_first>
    - .planning/phases/04-real-personal-reading-keystone/04-VALIDATION.md (Manual-Only Verifications table — the four subjective checks that CANNOT be auto-asserted: per-deck divergence felt-quality, live-API valid ReadingOutput for all 6 decks/7 spreads, ritual-covers-latency + failure UX, crisis tone)
    - .planning/REFERENCE-TZ.md §30 (the MVP north star "один и тот же вопрос ощущается по-разному в разных колодах" — the bar the per-deck check measures) , §9.8 (crisis/failure tone)
    - 04-CONTEXT.md (D-01/D-02 tone+focus+signature; D-03/D-04 crisis tone; D-07/D-08 ritual+failure UX)
  </read_first>
  <what-built>
    The full real-reading slice is wired end-to-end: POST /api/readings (gate→draw→one structured call→persist) under the existing ritual/reveal/result UI, with honest-fail + retry/change-deck. The automated suite mocks the LLM; these four checks require a human + a real ANTHROPIC_API_KEY and cannot be faked.
  </what-built>
  <how-to-verify>
    1. PER-DECK DIVERGENCE (READ-11 / Core Value): with the backend running + a real key, submit the SAME question on 2–3 decks (e.g. Тени, Сердце, Лесной). Confirm noticeably different tone AND focus, and a recognizable per-deck signature in each (D-02). Confirm no result text contains "AI / нейросеть / модель / ИИ".
    2. LIVE-API SMOKE (READ-03/05/06): run the env-gated live smoke test (set ANTHROPIC_API_KEY; the test is skipped by default) — confirm a valid ReadingOutput comes back for a sample across the 6 decks / 7 spreads with plausible Russian copy (D-14).
    3. RITUAL + FAILURE UX (D-07/D-08): in the Mini App, trigger a normal reading and confirm the ~3s ritual covers the real wait with NO spinner and reveal happens only after JSON is ready; then force a failure (e.g. temporarily bad key / induced timeout) and confirm «Колода замолчала…» + Повторить + Сменить колоду (question preserved), and that the limit was NOT consumed (the retry is free).
    4. CRISIS TONE (SAFE-03): submit a crisis-style question; confirm a warm, human, supportive response that fully breaks the mystical frame, points to a real person/specialist (generic, no phone number — D-04), with NO cards drawn and NO charge.
  </how-to-verify>
  <action>This is a manual UAT gate (autonomous: false) — no code is written. The executor runs the env-gated live smoke test, then pauses for the human to perform the four how-to-verify checks. Never auto-approve; tuning (if needed) edits prompt_templates / module-constant timings, not this task.</action>
  <verify>
    <human-check>Human confirms all four acceptance criteria below (per-deck divergence, live-API smoke, ritual/failure UX, crisis tone).</human-check>
  </verify>
  <resume-signal>Type "approved" if all four pass, or describe what felt off (tone/divergence/UX) so it can be tuned (prompt/templates are admin-editable; timings are module constants).</resume-signal>
  <acceptance_criteria>
    - Human confirms: the same question feels meaningfully different across decks with a per-deck signature (Core Value), and no result text contains a banned brand word.
    - Human confirms: the live-API smoke returns valid ReadingOutput + plausible Russian copy.
    - Human confirms: the ritual covers latency with no spinner; failure shows §9.8 + Повторить + Сменить колоду and does not consume the limit.
    - Human confirms: the crisis question reads as genuinely supportive (warm, generic resources, no cards, no charge).
  </acceptance_criteria>
  <done>A human has verified the felt quality, the live contract, the wait/failure UX, and the crisis tone — the subjective bars the automated suite cannot assert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mini App → POST /api/readings | The frontend sends the question + selections + reversals over the apiFetch Bearer seam; the backend is authoritative for cards/limit/identity (the frontend cannot forge a reading). |
| backend reading copy → rendered UI | Generated text reaches the result screen; brand-voice and tone are guaranteed server-side, but the UI must not introduce AI/brand words in its own failure/retry copy. |
| failure response → user | A failed generation must show a soft in-character message, never a raw error, and must keep the user's question + limit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-30 | Tampering | Frontend tries to supply cards/limit/orientation | mitigate | The body sends only question/topic/slugs/reversals; the backend draws (CSPRNG) + checks the limit server-side; the client cannot inject cards (Plan 02/05). |
| T-04-31 | Information Disclosure | A failed POST shows a raw error / leaks detail | mitigate | The backend returns a soft §9.8 200 body; the frontend renders `READING_ERROR` only and never a stack trace; the catch does not surface internals. |
| T-04-32 | (Brand) | Frontend failure/retry copy introduces an AI/brand word | mitigate | New labels live in `reading/copy.ts` and are covered by the SAFE-06 ban-list scan (`copy.test.ts`); no raw inline strings. |
| T-04-33 | Spoofing | Reading requested without a valid session | mitigate | `apiFetch` attaches the Bearer JWT from the session store; the backend `get_current_user` rejects missing/invalid tokens (401) — verified in Plan 05. |
</threat_model>

<verification>
- `cd frontend && pnpm vitest run src/reading/createReading.test.ts src/components/CatalogScreen.test.tsx` → green.
- `cd frontend && pnpm vitest run src/reading/copy.test.ts` → green (new Повторить/Сменить колоду labels pass the SAFE-06 scan).
- `cd frontend && pnpm tsc --noEmit` → 0 errors (no `any` in the mapping).
- Manual UAT checkpoint (Task 3) approved.
</verification>

<success_criteria>
- READ-01: the frontend creates a real reading via `POST /api/readings` through the Bearer seam, mechanically (MockReading shape unchanged).
- READ-11 / D-07: the result renders real per-deck copy; the ritual covers the real latency with no spinner; reveal awaits the promise.
- D-08: a failed generation shows §9.8 + Повторить + Сменить колоду (question preserved), limit not consumed.
- Core Value (manual): the same question feels different per deck with a recognizable signature; crisis tone is supportive; live contract holds.
</success_criteria>

<output>
Create `.planning/phases/04-real-personal-reading-keystone/04-06-SUMMARY.md` when done.
</output>
