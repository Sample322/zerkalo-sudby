---
phase: 04-real-personal-reading-keystone
plan: 02
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - backend/app/services/card_draw.py
  - backend/app/core/brand_guard.py
  - backend/tests/unit/test_card_draw.py
  - backend/tests/unit/test_brand_guard.py
autonomous: true
requirements: [READ-02, READ-11, SAFE-04, SAFE-05]

must_haves:
  truths:
    - "CardDrawService draws exactly `position_count` cards using a cryptographically secure RNG (secrets.SystemRandom), never Python `random` (D-13, §12.5)"
    - "reversals OFF → every drawn card is upright; reversals ON → orientation is ~70% upright / 30% reversed via the CSPRNG (D-13)"
    - "the draw selects from the active deck's deck_cards and joins cards for the universal meaning the prompt later needs; client-supplied cards are ignored (backend-only)"
    - "a backend brand guard mirrors the canonical SAFE-06 ban-list and flags AI/нейросеть/модель/ИИ tokens in any text (READ-11 output guard)"
  artifacts:
    - path: "backend/app/services/card_draw.py"
      provides: "CardDrawService.draw — CSPRNG shuffle + orientation, backend-only"
      contains: "secrets.SystemRandom"
    - path: "backend/app/core/brand_guard.py"
      provides: "backend port of BANNED_BRAND_TOKENS + contains_banned_brand_token()"
      contains: "нейросет"
  key_links:
    - from: "backend/app/services/card_draw.py"
      to: "backend/app/models/deck.py"
      via: "draws from the active deck's deck_cards, joins cards for universal meaning"
      pattern: "DeckCard"
---

<objective>
Build the two pure backend primitives the reading composes on: `CardDrawService` (the backend-only, cryptographically-secure card draw — READ-02 / D-13 / §12.5) and `core/brand_guard.py` (the backend port of the canonical SAFE-06 ban-list, the output guard ReadingService applies to generated text — READ-11 / SAFE-04/05). Both are pure (no LLM, no network), fully unit-tested, and own no files that the parallel Wave-1 plans touch.

Purpose: After this plan the server can draw a fair, unforgeable hand for any deck+spread, and can detect a brand-voice slip in any string — the two deterministic building blocks the orchestration (Plan 05) wires between the safety gate and the LLM call.
Output: `services/card_draw.py`, `core/brand_guard.py`, and the now-implemented `tests/unit/test_card_draw.py` + `tests/unit/test_brand_guard.py` (skips removed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-personal-reading-keystone/04-RESEARCH.md
@.planning/phases/04-real-personal-reading-keystone/04-VALIDATION.md
@backend/app/models/deck.py
@backend/app/models/card.py
@backend/app/models/spread.py
@backend/app/services/catalog.py
@frontend/src/reading/copy.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CardDrawService — backend-only CSPRNG draw + 70/30 orientation</name>
  <files>backend/app/services/card_draw.py, backend/tests/unit/test_card_draw.py</files>
  <read_first>
    - .planning/phases/04-real-personal-reading-keystone/04-RESEARCH.md (Pattern 3 — `secrets.SystemRandom()` shuffle, N by positions, orientation off→upright / on→`reversed if rng.random()<0.30 else upright`; draw from the active deck's deck_cards + join cards for universal meaning; seed/debug_hash is out-of-MVP per Open Question 1 / A5 — no DB column; Anti-Patterns: never `random`; Pitfall 5 — eager-load with selectinload to avoid MissingGreenlet)
    - .planning/REFERENCE-TZ.md §12.4 / §12.5 (card draw is backend-only; CSPRNG; orientation rule; N by positions)
    - backend/app/models/deck.py (DeckCard: deck_id, card_id, is_active — the style rows pinning which (deck,card) exist) + backend/app/models/card.py (Card: meaning_upright/reversed, keywords_upright/reversed — the universal meaning) + backend/app/models/spread.py (SpreadType.card_count, SpreadPosition.position_index/title/prompt_instruction)
    - backend/app/services/catalog.py (the established async service style: select() + selectinload, AsyncSession, no f-string SQL — mirror it)
    - backend/tests/unit/test_card_draw.py (the Wave-0 skipped stub from Plan 01 to implement)
    - CLAUDE.md ("What NOT to Use" → Python `random` for card selection is the documented anti-pattern; use secrets/SystemRandom)
  </read_first>
  <behavior>
    - draw returns exactly `position_count` results (one per spread position); count mismatch is impossible by construction. [test_count_matches_positions]
    - reversals OFF → every result orientation == "upright" regardless of RNG. [test_reversals_off_all_upright]
    - reversals ON → with a deterministic injected/seeded RNG, both "upright" and "reversed" appear; the value domain is exactly {upright, reversed}. [test_reversals_on_produces_both]
    - reversals ON over a large sample → reversed share ≈ 30% within a tolerance band (statistical, seeded so it is stable). [test_reversed_ratio_approx_30]
    - the service uses secrets.SystemRandom / `secrets`, NOT `random.shuffle` / `random.random`. [test_uses_csprng]
  </behavior>
  <action>
    Create `backend/app/services/card_draw.py` with a module-level CSPRNG `_rng = secrets.SystemRandom()` (NEVER `import random` for the draw — CLAUDE.md anti-pattern, §12.5) and an async `draw(session, *, deck_id, spread)` that: eager-loads the active `deck_cards` for `deck_id` (selectinload/explicit select, Pitfall 5) joined to `cards` for the universal `meaning_*`/`keywords_*`; shuffles the pool with `_rng.shuffle(...)`; takes the first `spread.card_count` (== number of positions); assigns each chosen card to a `spread.positions[i]` (ordered by position_index); sets orientation = `"upright"` when `reversals_enabled` is False, else `"reversed" if _rng.random() < 0.30 else "upright"` (D-13, 70/30). Return a list of draw records each carrying the data Plan 05 persists into `reading_cards` (card_id, deck_card_id, position_id, position_index, orientation) plus the joined universal meaning/keywords the PromptEngine needs — define a small frozen dataclass/NamedTuple for the record (no DB write here; ReadingService owns persistence + the transaction). Per RESEARCH A5 there is NO seed/debug_hash column → do not invent one; the immutable reading_cards rows are the durable record. Keep the orientation/ratio logic in a pure injectable-RNG helper so the seeded ratio test is deterministic.
    Implement `backend/tests/unit/test_card_draw.py` (remove the Wave-0 skip): the five behaviours above. For the count/orientation/ratio tests, drive the pure helper with a seeded `random.Random(seed)`-as-injected-rng OR `secrets.SystemRandom` plus a large N + tolerance for the ratio; for `test_uses_csprng`, assert the service module references `secrets.SystemRandom` and does NOT `import random` for the draw (read the module source / inspect the rng object type). DB-touching paths use the `seeded_catalog` fixture (Plan 01) and `pytest.skip` when Postgres is down.
  </action>
  <verify>
    <automated>cd backend && pytest -q tests/unit/test_card_draw.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q tests/unit/test_card_draw.py` exits 0; the Wave-0 skip is removed.
    - `card_draw.py` contains `secrets.SystemRandom` and does NOT contain `import random` / `random.shuffle` / `random.random` for the draw (grep → none).
    - reversals OFF → all upright; ON → both orientations appear and reversed share ≈ 0.30 over a seeded large sample (within tolerance); count always == positions.
    - The service performs no `reading_cards` INSERT (persistence is Plan 05); it returns plain draw records.
    - `ruff check app/services/card_draw.py` → 0.
  </acceptance_criteria>
  <done>The backend draws a fair, unforgeable hand for any deck+spread with correct 70/30 orientation, CSPRNG-verified, returning records Plan 05 will persist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backend brand guard (core/brand_guard.py) — SAFE-06 output ban-list port</name>
  <files>backend/app/core/brand_guard.py, backend/tests/unit/test_brand_guard.py</files>
  <read_first>
    - frontend/src/reading/copy.ts (the canonical `BANNED_BRAND_TOKENS` regex + the Cyrillic-`ИИ` word-boundary logic `(?:^|[^а-яё])ии(?:[^а-яё]|$)` that must NOT false-positive on гармонии/линии/версии — port this exact behaviour to Python)
    - .planning/phases/04-real-personal-reading-keystone/04-RESEARCH.md (Don't Hand-Roll → "port the canonical SAFE-06 ban-list as a backend guard, one source of truth"; Open Question 2 — recommendation is LOG + FLAG, do NOT fail the reading on a brand slip)
    - .planning/REFERENCE-TZ.md §15.1 (allowed vs banned formulations) — context for why the guard exists
    - backend/tests/unit/test_brand_guard.py (the Wave-0 skipped stub from Plan 01 to implement)
  </read_first>
  <behavior>
    - contains_banned_brand_token("сгенерировано ИИ") → True; "это ответ нейросети" → True; "наша модель" → True; "AI reading" → True. [test_detects_banned_tokens]
    - contains_banned_brand_token("в гармонии с собой") → False; "линии судьбы" → False; "несколько версий" → False (the Cyrillic-ИИ boundary does not false-positive). [test_no_false_positive_on_benign_ии]
    - brand-safe RU copy (e.g. "карты подсвечивают возможное направление") → False. [test_brand_safe_copy_passes]
  </behavior>
  <action>
    Create `backend/app/core/brand_guard.py` porting the frontend `BANNED_BRAND_TOKENS` to a Python `re` pattern with the SAME alternatives (`ai|нейросет|модель|сгенерирован` + the Cyrillic-word-boundary `ии` branch) compiled `re.IGNORECASE`, exposing `BANNED_BRAND_TOKENS` (the compiled pattern) and `contains_banned_brand_token(text: str) -> bool`. Match the frontend's word-boundary semantics exactly so benign words containing the «ии» bigram do not false-positive. This is the single backend source-of-truth reused by ReadingService's post-generation output guard (Plan 05). Document at the top that the disposition is LOG + FLAG (do not fail the reading — RESEARCH Open Question 2).
    Implement `backend/tests/unit/test_brand_guard.py` (remove the Wave-0 skip): the three behaviours above, including the benign-word false-positive guards (гармонии / линии / версии) mirroring the frontend `copy.test.ts` cases.
  </action>
  <verify>
    <automated>cd backend && pytest -q tests/unit/test_brand_guard.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q tests/unit/test_brand_guard.py` exits 0; the Wave-0 skip is removed.
    - `brand_guard.py` detects AI/нейросет/модель/сгенерирован and bare/embedded «ИИ» but NOT гармонии/линии/версии.
    - `brand_guard.py` exposes `contains_banned_brand_token` and a compiled `BANNED_BRAND_TOKENS`; module docstring states LOG+FLAG (non-failing) disposition.
    - `ruff check app/core/brand_guard.py` → 0.
  </acceptance_criteria>
  <done>A backend brand guard mirrors the canonical SAFE-06 ban-list (one source of truth, no false positives), ready for ReadingService to flag any generated-text slip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → card selection | A client could try to supply/forge the drawn cards or claim reversals state; the draw must be entirely server-side. |
| generated text → client | Brand-voice violations in model output must be detectable before/after persistence (READ-11). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Tampering | Client forges drawn cards / orientation / reversals state | mitigate | `CardDrawService` is backend-only; draws from the active `deck_cards` server-side; ignores any client-supplied cards (§12.4/§29.2). Plan 05 reads `reversals_enabled` from the validated body only. |
| T-04-13 | Cryptography | Predictable/seeded RNG enables hand prediction | mitigate | `secrets.SystemRandom` (CSPRNG) for both shuffle and the orientation coin; never `random` (§12.5; CLAUDE.md anti-pattern); CSPRNG asserted by `test_uses_csprng`. |
| T-04-14 | Information Disclosure / brand | Model output leaks AI/brand wording to the user | mitigate | Backend brand guard (port of the canonical SAFE-06 ban-list) flags violations; Plan 05 logs+flags on generated text (READ-11). |
</threat_model>

<verification>
- `cd backend && pytest -q tests/unit/test_card_draw.py tests/unit/test_brand_guard.py` → green (skips removed).
- Grep `app/services/card_draw.py` for `import random` / `random.shuffle` / `random.random` → none; `secrets.SystemRandom` present.
- `cd backend && ruff check app/services/card_draw.py app/core/brand_guard.py` → 0.
</verification>

<success_criteria>
- READ-02 / D-13: CSPRNG draw, count == positions, reversals off→upright / on→70/30; CSPRNG verified.
- READ-11 / SAFE-04/05: a backend brand guard mirrors the SAFE-06 ban-list and flags AI/brand tokens without false-positiving benign «ии» words.
</success_criteria>

<output>
Create `.planning/phases/04-real-personal-reading-keystone/04-02-SUMMARY.md` when done.
</output>
