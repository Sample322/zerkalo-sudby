---
phase: 04-real-personal-reading-keystone
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/app/core/llm_client.py
  - backend/app/schemas/reading.py
  - backend/app/schemas/__init__.py
  - backend/tests/unit/test_card_draw.py
  - backend/tests/unit/test_reading_schema.py
  - backend/tests/unit/test_safety_routing.py
  - backend/tests/unit/test_brand_guard.py
  - backend/tests/integration/test_readings_auth.py
  - backend/tests/integration/test_readings_flow.py
  - backend/tests/integration/test_readings_limit.py
  - backend/tests/integration/test_safety_gate.py
  - backend/tests/integration/test_generation_logs.py
  - backend/tests/integration/conftest.py
autonomous: false
requirements: [READ-03, READ-05, READ-06, SAFE-01]

must_haves:
  truths:
    - "`anthropic` and `tenacity` are in backend/pyproject.toml and importable; the human legitimacy checkpoint approved both before install"
    - "`schemas/reading.py` defines the fused single-call output contract (ReadingOutput/CardInterpretation/ReadingSummary), the classify contract (SafetyCategory enum + SafetyVerdict), and the request/response contract (ReadingCreate/ReadingOut) — D-10/D-11 length lives in field descriptions, not max_length"
    - "All Wave-0 test files exist and collect (skipped stubs assert the future behaviour); `pytest -q` collects 0 errors"
    - "tests/integration/conftest.py provides fake_llm / fake_safety injectables and a seeded-catalog fixture so no test hits Anthropic or requires manual seed"
  artifacts:
    - path: "backend/app/schemas/reading.py"
      provides: "ReadingOutput/CardInterpretation/ReadingSummary (fused §17+§18), SafetyCategory/SafetyVerdict, ReadingCreate/ReadingOut"
      contains: "class ReadingOutput"
    - path: "backend/app/core/llm_client.py"
      provides: "module-level anthropic.AsyncAnthropic() singleton reading ANTHROPIC_API_KEY"
      contains: "AsyncAnthropic"
    - path: "backend/tests/integration/conftest.py"
      provides: "fake_llm / fake_safety fixtures + seeded deck/spread/cards fixture"
      contains: "fake_llm"
  key_links:
    - from: "backend/app/core/llm_client.py"
      to: "backend/app/core/config.py"
      via: "AsyncAnthropic reads ANTHROPIC_API_KEY (already a required secret)"
      pattern: "ANTHROPIC_API_KEY"
    - from: "backend/tests/integration/conftest.py"
      to: "backend/app/seed/loader.py"
      via: "seeded-catalog fixture reuses run_seed so the flow has real catalog rows to draw from"
      pattern: "run_seed"
---

<objective>
Lay the Wave-0 foundation the whole keystone composes on: add the two MISSING locked dependencies (`anthropic`, `tenacity`) behind a blocking legitimacy checkpoint, define the single-call + classify + request/response schema contracts in `schemas/reading.py`, create the `AsyncAnthropic` client singleton, and create every Wave-0 test stub + the shared `fake_llm`/`fake_safety`/seeded-catalog fixtures from 04-VALIDATION.md. This is the interface-first wave: every later plan (02-06) imports these contracts and runs against these fixtures, never the real Anthropic API.

Purpose: Establish the contracts and the test substrate ONCE so the feature waves are deterministic, mockable, and never hit the network. The packages are the single hard prerequisite that blocks execution (04-RESEARCH Environment Availability).
Output: `pyproject.toml` (+2 deps), `core/llm_client.py`, `schemas/reading.py`, 9 skipped test stubs, and the extended `tests/integration/conftest.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-personal-reading-keystone/04-RESEARCH.md
@.planning/phases/04-real-personal-reading-keystone/04-VALIDATION.md
@backend/pyproject.toml
@backend/app/core/config.py
@backend/app/models/reading.py
@backend/tests/conftest.py
@backend/tests/integration/conftest.py
@backend/app/seed/loader.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy checkpoint + add anthropic + tenacity to pyproject</name>
  <files>backend/pyproject.toml</files>
  <read_first>
    - .planning/phases/04-real-personal-reading-keystone/04-RESEARCH.md (Package Legitimacy Audit — both [OK]/[VERIFIED] via slopcheck; Standard Stack → Installation block with the exact dependency lines and floors)
    - backend/pyproject.toml (the aiogram precedent comment in [project].dependencies — the established "human-approved addition during the Package Legitimacy checkpoint" pattern this mirrors)
  </read_first>
  <what-built>
    Both packages are already slopcheck-clean ([OK] pypi), registry-confirmed, locally resolvable (anthropic 0.109.1, tenacity 9.1.4), and `anthropic` is the CLAUDE.md-locked provider. Per the Phase-1 aiogram precedent the dependency edit is gated behind this checkpoint so the lock is established once and stays consistent.
  </what-built>
  <how-to-verify>
    1. Confirm `anthropic` on PyPI: open https://pypi.org/project/anthropic/ — official Anthropic SDK, github.com/anthropics/anthropic-sdk-python.
    2. Confirm `tenacity` on PyPI: open https://pypi.org/project/tenacity/ — github.com/jd/tenacity, industry-standard retry library.
    3. On approval, add to `backend/pyproject.toml` `[project].dependencies` (with an aiogram-style provenance comment): `"anthropic>=0.69,<1",` and `"tenacity>=9,<10",`.
    4. Install into the backend env (`uv pip install -e ".[dev]"` or the project's documented installer) and verify `python -c "import anthropic, tenacity"` exits 0.
  </how-to-verify>
  <action>After human approval, add `"anthropic>=0.69,<1"` and `"tenacity>=9,<10"` to `backend/pyproject.toml` `[project].dependencies` with a provenance comment mirroring the aiogram precedent, then install via the project installer. This is a blocking-human legitimacy gate — never auto-approve.</action>
  <verify>
    <automated>cd backend && python -c "import anthropic, tenacity; from anthropic import AsyncAnthropic"</automated>
  </verify>
  <resume-signal>Type "approved" to add + install, or describe a concern.</resume-signal>
  <acceptance_criteria>
    - `backend/pyproject.toml` `[project].dependencies` contains `anthropic>=0.69,<1` and `tenacity>=9,<10` with a provenance comment.
    - `cd backend && python -c "import anthropic, tenacity"` exits 0.
    - `cd backend && python -c "from anthropic import AsyncAnthropic"` exits 0 (the async client the LLM layer needs exists).
  </acceptance_criteria>
  <done>Both locked dependencies are human-approved, added to pyproject with the floors from RESEARCH, and importable.</done>
</task>

<task type="auto">
  <name>Task 2: Schema contracts (schemas/reading.py) + AsyncAnthropic client singleton (core/llm_client.py)</name>
  <files>backend/app/schemas/reading.py, backend/app/schemas/__init__.py, backend/app/core/llm_client.py, backend/tests/unit/test_reading_schema.py</files>
  <read_first>
    - .planning/phases/04-real-personal-reading-keystone/04-RESEARCH.md (Pattern 1 — the exact ReadingOutput/CardInterpretation/ReadingSummary field set + descriptions + the DB-mapping table; Pattern 2 — SafetyCategory StrEnum (the 7 §20.4 members) + SafetyVerdict; Pitfall 1 — min/maxLength are STRIPPED so length MUST live in the description; Code Examples → AsyncAnthropic singleton)
    - .planning/REFERENCE-TZ.md §14.5 (ReadingCreate request: question, topic, deck_slug, spread_slug, reversals_enabled; response: reading_id, status, selected_cards, interpretations, summary, remaining_limits), §17 (per-card fields), §18 (summary fields), §20.4 (the 7 classifier categories)
    - backend/app/models/reading.py (the LOCKED columns the output maps onto: reading_cards.short_meaning/interpretation/mystical_accent; readings.summary_short/summary_full/main_factor/advice)
    - backend/app/models/enums.py (Orientation/ReadingStatus value style — StrEnum with lowercase slug values; mirror for SafetyCategory)
    - backend/app/schemas/catalog.py (the established Pydantic v2 from_attributes projection style to mirror for ReadingOut)
    - backend/app/core/config.py (ANTHROPIC_API_KEY is already a required secret — the client reads it from env)
    - frontend/src/reading/types.ts (MockReadingCard/MockReadingSummary field names ReadingOut must carry so the Phase-6 seam mapping is mechanical: linkage/mainFactor/attention/softAdvice/closingPhrase)
  </read_first>
  <behavior>
    - ReadingOutput(cards=[CardInterpretation(...)], summary=ReadingSummary(...)) constructs and `ReadingOutput.model_validate_json(good_json)` round-trips. [test_reading_output_roundtrip]
    - `ReadingOutput.model_validate_json(bad_shape)` (missing required field / wrong type) raises pydantic.ValidationError. [test_reading_output_rejects_bad_shape]
    - The generated JSON schema for `short_meaning` carries the "до 140 символов" hint in its description and does NOT carry a maxLength constraint (Pitfall 1). [test_length_lives_in_description_not_constraint]
    - SafetyVerdict(category="crisis_sensitive") validates; SafetyCategory has exactly the 7 §20.4 members. [test_safety_category_members]
  </behavior>
  <action>
    Create `backend/app/schemas/reading.py` per RESEARCH Pattern 1 + Pattern 2. LLM output contract: `CardInterpretation` with fields `position_index:int`, `short_meaning:str`, `interpretation:str`, `mystical_accent:str`, `soft_advice:str` — every field a `Field(description=...)` whose RU description encodes the §17 length/shape (short_meaning "до 140 символов", interpretation "2–3 коротких предложения", D-10/D-11). `ReadingSummary` with `summary_short`, `connection`, `main_factor`, `attention_point`, `advice`, `closing_phrase` (RU descriptions per §18). `ReadingOutput` with `cards: list[CardInterpretation]` (description "ровно по одной на каждую выпавшую карту, в порядке позиций") + `summary: ReadingSummary`. Zero optional fields, zero unions (within the SO budget). Do NOT use `max_length`/`min_length` (stripped — Pitfall 1; length is description-only). Classify contract: `SafetyCategory(enum.StrEnum)` with the 7 §20.4 members (normal, relationship_sensitive, financial_sensitive, health_sensitive, legal_sensitive, crisis_sensitive, abusive_or_manipulative) and `SafetyVerdict` with `category: SafetyCategory`. Request/response: `ReadingCreate` (§14.5 — question: str | None with the HOME-01 10–500 / HOME-02 empty-allowed validation, topic: str, deck_slug: str, spread_slug: str, reversals_enabled: bool) and `ReadingOut` (from_attributes; reading_id, status, plus the per-card list carrying name/positionTitle/orientation/shortMeaning/interpretation/deckAccent + all five summary fields named to mirror the frontend MockReading shape, plus remaining_limits) so Plan 06's mapping is mechanical. Export everything from `schemas/__init__.py`.
    Create `backend/app/core/llm_client.py`: a module-level `client = anthropic.AsyncAnthropic()` singleton (reads ANTHROPIC_API_KEY from env via the SDK default; do NOT pass the key explicitly or log it — V8). Use `anthropic.AsyncAnthropic` (async — Pitfall 6), never the sync client.
    Create `backend/tests/unit/test_reading_schema.py` implementing the four behaviours above (no skip — this stub is born green since the schema ships in this task). Inspect the JSON schema via `ReadingOutput.model_json_schema()` for the description/constraint assertions.
  </action>
  <verify>
    <automated>cd backend && pytest -q tests/unit/test_reading_schema.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q tests/unit/test_reading_schema.py` exits 0.
    - `schemas/reading.py` contains `class ReadingOutput`, `class SafetyVerdict`, `class ReadingCreate`, `class ReadingOut` and a `SafetyCategory` with 7 members.
    - No `max_length`/`min_length` on any LLM-output field (grep `reading.py` for `max_length` → none on CardInterpretation/ReadingSummary); the 140-char target is in the field description.
    - `core/llm_client.py` contains `AsyncAnthropic` and does NOT contain a hardcoded key or `print`.
    - `cd backend && python -c "from app.schemas.reading import ReadingOutput, SafetyVerdict, ReadingCreate, ReadingOut, SafetyCategory"` exits 0.
  </acceptance_criteria>
  <done>The single-call, classify, and request/response contracts exist and validate; the AsyncAnthropic singleton reads the env key. Every later plan imports from here.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 test stubs (9 files) + fake_llm / fake_safety / seeded-catalog fixtures in tests/integration/conftest.py</name>
  <files>backend/tests/integration/conftest.py, backend/tests/unit/test_card_draw.py, backend/tests/unit/test_safety_routing.py, backend/tests/unit/test_brand_guard.py, backend/tests/integration/test_readings_auth.py, backend/tests/integration/test_readings_flow.py, backend/tests/integration/test_readings_limit.py, backend/tests/integration/test_safety_gate.py, backend/tests/integration/test_generation_logs.py</files>
  <read_first>
    - .planning/phases/04-real-personal-reading-keystone/04-VALIDATION.md (Wave 0 Requirements list — the exact 9 files + the conftest extension + the Req→Test map giving each file's target behaviour and command)
    - .planning/phases/04-real-personal-reading-keystone/04-RESEARCH.md (Code Examples → "Mocking the LLM in tests" — fake_reading_output fixture + inject LLMService/SafetyService so no network call; Wave 0 Gaps)
    - backend/tests/conftest.py (root fixtures already present: client, db_session, _db_ready, redis_client, make_init_data, SAMPLE_USER — DO NOT redefine; build on them; note the Phase-1 skipped-stub pattern these stubs mirror)
    - backend/tests/integration/conftest.py (the existing auth_session/auth_client transaction-isolation recipe to reuse for the readings flow)
    - backend/app/seed/loader.py (run_seed — reuse to seed the catalog fixture so the flow has real deck/spread/cards rows to draw from)
    - backend/app/schemas/reading.py (the contracts the stubs reference — from Task 2)
  </read_first>
  <action>
    Extend `backend/tests/integration/conftest.py` (do NOT touch root conftest): add (1) a `fake_reading_output` fixture returning a fully-populated brand-safe `ReadingOutput` (RU placeholder copy, one card per a 3-card spread); (2) a `fake_llm` fixture — an injectable stand-in for the future `LLMService` whose `generate(...)` returns `fake_reading_output` by default and can be parametrized to raise `pydantic.ValidationError` (honest-fail / corrective-retry paths); (3) a `fake_safety` fixture — an injectable stand-in for the future `SafetyService` whose `classify(...)` returns a `SafetyVerdict` (default `normal`, parametrizable to crisis/abusive/sensitive); (4) a `seeded_catalog` fixture that runs `run_seed` against the transaction-isolated session (or `pytest.skip` when Postgres is down, mirroring `_db_ready`) so a real deck/spread/cards set exists. Keep fixtures injectable via `app.dependency_overrides` so Plan 05 substitutes them for the real services.
    Create the 9 test files as SKIPPED stubs (Phase-1 Wave-0 pattern: `@pytest.mark.skip(reason="Wave 0 stub — implemented in Plan 04-0X")`) with a docstring naming the requirement + the exact behaviour each will assert (copy the Behavior column from 04-VALIDATION.md). Files and their owning plan: `tests/unit/test_card_draw.py` (READ-02 → Plan 02), `tests/unit/test_safety_routing.py` (SAFE-01/02/04/05 → Plan 03), `tests/unit/test_brand_guard.py` (SAFE-06 backend guard → Plan 02), `tests/integration/test_readings_auth.py` (READ-01 → Plan 05), `tests/integration/test_readings_flow.py` (success/honest-fail/corrective-retry → Plan 05), `tests/integration/test_readings_limit.py` (READ-10 → Plan 05), `tests/integration/test_safety_gate.py` (SAFE-03/D-06 gate-before-draw → Plan 05), `tests/integration/test_generation_logs.py` (ANALYTICS-02 → Plan 05). Each later plan removes its own skip and implements the body. The stubs must COLLECT cleanly (import the contracts, no syntax/import errors).
  </action>
  <verify>
    <automated>cd backend && pytest -q --collect-only tests/unit/test_card_draw.py tests/unit/test_safety_routing.py tests/unit/test_brand_guard.py tests/integration/test_readings_auth.py tests/integration/test_readings_flow.py tests/integration/test_readings_limit.py tests/integration/test_safety_gate.py tests/integration/test_generation_logs.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q --collect-only` over the 8 stub files exits 0 (all collect; no import/syntax errors).
    - `cd backend && pytest -q` exits 0 with the new stubs reported as skipped (not failed) and zero collection errors.
    - `tests/integration/conftest.py` defines `fake_llm`, `fake_safety`, `fake_reading_output`, and `seeded_catalog`; none hit Anthropic.
    - Each stub docstring names its requirement ID and its owning Plan number; each stub carries `@pytest.mark.skip`.
    - The root `tests/conftest.py` is unchanged (grep diff — only `tests/integration/conftest.py` extended).
  </acceptance_criteria>
  <done>All 9 Wave-0 test files exist and collect; the fake_llm/fake_safety/seeded_catalog fixtures are in place so the feature waves are fully mockable and never touch the network. The Nyquist substrate for Plans 02-06 is ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dependency supply chain → backend env | Two new third-party packages enter the build; an unvetted/typosquat package would be a supply-chain risk. |
| env → AsyncAnthropic client | `ANTHROPIC_API_KEY` crosses into the SDK; it must never be hardcoded, logged, or echoed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | npm/pip install of `anthropic` + `tenacity` | mitigate | Blocking human-verify legitimacy checkpoint (Task 1) verifying both on PyPI before install; both already slopcheck `[OK]`/registry-confirmed/locally resolvable; `anthropic` is the CLAUDE.md-locked provider. Checkpoint is never auto-approved. |
| T-04-10 | Information Disclosure | `ANTHROPIC_API_KEY` exposure via client init / logs | mitigate | `AsyncAnthropic()` reads the key from env via the SDK default (no explicit pass, no log); the key is already a required no-default secret (config.py); never echoed in responses. |
| T-04-11 | Tampering | LLM output schema accepts a malformed object | mitigate | `ReadingOutput` is a strict Pydantic model; `model_validate_json` raises on bad shape (asserted in Task 2) — the validation seam Plan 03's retry trigger depends on. |
</threat_model>

<verification>
- `cd backend && python -c "import anthropic, tenacity; from anthropic import AsyncAnthropic"` exits 0.
- `cd backend && pytest -q tests/unit/test_reading_schema.py` → green.
- `cd backend && pytest -q` → green with the 8 new stubs skipped, 0 collection errors.
- `cd backend && ruff check app/schemas/reading.py app/core/llm_client.py` → 0.
- Grep `app/core/llm_client.py` + `app/schemas/reading.py` for a literal API key or `print(` → none.
</verification>

<success_criteria>
- READ-03/05/06: the fused single-call `ReadingOutput`/`CardInterpretation`/`ReadingSummary` contract exists, round-trips, and rejects bad shapes; length lives in descriptions (Pitfall 1).
- SAFE-01: the `SafetyCategory`/`SafetyVerdict` classify contract exists with the 7 §20.4 categories.
- Dependencies: `anthropic` + `tenacity` are human-approved, pinned in pyproject, and importable (the single hard prerequisite cleared).
- Nyquist: all 9 Wave-0 test files exist + collect; `fake_llm`/`fake_safety`/`seeded_catalog` fixtures ready; nothing hits the network.
</success_criteria>

<output>
Create `.planning/phases/04-real-personal-reading-keystone/04-01-SUMMARY.md` when done.
</output>
