---
phase: 06-free-limits-soft-paywall
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/0002_user_limits_rolling_window.py
  - backend/app/models/billing.py
  - backend/app/schemas/auth.py
  - backend/app/services/telegram_auth.py
  - backend/tests/integration/conftest.py
  - backend/tests/integration/test_limit_concurrency.py
  - backend/tests/integration/test_limits_reset.py
  - backend/tests/integration/test_throttle.py
  - backend/tests/unit/test_determine_access.py
  - backend/tests/integration/test_paywall_block.py
  - backend/tests/integration/test_auth_flow.py
autonomous: false
requirements: [LIMIT-01, LIMIT-02]
must_haves:
  truths:
    - "Every authenticated user has a user_limits row immediately after auth (no authed user is ever unbounded — fixes the Phase-4 'no row → unlimited' gap, D-02)"
    - "A brand-new user's user_limits row is created with week_start=NULL (anchors on the first reading, NOT an ISO-Monday date), free_weekly_limit=3, free_used_this_week=0 (D-01/D-02)"
    - "Two concurrent first-logins for the same brand-new user create exactly ONE user_limits row (race-safe ON CONFLICT, relies on the new UNIQUE constraint, A2)"
    - "week_start stores a timezone-aware TIMESTAMP (not a DATE) so the rolling 7-day window is hour-accurate (A1/Pitfall 1)"
    - "A committed two-independent-connection test fixture exists so the boundary-race atomicity proof (Plan 02) can exercise true cross-connection PostgreSQL row locks (Pitfall 3)"
    - "Red stub test files exist for LIMIT-02/03/04/05 + the paywall/refund behavior, importing the symbols Plans 02/03 will create (xfail until those plans land)"
  artifacts:
    - path: "backend/alembic/versions/0002_user_limits_rolling_window.py"
      provides: "Alembic revision: week_start DATE→TIMESTAMP(timezone=True) + UNIQUE constraint on user_limits.user_id, fully reversible"
      contains: "def upgrade"
    - path: "backend/app/models/billing.py"
      provides: "UserLimits.week_start typed Mapped[datetime | None] (TIMESTAMP) + UniqueConstraint on user_id"
      contains: "week_start"
    - path: "backend/app/services/telegram_auth.py"
      provides: "Race-safe user_limits row-ensure via ON CONFLICT DO NOTHING with week_start=NULL; _current_week_start helper removed"
      contains: "on_conflict_do_nothing"
    - path: "backend/tests/integration/conftest.py"
      provides: "committed-seed fixture + two-independent-AsyncSession factory for true concurrency"
      contains: "SessionLocal"
    - path: "backend/tests/integration/test_limit_concurrency.py"
      provides: "LIMIT-03 boundary-race red stub (two committed sessions + asyncio.gather)"
      contains: "asyncio.gather"
    - path: "backend/tests/unit/test_determine_access.py"
      provides: "LIMIT-04 determine_access bucket-order red stub (pure fn)"
      contains: "determine_access"
  key_links:
    - from: "backend/app/services/telegram_auth.py"
      to: "user_limits row (week_start NULL)"
      via: "INSERT ... ON CONFLICT (user_id) DO NOTHING at the user upsert"
      pattern: "on_conflict_do_nothing"
    - from: "backend/alembic/versions/0002_user_limits_rolling_window.py"
      to: "user_limits.week_start column"
      via: "alter_column type_ change DATE→TIMESTAMP"
      pattern: "alter_column"
---

<objective>
Lay the Phase-6 foundation the atomic-consume and throttle slices stand on: the schema migration (A1+A2, USER-APPROVED override of the CONTEXT "no schema change" assumption), the D-02 race-safe `user_limits` row at auth (closing the Phase-4 "no row → unlimited" gap), and the Wave-0 nyquist test substrate — including the committed two-connection concurrency fixture that the existing savepoint harness cannot provide (research Pitfall 3).

Purpose: Plan 02's atomic `UPDATE … RETURNING` re-anchors `week_start = now` and checks `now − week_start ≥ 7d`, which needs sub-day precision (A1) and a UNIQUE target for the auth `ON CONFLICT` (A2). Both MUST land before Plans 02/03. The concurrency fixture is the single most load-bearing test asset in the phase — without it, success-criterion 3 cannot be proven.

Output: One reversible Alembic revision, an updated `UserLimits` model + `LimitsOut` schema, a race-safe `_ensure_user_limits`, and six red/extended test files + two new fixtures that the later plans turn green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-free-limits-soft-paywall/06-CONTEXT.md
@.planning/phases/06-free-limits-soft-paywall/06-RESEARCH.md
@.planning/phases/06-free-limits-soft-paywall/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic revision 0002 — week_start DATE→TIMESTAMP + UNIQUE(user_id), model + schema</name>
  <files>backend/alembic/versions/0002_user_limits_rolling_window.py, backend/app/models/billing.py, backend/app/schemas/auth.py</files>
  <read_first>
    - backend/alembic/versions/0001_initial_schema.py (the migration head: `revision = "0001_initial_schema"`, `down_revision = None`; the hand-written-not-autogenerate convention because no live DB in the build env; line 411 `week_start sa.Date()`, line 431 `ix_user_limits_user_id` non-unique index, lines 549-550 downgrade order — mirror this style)
    - backend/app/models/billing.py (UserLimits: `week_start: Mapped[date | None]` line 51, the `date` import line 22, the `index=True` on user_id line 43)
    - backend/app/schemas/auth.py (LimitsOut: `week_start: date | None` line 56, the `date` import line 13)
    - .planning/phases/06-free-limits-soft-paywall/06-RESEARCH.md (Pitfall 1 + Assumptions A1/A2 + Runtime State Inventory: existing DATE values cast to midnight-timestamp cleanly, self-heal on first lazy reset, no data backfill)
  </read_first>
  <action>
    Create `backend/alembic/versions/0002_user_limits_rolling_window.py` with `revision = "0002_user_limits_rolling_window"`, `down_revision = "0001_initial_schema"`, hand-written (no autogenerate — no live DB), fully reversible. In `upgrade()`: (1) `op.alter_column("user_limits", "week_start", type_=sa.TIMESTAMP(timezone=True), existing_type=sa.Date(), existing_nullable=True, postgresql_using="week_start::timestamptz")` — the `postgresql_using` cast makes existing ISO-Monday DATE values become midnight timestamptz cleanly (self-heal on first lazy reset, per A1); (2) `op.create_unique_constraint("uq_user_limits_user_id", "user_limits", ["user_id"])` (A2 — required for the auth `ON CONFLICT (user_id)`). In `downgrade()` (reverse order): drop the unique constraint, then `alter_column` back to `sa.Date()` with `postgresql_using="week_start::date"`. Then update `backend/app/models/billing.py`: change `week_start` to `Mapped[datetime | None] = mapped_column(sa.TIMESTAMP(timezone=True), nullable=True)` (import `datetime`, drop the now-unused `date` import if no longer used), and add `__table_args__ = (UniqueConstraint("user_id", name="uq_user_limits_user_id"),)` (import `UniqueConstraint` from sqlalchemy) — keep the existing `index=True` mapping or fold it into the constraint, but the migration's actual constraint name MUST match. Update `backend/app/schemas/auth.py` `LimitsOut.week_start` to `datetime | None` (import `datetime`). Do NOT add `paid_spreads_balance`/subscription column changes — they are unchanged this phase. Confirm imports resolve with `cd backend && uv run python -c "import app.models.billing, app.schemas.auth"`.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from app.models.billing import UserLimits; from app.schemas.auth import LimitsOut; import inspect; src=inspect.getsource(UserLimits); assert 'TIMESTAMP' in src or 'timestamp' in src.lower(), 'week_start not TIMESTAMP'; assert 'uq_user_limits_user_id' in src or 'UniqueConstraint' in src, 'no unique constraint'; print('OK model+schema')"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/alembic/versions/0002_user_limits_rolling_window.py` exists with `down_revision = "0001_initial_schema"` and both `upgrade()` and `downgrade()` defined.
    - `upgrade()` contains an `alter_column` on `week_start` to a `TIMESTAMP(timezone=True)` with a `postgresql_using` cast, and a `create_unique_constraint` named `uq_user_limits_user_id` on `["user_id"]`.
    - `UserLimits.week_start` is `Mapped[datetime | None]` and the model declares the `uq_user_limits_user_id` unique constraint.
    - `LimitsOut.week_start` is typed `datetime | None`.
    - The verify command prints `OK model+schema` (imports resolve, the new type + constraint are present in the model source).
    - The migration is NOT applied here (no Docker in agent env) — applying it (`alembic upgrade head`) is documented as a user-smoke in the SUMMARY.
  </acceptance_criteria>
  <done>Revision 0002 authored + reversible, UserLimits.week_start is TIMESTAMP with a UNIQUE(user_id) constraint, LimitsOut.week_start is datetime, all imports resolve.</done>
</task>

<task type="auto">
  <name>Task 2: D-02 — race-safe user_limits row at auth with week_start=NULL</name>
  <files>backend/app/services/telegram_auth.py</files>
  <read_first>
    - backend/app/services/telegram_auth.py (the WHOLE file: `_ensure_user_limits` SELECT-then-INSERT lines 151-172, `_current_week_start()` ISO-Monday helper lines 94-97, `_FREE_WEEKLY_LIMIT = 3` line 43, the `pg_insert` already imported line 30, the existing `on_conflict_do_update` user upsert lines 115-141 as the ON CONFLICT idiom to mirror, `get_user_limits` lines 175-181 which stays)
    - backend/app/models/billing.py (UserLimits columns — server defaults `free_weekly_limit=3`, `free_used_this_week=0` already exist; week_start now nullable TIMESTAMP after Task 1)
    - .planning/phases/06-free-limits-soft-paywall/06-RESEARCH.md (Pattern 5: replace SELECT-then-INSERT with INSERT … ON CONFLICT (user_id) DO NOTHING; "Also adjust for D-02": week_start=NULL, delete _current_week_start)
  </read_first>
  <action>
    Rewrite `_ensure_user_limits` in `backend/app/services/telegram_auth.py` to a single race-safe `pg_insert(UserLimits).values(user_id=user_id, free_weekly_limit=_FREE_WEEKLY_LIMIT, free_used_this_week=0).on_conflict_do_nothing(index_elements=["user_id"])` — relying on the `uq_user_limits_user_id` constraint from Task 1 (D-02 / A2). Deliberately OMIT `week_start` from `.values()` so it defaults to NULL (D-01 anchors on first reading, NOT an ISO-Monday date). Because `ON CONFLICT DO NOTHING` returns no row when the row already exists, do NOT use `.returning()` for the row; the function's contract becomes "ensure the row exists" (return `None` or keep a follow-up `get_user_limits` read only if a caller needs the row — the auth flow calls `session.refresh(user)` separately and reads limits via `get_user_limits` for the response, so a returning value is not required). Delete the now-unused `_current_week_start()` helper and its `date`/`timedelta` imports if they become unused (it encodes the overridden ISO-week model — research "Deprecated/outdated"). Keep `authenticate()` calling `await _ensure_user_limits(session, user.id)` before `session.commit()`. Confirm `cd backend && uv run python -c "import app.services.telegram_auth"` and `uv run ruff check app/services/telegram_auth.py`.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "import inspect, app.services.telegram_auth as t; src=inspect.getsource(t); assert 'on_conflict_do_nothing' in src, 'not race-safe'; assert '_current_week_start' not in src, 'ISO-week helper still present'; print('OK auth upsert')" && uv run ruff check app/services/telegram_auth.py</automated>
  </verify>
  <acceptance_criteria>
    - `_ensure_user_limits` uses `pg_insert(...).on_conflict_do_nothing(index_elements=["user_id"])` (no SELECT-then-INSERT).
    - `week_start` is NOT passed in `.values()` (the row is created with `week_start = NULL`).
    - `_current_week_start()` is deleted and no `date`/`timedelta` import remains unused.
    - `authenticate()` still ensures the row before commit.
    - `ruff check` passes on the file; the verify command prints `OK auth upsert`.
  </acceptance_criteria>
  <done>A brand-new user gets a user_limits row via race-safe ON CONFLICT DO NOTHING with week_start=NULL; the ISO-week helper is gone; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 substrate — committed two-connection fixture + 6 red/extended test files</name>
  <files>backend/tests/integration/conftest.py, backend/tests/integration/test_limit_concurrency.py, backend/tests/integration/test_limits_reset.py, backend/tests/integration/test_throttle.py, backend/tests/unit/test_determine_access.py, backend/tests/integration/test_paywall_block.py, backend/tests/integration/test_auth_flow.py</files>
  <read_first>
    - backend/tests/integration/conftest.py (the WHOLE file: `auth_session` savepoint fixture lines 36-51 — the one that CANNOT do cross-connection concurrency (Pitfall 3); `seeded_catalog` lines 215-232 reusing `run_seed` + `_ensure_deck_cards`; `FakeLLM`/`FakeSafety` lines 116-168; `fake_reading_output` lines 82-113)
    - backend/tests/integration/test_readings_limit.py (the WHOLE file: the 4 `test_limit_untouched_on_*` tests + `test_limit_consumed_once_on_success`; the `_make_user(auth_session, free_used=N)` / `_REQ` / `_spread_position_indices` / `_output_for_indices` helpers imported from test_readings_flow — these MUST still pass under Plan 02's consume-as-gate+refund order)
    - backend/app/core/db.py (the engine + the session factory name — confirm whether `async_sessionmaker`/`SessionLocal` is exported; the committed fixture needs an independent-connection session maker)
    - backend/tests/conftest.py (the root `_db_ready` skip fixture so the new committed fixture + tests skip cleanly without Postgres)
    - .planning/phases/06-free-limits-soft-paywall/06-VALIDATION.md (Per-Task Verification Map rows + Wave 0 Requirements list + the concurrency-test design spec) and 06-RESEARCH.md "The concurrency test (the load-bearing one — design spec)" + "Phase Requirements → Test Map"
  </read_first>
  <action>
    Add to `backend/tests/integration/conftest.py`: (a) a `committed_seeded_catalog` fixture — a sibling of `seeded_catalog` that opens its OWN session via the app's session factory (import the real `async_sessionmaker`/engine from `app.core.db`; if no `SessionLocal` is exported, build one with `async_sessionmaker(engine, expire_on_commit=False)`), runs `run_seed` + `_ensure_deck_cards`, and `commit`s so two independent connections both see the rows; it MUST register an explicit teardown that deletes what it seeded (no outer savepoint rollback can clean cross-connection commits); skip via `_db_ready`; (b) a `two_committed_sessions` factory/helper that yields a callable opening independent `AsyncSession`s on real connections (for `asyncio.gather`). Then create the red/extended test files exactly per VALIDATION.md, each importing the symbols Plans 02/03 will create and marking them `@pytest.mark.xfail(strict=False, reason="Plan 06-02/03 implements")` (so they xpass when the implementation lands) and skip-if-PG/Redis-down:
    - `backend/tests/integration/test_limit_concurrency.py` — `test_two_concurrent_at_boundary_only_one_succeeds`: arrange a COMMITTED user at `free_used=2, free_weekly_limit=3, week_start=now`, `asyncio.gather` two real `create_reading` calls each on its own committed session, assert `sorted(statuses) == ["completed","failed"]` and the final `free_used_this_week == 3` (NEVER 4 — the atomicity proof). Add a comment block documenting the mutation-test step (break `WHERE free_used < limit` then expect `used == 4`).
    - `backend/tests/integration/test_limits_reset.py` — `test_reset_on_stale_window` (week_start = now−7d → used resets to 1, week_start re-anchors ≈ now), `test_no_reset_within_window` (week_start = now−3d, used=3 → still blocked, reset does NOT fire), `test_first_reading_anchors` (week_start IS NULL, D-02 → anchors, used=1). Import `ReadingService` (the consume path) — these drive the real service.
    - `backend/tests/integration/test_throttle.py` — `test_burst_blocked` (the (cap+1)th rapid call → 429 before PG/LLM), `test_window_expires`, `test_throttle_short_circuits_before_pg` (`fake_llm.calls == 0`, no reading row). Import the throttle symbol Plan 03 will create (e.g. `from app.api.deps import throttle_gate` or `from app.core.redis import throttle_ok`); use a real `redis_client` and skip if Redis is down.
    - `backend/tests/unit/test_determine_access.py` — `test_free_when_room_or_stale`, `test_bucket_order` (with mocked sub/paid>0 → SUBSCRIPTION before PAID, the Phase-7 seam), `test_none_when_exhausted`. Import `from app.services.reading import determine_access, Bucket` (Plan 02 creates these). Pure fn — no DB, no xfail-skip needed beyond the import-guard xfail.
    - `backend/tests/integration/test_paywall_block.py` — `test_paywall_on_exhausted` (4th in a fresh window → soft body with the new `reason`/`reset_at` fields, no draw) + `test_refund_on_honest_fail` (consume-as-gate then LLM exhaustion → counter net unchanged). Import `ReadingService`.
    - Extend `backend/tests/integration/test_auth_flow.py` (create it if absent, mirroring the auth test style) — `test_limits_row_created` asserting `week_start is None` at creation + `test_double_login_single_limits_row` (two concurrent first-logins via the committed two-connection helper → exactly one row).
    Do NOT modify `test_readings_limit.py` here (Plan 02 owns the refund-compatibility extension). Run the suite to confirm collection is clean and nothing un-xfailed turns red.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/integration/test_limit_concurrency.py tests/integration/test_limits_reset.py tests/integration/test_throttle.py tests/unit/test_determine_access.py tests/integration/test_paywall_block.py tests/integration/test_auth_flow.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `committed_seeded_catalog` + a two-independent-connection helper exist in `tests/integration/conftest.py`, both skip cleanly when `_db_ready` is False, and the committed fixture deletes its seeded rows at teardown.
    - All six files exist with the test names from VALIDATION.md; each importable symbol from Plans 02/03 is referenced (so the stubs fail loudly when the implementation is wrong, not silently).
    - The concurrency stub asserts `free_used_this_week == 3` (not 4) and documents the mutation-test break-step in a comment.
    - Running the listed files collects with zero errors and reports only `xfailed`/`skipped` (no unexpected `failed`/`error`) — the full backend suite baseline (83 pass / 65 skip) is not regressed.
    - The determine_access unit stub asserts SUBSCRIPTION is selected before PAID (the Phase-7 bucket-order seam).
  </acceptance_criteria>
  <done>The committed concurrency fixture + 6 red/extended test files exist, collect cleanly, skip without Docker/Redis, and reference every symbol Plans 02/03 must provide.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint — apply migration 0002 against a real database</name>
  <what-built>Alembic revision 0002 (`week_start` DATE→TIMESTAMP + UNIQUE(user_id)) authored against `Base.metadata` but NOT applied — there is no Docker/Postgres in the agent environment (locked env constraint).</what-built>
  <how-to-verify>
    1. Against a real database: `cd backend && uv run alembic upgrade head`. Confirm it applies without error and `alembic current` shows `0002_user_limits_rolling_window`.
    2. Confirm the column type changed and the constraint exists, e.g. `\d user_limits` in psql — `week_start` is `timestamp with time zone` and `uq_user_limits_user_id` is a UNIQUE constraint.
    3. Confirm existing rows survived (any pre-existing ISO-Monday DATE values are now midnight timestamptz, not NULL, not errored).
    4. (Reversibility) `uv run alembic downgrade -1` then `upgrade head` again succeeds.
  </how-to-verify>
  <resume-signal>Type "approved" once the migration applies cleanly and self-heals existing rows, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/auth/telegram | Untrusted initData; identity (`telegram_id`) derived ONLY from the validated blob, never the body (existing T-04-01) |
| concurrent first-logins → user_limits INSERT | Two requests for the same brand-new user could race to insert two rows |
| Alembic migration → existing user_limits rows | A type change must not destroy or NULL existing quota state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `user_limits` row-ensure at auth | mitigate | `INSERT … ON CONFLICT (user_id) DO NOTHING` + the new UNIQUE constraint — a double-login can never create two rows or a row a later `scalar_one()` read would choke on (Pattern 5, Task 2/Task 1) |
| T-06-02 | Elevation | The Phase-4 "no user_limits row → treated as unlimited" gap | mitigate | D-02 guarantees a row at auth; after this no authenticated user is ever unbounded (the gap the whole phase closes) |
| T-06-03 | Denial of Service / data loss | The `week_start` DATE→TIMESTAMP migration | mitigate | `postgresql_using` cast self-heals existing rows (no NULL, no error); fully reversible `downgrade`; applied under a human checkpoint against a real DB (no silent destructive change) |
| T-06-04 | Spoofing | identity at the upsert | accept (unchanged) | `telegram_id` still derived only from validated initData (existing T-04-01 control, untouched by this plan) |
| T-06-SC | Tampering | npm/pip installs | mitigate | Zero new packages this phase (RESEARCH "Standard Stack: No new runtime dependencies"); if any package is added it is gated behind a blocking human checkpoint |
</threat_model>

<verification>
- `cd backend && uv run pytest -q` — full suite collects; baseline 83 pass / 65 skip not regressed; new stub files report xfailed/skipped only.
- `cd backend && uv run ruff check app/ tests/` — clean.
- Migration apply + reversibility verified at the human checkpoint against a real DB (no Docker in agent env).
</verification>

<success_criteria>
- One reversible Alembic revision (0002) migrates `week_start` to TIMESTAMP and adds UNIQUE(user_id), self-healing existing rows.
- `UserLimits` model + `LimitsOut` schema reflect the TIMESTAMP `week_start`.
- A brand-new user gets a race-safe `user_limits` row at auth with `week_start = NULL`; the ISO-week helper is removed.
- The committed two-connection concurrency fixture + six red/extended test files exist, collect cleanly, and reference every symbol Plans 02/03 must provide.
</success_criteria>

<output>
Create `.planning/phases/06-free-limits-soft-paywall/06-01-SUMMARY.md` when done. Record the migration apply step as a user-smoke (no Docker in agent env), the new fixture names, and the exact symbol names the stubs expect from Plans 02/03 (so those plans match them).
</output>
