---
phase: 06-free-limits-soft-paywall
plan: 04
type: execute
wave: 3
depends_on: [06-02, 06-03]
files_modified:
  - frontend/src/reading/copy.ts
  - frontend/src/reading/types.ts
  - frontend/src/api/auth.ts
  - frontend/src/reading/createReading.ts
  - frontend/src/reading/limitCopy.ts
  - frontend/src/components/PaywallSheet.tsx
  - frontend/src/components/ThrottleToast.tsx
  - frontend/src/components/CatalogScreen.tsx
  - frontend/src/components/profile/ProfileScreen.tsx
  - frontend/src/reading/createReading.test.ts
  - frontend/src/reading/limitCopy.test.ts
  - frontend/src/components/profile/ProfileScreen.test.tsx
autonomous: false
requirements: [LIMIT-01, LIMIT-03, LIMIT-05]
must_haves:
  truths:
    - "After 3 free readings the user sees a soft in-character paywall bottom-sheet (no fear, no pressure, no 'buy' button) with a per-user reset countdown — NOT a generic upgrade modal (success-criterion 1, LIMIT-01, D-03/D-04/D-05)"
    - "createReading rejects with a DISCRIMINABLE error so one catch routes to three distinct surfaces: HTTP 429 → throttle toast (kind:'throttle'); limit-block body (reason='paywall') → paywall sheet (kind:'paywall') with reset_at; any other non-completed → the existing failure band (kind:'failure') — never conflated (D-08, error-transport contract)"
    - "The transient throttle toast (auto-dismiss ~3.5-4s, reuse the UndoSnackbar setTimeout+AnimatePresence pattern) is visually and behaviorally distinct from the persistent paywall sheet (D-08)"
    - "The remaining free count «Осталось N из 3» shows subtly near «Начать расклад» AND in the profile, sourced from GET /api/me limits via useMe; a gentle accent «последний на этой неделе» hint appears at exactly 1 remaining; the selection count is suppressed at 0 (the paywall carries it) (D-09/D-10)"
    - "The Phase-5 ProfileScreen.test.tsx assertion that the readings count is ABSENT is INVERTED to assert the count is PRESENT (D-09 un-hides the Phase-5 D-08 block) - updated, not deleted"
    - "Every new string lives in copy.ts (or the ban-list-scanned limitCopy.ts) and passes BANNED_BRAND_TOKENS - zero AI/ИИ/нейросеть/модель/сгенерировано, no fear/«приговор»/pressure (SAFE-06)"
    - "createReading keeps its existing signature and MockReading return type - only the error type is extended, not the return shape (D-05/D-07 architecture guard)"
  artifacts:
    - path: "frontend/src/reading/createReading.ts"
      provides: "discriminated ReadingError (kind: throttle | paywall | failure) thrown from the one mutation; resetAt carried on the paywall branch"
      contains: "kind"
    - path: "frontend/src/components/PaywallSheet.tsx"
      provides: "bottom-sheet paywall (scrim + glass sheet, PAYWALL_* copy, formatReset countdown, dismiss preserves selection)"
      contains: "PAYWALL_TITLE"
    - path: "frontend/src/components/ThrottleToast.tsx"
      provides: "transient auto-dismiss throttle toast (THROTTLE_MESSAGE, UndoSnackbar pattern)"
      contains: "THROTTLE_MESSAGE"
    - path: "frontend/src/reading/limitCopy.ts"
      provides: "formatRemaining + formatReset pure helpers (RU plural rules, clamp >=0, NaN-guard, «совсем скоро» fallback)"
      contains: "formatReset"
    - path: "frontend/src/components/CatalogScreen.tsx"
      provides: "paywall trigger at free_left===0, throttle/paywall/failure catch routing, remaining-count line + 1-remaining hint"
      contains: "PaywallSheet"
  key_links:
    - from: "frontend/src/components/CatalogScreen.tsx handleStart catch"
      to: "PaywallSheet / ThrottleToast / failure band"
      via: "switch on the thrown ReadingError.kind"
      pattern: "kind"
    - from: "frontend/src/components/CatalogScreen.tsx + ProfileScreen.tsx"
      to: "GET /api/me limits"
      via: "useMe().data.limits then formatRemaining(free_left, total)"
      pattern: "formatRemaining"
    - from: "frontend/src/components/PaywallSheet.tsx"
      to: "the per-user reset moment"
      via: "formatReset(resetAt) rendered after PAYWALL_RESET_LEAD"
      pattern: "formatReset"
---

<objective>
Build the three locked UI-SPEC surfaces on the existing screens (no new routes): the soft paywall bottom-sheet, the transient throttle toast, and the subtle remaining-count line — all routed from one discriminated `createReading` error so the throttle (429), the paywall (reason='paywall' body), and a generation failure are never conflated (D-08). Un-hide the Phase-5 profile count block (D-09) and invert its now-wrong-direction regression test.

Purpose: the paywall must feel like an honest "the colода rests, returns on {date}" (TZ §11.2) — the countdown (D-04) is the antidote to a dead-end. The count (D-09/D-10) sets an honest expectation so the block is never a surprise. Backend gates are authoritative; this is display + transport branching only.

Output: a discriminated `ReadingError`, a `PaywallSheet` + `ThrottleToast`, the `formatRemaining`/`formatReset` helpers, the count surfaces on Catalog + Profile, the inverted ProfileScreen test, and brand-safe copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-free-limits-soft-paywall/06-CONTEXT.md
@.planning/phases/06-free-limits-soft-paywall/06-UI-SPEC.md
@.planning/phases/06-free-limits-soft-paywall/06-02-SUMMARY.md
@.planning/phases/06-free-limits-soft-paywall/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: limit copy + formatRemaining/formatReset helpers + discriminated ReadingError transport</name>
  <files>frontend/src/reading/copy.ts, frontend/src/reading/limitCopy.ts, frontend/src/reading/types.ts, frontend/src/reading/createReading.ts, frontend/src/reading/limitCopy.test.ts, frontend/src/reading/createReading.test.ts</files>
  <behavior>
    - formatRemaining(left, total) returns «Осталось 2 из 3»; clamps left to >=0 (never negative); guards NaN (returns the safe fallback / empty, never «NaN из 3»).
    - formatReset(resetAt, now?): within ~48h returns «через 1 день»/«через 2 дня»/«через 5 дней» (RU plural день/дня/дней); else an absolute «16 июня» (RU month genitive); resetAt absent/invalid returns «совсем скоро» (never «NaN», never empty).
    - createReading: HTTP 429 throws ReadingError{kind:'throttle'}; non-OK other status throws kind:'failure'; 200 body with status!=='completed' AND reason==='paywall' throws kind:'paywall' carrying resetAt; other non-completed 200 throws kind:'failure'. On success the MockReading return is unchanged.
    - createReading signature + MockReading return type are unchanged (only the thrown error type is new).
  </behavior>
  <read_first>
    - frontend/src/reading/copy.ts (BANNED_BRAND_TOKENS / containsBannedBrandToken lines 17-23 — the ban-list the new strings MUST pass; the existing READING_ERROR/READING_RETRY/READING_CHANGE_DECK §9.8 block lines 168-171 which stays DISTINCT; the centralized-copy convention)
    - frontend/src/reading/createReading.ts (the WHOLE file: the signature + MockReading return contract lines 137-179; the current generic `throw new Error` on non-OK line 158 and on non-completed line 167 — these become the discriminated throws; ReadingOutResponse usage)
    - frontend/src/reading/types.ts (ReadingOutResponse lines 86-92 — add `reason` + `reset_at`; the existing snake_case fields)
    - .planning/phases/06-free-limits-soft-paywall/06-UI-SPEC.md (Copywriting Contract: the verbatim PAYWALL_*/THROTTLE_*/LIMIT_* constants + the interpolation contracts for formatRemaining/formatReset incl. the «совсем скоро» fallback and the RU plural rules; the Error-branch contract table)
    - .planning/phases/06-free-limits-soft-paywall/06-02-SUMMARY.md + 06-03-SUMMARY.md (the EXACT backend field names — the `reason` value `"paywall"`, the `reset_at` field, the 429 status — so the discriminant matches what the backend sends)
  </read_first>
  <action>
    Add the verbatim UI-SPEC constants to `frontend/src/reading/copy.ts`: `PAYWALL_TITLE`, `PAYWALL_RESET_LEAD` (trailing space), `PAYWALL_SOON_NOTE`, `PAYWALL_DISMISS`, `THROTTLE_MESSAGE`, `LIMIT_REMAINING_PREFIX`, `LIMIT_LAST_ONE_HINT`, `PROFILE_LIMIT_LABEL` (exact Russian strings from the UI-SPEC Copywriting table). Create `frontend/src/reading/limitCopy.ts` exporting two PURE helpers (kept here so `limitCopy.test.ts` plus the copy ban-list scan reach them): `formatRemaining(left: number, total: number): string` (clamp `left = Math.max(0, left)`; if `Number.isNaN(left) || Number.isNaN(total)` return a safe fallback; compose `Осталось ${left} из ${total}`) and `formatReset(resetAt: string | Date, now?: Date): string` (parse to Date; invalid → «совсем скоро»; compute day delta; <=~2 days → relative with a RU `день/дня/дней` plural chooser; else absolute «D MMMM» with a RU month-genitive table). Extend `frontend/src/reading/types.ts` `ReadingOutResponse` with `reason?: string | null` and `reset_at?: string | null`. In `frontend/src/reading/createReading.ts`, define and export `class ReadingError extends Error` carrying a readonly `kind: "throttle" | "paywall" | "failure"` and an optional readonly `resetAt?: string | null`; replace the two generic `throw new Error(...)`: on `!response.ok`, throw `new ReadingError(response.status === 429 ? "throttle" : "failure", ...)`; on a 200 body that is not completed, if `data.reason === "paywall"` throw `kind:"paywall"` carrying `data.reset_at` as `resetAt`, else `kind:"failure"`. Keep `createReading`'s signature, the `mapReadingOutToMock` success path, and the `MockReading` return type EXACTLY as-is (D-05/D-07 guard — extend only the error). Create `frontend/src/reading/limitCopy.test.ts` (vitest) covering the formatRemaining/formatReset behaviors above (clamp, NaN-guard, plural rules, absolute fallback) and extend `frontend/src/reading/createReading.test.ts` (create if absent, mirroring the project test style) asserting the three discriminated branches (429→throttle, paywall-body→paywall with resetAt, other-non-completed→failure) by stubbing `fetch`/`apiFetch`. Confirm `reading/copy.test.ts` still scans the added constants clean.
  </action>
  <verify>
    <automated>cd frontend && node_modules/.bin/vitest run src/reading/limitCopy.test.ts src/reading/createReading.test.ts src/reading/copy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `formatRemaining` clamps negative to 0 and guards NaN; `formatReset` returns RU-plural relative days within ~48h, an absolute RU-genitive date beyond, and «совсем скоро» when resetAt is absent/invalid (never «NaN»/empty).
    - `createReading` throws `ReadingError` with `kind` in {throttle, paywall, failure}: 429→throttle, reason==='paywall' body→paywall (with `resetAt`), other non-completed→failure; the success path + signature + MockReading return are unchanged.
    - `ReadingOutResponse` carries `reason`/`reset_at`.
    - All three vitest files pass; `copy.test.ts` confirms the new constants carry no banned token.
  </acceptance_criteria>
  <done>The discriminated ReadingError + the two pure limit helpers + the new brand-safe copy exist and are tested; createReading's public surface is unchanged except the error type.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PaywallSheet + ThrottleToast components</name>
  <files>frontend/src/components/PaywallSheet.tsx, frontend/src/components/ThrottleToast.tsx</files>
  <behavior>
    - PaywallSheet (open): renders PAYWALL_TITLE (display), PAYWALL_RESET_LEAD + formatReset(resetAt) (the reset value accent-tinted), PAYWALL_SOON_NOTE (quiet), and a dismiss affordance labelled PAYWALL_DISMISS over a dimmed scrim; dismissing calls onDismiss; NO «buy»/price/Stars/dead CTA anywhere.
    - PaywallSheet enters/exits via AnimatePresence with the locked screen-enter token (compositor-only opacity/translateY).
    - ThrottleToast (open): renders THROTTLE_MESSAGE on a glass surface, auto-dismisses after ~3.5-4s (setTimeout cleared on unmount/close), no buttons.
    - Both contain zero banned brand tokens and no red/alarm hue (a blocked reading is a neutral state).
  </behavior>
  <read_first>
    - frontend/src/components/history/UndoSnackbar.tsx (the WHOLE file: the self-contained `useEffect` setTimeout + AnimatePresence transient pattern lines 32-79, the `m.div` enter/exit `{opacity, y}`, the styling, the safe-area `getContentSafeAreaInsets` bottom padding — ThrottleToast reuses this shape with a ~3.5-4s window)
    - frontend/src/components/profile/ProfileScreen.tsx (the `GLASS` const lines 34-38 + the `m.*` from "motion/react-m" import + the deck-var styling to reuse for the sheet/toast surfaces)
    - frontend/src/reading/copy.ts + frontend/src/reading/limitCopy.ts (Task 1's PAYWALL_*/THROTTLE_* constants + formatReset)
    - .planning/phases/06-free-limits-soft-paywall/06-UI-SPEC.md (Surface 1 Soft paywall: form bottom-sheet over scrim, contents top→bottom separated by lg, the accent reserved-for list, the motion token `{duration:0.28, ease:[0.16,1,0.3,1]}`, what it must NOT contain; Surface 2 Throttle message: transient toast distinct from the sheet, the distinction table; Telegram safe-area + the `m.*` LazyMotion namespace; the discouraged-haptic note)
  </read_first>
  <action>
    Create `frontend/src/components/PaywallSheet.tsx` with props `open: boolean`, `resetAt?: string | null`, `onDismiss: () => void`. Render (under `AnimatePresence`) a dimmed scrim (`color-mix(in srgb, var(--deck-bg) ~70%, transparent)`, animate `opacity` only) plus a glass bottom-sheet (reuse the `GLASS` language) pinned to the `fixed inset-x-0 bottom-0` region with dynamic safe-area bottom padding `16 + Math.max(getSafeAreaInsets().bottom, getContentSafeAreaInsets().bottom)` (NEVER `env()`/`100vh`). Contents top→bottom, each block separated by 24px: `PAYWALL_TITLE` (Display, `--deck-soft`); a Body line `PAYWALL_RESET_LEAD` followed by the `formatReset(resetAt)` value wrapped in a span tinted `var(--deck-accent)` (the single hopeful focal point); `PAYWALL_SOON_NOTE` (Body, `opacity-70`); a dismiss glyph button (reuse the `h-10 w-10` icon-button pattern, hit-area padded to >=44px, `aria-label={PAYWALL_DISMISS}`) calling `onDismiss`, and the scrim is tap-to-dismiss. Motion: sheet `initial {opacity:0,y:24}` → `animate {opacity:1,y:0}` → `exit {opacity:0,y:24}`, `transition {duration:0.28, ease:[0.16,1,0.3,1]}`; scrim crossfades opacity over 0.28s. Use `m.*` from "motion/react-m" (renders under FlowRoot's LazyMotion). NO price/Stars/«buy»/dead CTA, NO red/alarm hue, NO live-ticking clock. An optional soft `haptic.selection()` on open is allowed; never `notify("warning"/"error")`. Create `frontend/src/components/ThrottleToast.tsx` with props `open: boolean`, `onDismiss: () => void` — a transparent copy of the `UndoSnackbar` transient pattern (self-contained `useEffect` setTimeout ~3500-4000ms cleared on unmount/close; AnimatePresence enter `{opacity:0,y:8}` → `{opacity:1,y:0}` → exit `{opacity:0,y:-8}`), rendering `THROTTLE_MESSAGE` on a glass pill near the bottom safe area, no buttons, `role="status" aria-live="polite"`. No new dependency.
  </action>
  <verify>
    <automated>cd frontend && node_modules/.bin/tsc --noEmit --pretty false 2>&1 | tail -5 && node -e "const fs=require('fs');for(const f of ['src/components/PaywallSheet.tsx','src/components/ThrottleToast.tsx']){const s=fs.readFileSync(f,'utf8');if(/env\(|100vh/.test(s))throw new Error(f+': forbidden env/100vh');if(/купи|оплат|Stars|XTR|\bbuy\b/i.test(s))throw new Error(f+': purchase affordance present');}console.log('OK components')"</automated>
  </verify>
  <acceptance_criteria>
    - `PaywallSheet` renders the four blocks (title / reset-lead+accent-tinted countdown / soon-note / dismiss) over a scrim, uses dynamic safe-area padding (no `env()`/`100vh`), and contains no purchase affordance, no red/alarm hue, no ticking clock.
    - `ThrottleToast` auto-dismisses after ~3.5-4s via a self-contained setTimeout (cleared on close/unmount), has no buttons, and is a glass pill distinct from the sheet.
    - Both use `m.*` from "motion/react-m" with compositor-only motion.
    - `tsc --noEmit` passes; the node guard prints `OK components` (no forbidden env/100vh, no purchase strings).
  </acceptance_criteria>
  <done>PaywallSheet (persistent sheet+scrim) and ThrottleToast (transient pill) exist, are type-clean, brand-safe, purchase-free, and visually distinct (D-08).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Catalog (catch routing + count + 1-remaining hint) + Profile count block + invert the D-08 test</name>
  <files>frontend/src/api/auth.ts, frontend/src/components/CatalogScreen.tsx, frontend/src/components/profile/ProfileScreen.tsx, frontend/src/components/profile/ProfileScreen.test.tsx</files>
  <behavior>
    - CatalogScreen: when free_left===0 (from useMe limits), tapping «Начать расклад» opens the PaywallSheet instead of starting; the handleStart catch routes ReadingError.kind: throttle→ThrottleToast, paywall→PaywallSheet (with resetAt from the error), failure→the existing READING_ERROR band.
    - CatalogScreen: a quiet «Осталось N из 3» line near the CTA when limits present and free_left>0; at free_left===1 also «последний на этой неделе» tinted accent; at free_left===0 the count line is suppressed (the sheet carries the state); while useMe pending/absent, render nothing.
    - ProfileScreen: a glass block (PROFILE_LIMIT_LABEL + formatRemaining) between identity and settings, sourced from useMe limits.
    - ProfileScreen.test.tsx: the D-08 «count absent» test is inverted to assert the count IS present (D-09); the other Profile tests still pass.
  </behavior>
  <read_first>
    - frontend/src/components/CatalogScreen.tsx (the WHOLE file: `useMe()` already called line 68; `handleStart` try/catch lines 83-115 (the generic `catch { setStartError(true) }` becomes the kind-switch); `handleChangeDeck` line 120 (dismiss preserves question, mirror for the sheet); the sticky CTA band lines 307-390 + the `START_GATE_HINT` Label styling line 324; the `m.*`/copy imports lines 1-25)
    - frontend/src/components/profile/ProfileScreen.tsx (the WHOLE file: `useMe()` line 138, the `GLASS` block, the DELIBERATE-OMISSION comment lines 8-10 + the identity/settings sections lines 161-206 — insert the count block between them, and update the omission comment to reflect D-09 un-hiding)
    - frontend/src/components/profile/ProfileScreen.test.tsx (the WHOLE file: the `FREE_WEEKLY_LIMIT = 37` sentinel line 24, the `ME_RESPONSE.limits` mock lines 39-45, and the D-08 test lines 134-143 `expect(container.textContent).not.toContain(String(FREE_WEEKLY_LIMIT))` — INVERT this to assert the count IS rendered; the other tests lines 113-178)
    - frontend/src/reading/createReading.ts (Task 1's `ReadingError` + `kind`), frontend/src/reading/copy.ts + limitCopy.ts (LIMIT_REMAINING_PREFIX/LIMIT_LAST_ONE_HINT/PROFILE_LIMIT_LABEL + formatRemaining), frontend/src/hooks/useMe.ts (the `useMe` shape: `data.limits.free_weekly_limit`/`free_used_this_week`)
    - frontend/src/api/auth.ts (the `SessionLimits` interface lines 23-29 — it has NO `week_start`; the backend `LimitsOut` (schemas/auth.py) already serializes `week_start`, so add `week_start?: string | null` to the interface or the `limits?.week_start` read below is a TS2339 error that fails this task's `tsc --noEmit` gate)
    - .planning/phases/06-free-limits-soft-paywall/06-UI-SPEC.md (Surface 3 Remaining-count: selection placement + the 1-remaining accent hint + 0-suppress + loading-absent renders-nothing; profile un-hide between identity and settings; the «invert the Phase-5 assertion» flag)
  </read_first>
  <action>
    In `frontend/src/api/auth.ts`: add `week_start?: string | null;` to the `SessionLimits` interface (the backend `LimitsOut` already serializes it; without this field the `limits?.week_start` read below is a TS2339 error that fails the `tsc --noEmit` verify gate). `useMe`'s limits reuse this interface, so the field propagates to `meQuery.data.limits`. Then in `frontend/src/components/CatalogScreen.tsx`: import `PaywallSheet`, `ThrottleToast`, `ReadingError`, `formatRemaining`, and `LIMIT_LAST_ONE_HINT`. Compute `const limits = meQuery.data?.limits;` and `const freeLeft = limits ? Math.max(0, limits.free_weekly_limit - limits.free_used_this_week) : undefined;`. Add component state `const [paywallOpen, setPaywallOpen] = useState(false); const [paywallResetAt, setPaywallResetAt] = useState<string | null>(null); const [throttleOpen, setThrottleOpen] = useState(false);`. In `handleStart`, BEFORE calling `createReading`, if `freeLeft === 0` → `setPaywallResetAt(limits?.week_start ? ... : null); setPaywallOpen(true); return;` (open the sheet instead of starting; the precise resetAt is recomputed from the catch on the belt-and-suspenders path). Replace the generic `catch {}` with `catch (err) { if (err instanceof ReadingError && err.kind === "throttle") { setThrottleOpen(true); } else if (err instanceof ReadingError && err.kind === "paywall") { setPaywallResetAt(err.resetAt ?? null); setPaywallOpen(true); } else { setStartError(true); } setIsStarting(false); }`. Render `<PaywallSheet open={paywallOpen} resetAt={paywallResetAt} onDismiss={() => setPaywallOpen(false)} />` and `<ThrottleToast open={throttleOpen} onDismiss={() => setThrottleOpen(false)} />` at the end of the `<main>` (dismiss preserves question + selections — never clear `question`). Add the remaining-count Label inside the sticky band, ABOVE the CTA, styled like `START_GATE_HINT` (`px-1 text-center text-sm opacity-70`): render ONLY when `limits` is present AND `freeLeft !== undefined && freeLeft > 0` → `formatRemaining(freeLeft, limits.free_weekly_limit)`; when `freeLeft === 1` ALSO render `LIMIT_LAST_ONE_HINT` in a second line tinted `var(--deck-accent)` at full opacity (one tint bump, no size/weight bump); when `freeLeft === 0` render nothing (the sheet carries it); while `meQuery.isPending` or `limits` absent render nothing (no skeleton, no blank «осталось»). In `frontend/src/components/profile/ProfileScreen.tsx`: import `PROFILE_LIMIT_LABEL` + `formatRemaining`; between the identity `<section>` and the settings `<section>`, when `data.limits` is present, add a small `GLASS` block with `PROFILE_LIMIT_LABEL` (Label eyebrow) + `formatRemaining(Math.max(0, free_weekly_limit - free_used_this_week), free_weekly_limit)` (Body) — free count ONLY, no subscription/paid, no «buy». Update the lines 8-10 DELIBERATE-OMISSION comment to state the count is now un-hidden per D-09. In `frontend/src/components/profile/ProfileScreen.test.tsx`: INVERT the D-08 test (lines 134-143) — rename it to assert the count IS rendered, e.g. `expect(getByText(...formatRemaining(36, 37)...)).toBeTruthy()` (the mock is `free_weekly_limit: 37, free_used_this_week: 1` → 36 left) and that `PROFILE_LIMIT_LABEL` appears; keep the other three Profile tests passing (the brand-safe scan now also covers the new count copy). Run `tsc`, the targeted vitest files, and the full FE suite.
  </action>
  <verify>
    <automated>cd frontend && node_modules/.bin/tsc --noEmit --pretty false 2>&1 | tail -5 && node_modules/.bin/vitest run src/components/profile/ProfileScreen.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - CatalogScreen opens the PaywallSheet when `freeLeft === 0` on CTA tap (instead of starting) AND routes the `handleStart` catch by `ReadingError.kind` to throttle toast / paywall sheet / failure band (three distinct surfaces, D-08).
    - The remaining-count Label renders «Осталось N из 3» only when limits present and freeLeft>0; the accent «последний на этой неделе» hint shows at freeLeft===1; nothing renders at freeLeft===0 or while useMe is pending.
    - ProfileScreen shows a free-count glass block (label + formatRemaining) between identity and settings; no subscription/paid/buy; the omission comment is updated to D-09.
    - The Phase-5 ProfileScreen D-08 «count absent» test is INVERTED to assert the count is PRESENT; all four Profile tests pass; `tsc --noEmit` clean.
    - The full FE suite (`node_modules/.bin/vitest run`) is green and not regressed.
  </acceptance_criteria>
  <done>The paywall opens on exhaustion + the catch routes all three error kinds; the subtle count + 1-remaining hint render on Catalog; the profile count block is un-hidden; the inverted Profile test passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint — verify the three surfaces in the real Mini App</name>
  <what-built>The three UI surfaces (paywall bottom-sheet, throttle toast, remaining-count line + profile block) are implemented and unit-tested headlessly, but their real rendering in the Telegram Mini App (per-deck atmosphere, safe-area, motion, real exhausted-limit state) needs a live visual check.</what-built>
  <how-to-verify>
    1. With a deployed build and a live backend: exhaust the 3 free readings for a test user. On the 4th, tapping «Начать расклад» surfaces the soft paywall bottom-sheet — confirm the headline, a sensible reset countdown («вернутся через N дней» / a date), the «скоро ещё» note, NO «buy»/price/Stars, NO red/alarm hue, and that dismissing preserves the question + selections.
    2. Confirm the remaining-count line «Осталось N из 3» shows quietly near the CTA at 2 and 3 left, shows the accent «последний на этой неделе» hint at exactly 1 left, and is suppressed at 0 (the sheet carries it). Confirm the same count appears in the profile.
    3. Fire reading requests rapidly (<10-15s apart) to trip the throttle — confirm the transient «Колода переводит дыхание» toast appears, auto-dismisses, and is visibly DIFFERENT from the paywall sheet (D-08), then a normal-paced retry works.
    4. Scan all three surfaces for any «AI / ИИ / нейросеть / модель» or fear/pressure copy — there must be none (SAFE-06 / TZ §11.2).
  </how-to-verify>
  <resume-signal>Type "approved" once the paywall + count + throttle render correctly in the real Mini App with brand-safe copy, or describe the issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend response → FE error branching | The FE must discriminate throttle vs paywall vs failure from the backend's signals only |
| GET /api/me limits → displayed count | The count is non-authoritative display chrome; the real gate is server-side |
| user-supplied display state → limit decision | The FE count/sheet must never be able to influence the server's allow/deny |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Elevation | the FE remaining-count / paywall trigger | accept (display-only) | The count + the `freeLeft===0` CTA gate are display chrome; the authoritative gate is the backend atomic consume (Plan 02) + throttle (Plan 03). A user editing the FE state can at most open/skip their own sheet — the server still blocks the 4th reading. Per the Architectural Responsibility Map (display tier) |
| T-06-15 | Information Disclosure | the paywall countdown | mitigate | `reset_at` is the caller's OWN reopen moment (their `week_start + 7d`); no cross-user data; the FE never receives another user's limits (T-05 IDOR discipline on `GET /api/me`) |
| T-06-16 | Spoofing (brand) | the new paywall/throttle/count copy | mitigate | All strings in `copy.ts`/`limitCopy.ts`, scanned by `BANNED_BRAND_TOKENS` (SAFE-06) — no AI/нейросеть/модель; no fear/«приговор»/pressure (TZ §11.2); enforced by `copy.test.ts` + the ProfileScreen brand-safe test |
| T-06-17 | Tampering | XSS via the rendered count/reset/identity | mitigate | Every value renders as a React text node (no `dangerouslySetInnerHTML`) — same discipline as the existing ProfileScreen (T-05-XSS) |
| T-06-SC | Tampering | npm installs | mitigate | Zero new runtime dependency (UI-SPEC Registry Safety: hand-authored against existing components + `motion` already in the lockfile); none added by this plan |
</threat_model>

<verification>
- `cd frontend && node_modules/.bin/vitest run` — full FE suite green (baseline 101 + the new createReading/limitCopy/inverted-Profile assertions), not regressed.
- `cd frontend && node_modules/.bin/tsc --noEmit --pretty false` — 0 errors.
- `cd frontend && node_modules/.bin/vitest run src/reading/copy.test.ts` — the new PAYWALL_*/THROTTLE_*/LIMIT_* constants pass the SAFE-06 ban-list.
- Real-Mini-App paywall/count/throttle + brand-safe copy verified at the human checkpoint (Telegram WebApp + live exhausted-limit state).
</verification>

<success_criteria>
- After 3 free readings the user sees a soft, in-character paywall bottom-sheet with a per-user reset countdown — no fear, no «buy» (success-criterion 1, LIMIT-01, D-03/04/05).
- One `createReading` catch routes to three distinct surfaces (throttle toast / paywall sheet / failure band) — never conflated (D-08).
- The subtle «Осталось N из 3» count + the 1-remaining accent hint render on Catalog + Profile from `GET /api/me` (D-09/D-10); the Phase-5 absence test is inverted.
- All new copy is brand-safe (SAFE-06); zero new dependency; `createReading`'s signature/return unchanged.
</success_criteria>

<output>
Create `.planning/phases/06-free-limits-soft-paywall/06-04-SUMMARY.md` when done. Record the `ReadingError.kind` values, the inverted Profile test name, the final count/hint placement, and the live-Mini-App paywall/throttle checks as user-smokes (deploy-time).
</output>
